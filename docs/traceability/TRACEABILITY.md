# Traceability

Trace both directions: requirements reach evidence, and material code has a
requirement, defect, maintenance policy, or ADR.

| Requirement | Roadmap | Decision/interface | Implementation | Verification | PR/release | State |
|---|---|---|---|---|---|---|
| `RB-VERIFY-003-FR-001` | `PHASE-0-BOOTSTRAP`, `PHASE-0-EXIT` | `rb_domain::divergence::score`, `rb_verify_cli::score_replay_against_capture` | `crates/rb_domain/src/divergence.rs`, `crates/rb_verify_cli/src/lib.rs` | `rb_domain::divergence::tests` + `rb_verify_cli` tests (3) + manual end-to-end run against synthetic capture (6 frames matched, mean/max 0.25 uu) + manual end-to-end run against a real BakkesMod capture (`RB-VERIFY-002-FR-001`, 343 frames matched, mean ball distance 3640.81 uu) | bootstrap + cli-wiring + timestamp-alignment + real-capture-run commits | Implemented, Verified (algorithm; CLI wiring mechanical only, run on both a synthetic and now a real capture — neither is a fidelity comparison, closing `PHASE-0-EXIT`'s own literal exit criterion) |
| `RB-VERIFY-003-NFR-001` | `PHASE-0-BOOTSTRAP` | `rb_domain::divergence::score` | `crates/rb_domain/src/divergence.rs` | `empty_inputs_score_zero_not_nan` | bootstrap commit | Implemented, Verified |
| `RB-VERIFY-003-FR-002` (car-state scoring) | `PHASE-0-EXIT` | `rb_domain::divergence::score`, `Quat::angle_to` | `crates/rb_domain/src/{divergence,state}.rs` | `rb_domain::divergence::tests` (4 car-scoring tests) + `Quat::angle_to` tests (3) + manual end-to-end run (6 car pairs matched) | car-state-scoring commit | Implemented, Verified (algorithm; not yet a fidelity comparison) |
| `RB-VERIFY-003-FR-003` (timestamp alignment) | `PHASE-0-EXIT` | `rb_domain::divergence::score` (`max_timestamp_delta_secs`), `rb_verify_cli::DEFAULT_MAX_TIMESTAMP_DELTA_SECS` | `crates/rb_domain/src/divergence.rs`, `crates/rb_verify_cli/src/lib.rs` | `rb_domain::divergence::tests` (2 alignment-specific: different tick rates, shorter-sequence matching) + manual end-to-end run | timestamp-alignment commit | Implemented, Verified (algorithm; not yet a fidelity comparison) |
| `RB-VERIFY-003-FR-004` (divergence-growth diagnostic) | `PHASE-1-PHYSICS-CORE` | `rb_domain::divergence::score_windows` (windowed variant of `score`, same nearest-timestamp matching via a shared `matched_pairs`/`score_pairs` pipeline, partitioned into `window_secs`-wide buckets), `rb_verify_cli::score_capture_growth`, `rb-verify --self-growth <capture-file> [window-secs] [max-timestamp-delta-secs]` | `crates/rb_domain/src/divergence.rs`, `crates/rb_verify_cli/src/{lib,main}.rs` | 4 new `rb_domain::divergence::tests` (single-window reproduces `score` exactly, two-window known offset, first-window-starts-at-first-match, no-matched-pairs) + 3 new `rb_verify_cli` tests + manual run against the synthetic capture fixture (1 window, 5 frames) + manual run against `RB-PHYSICS-001-FR-077`'s own real capture (23 windows, abrupt derailment at t≈4-5s) | divergence-growth-diagnostic commit | Implemented, Verified (algorithm, CLI wiring, and now the real run this diagnostic exists for — see `RB-PHYSICS-001-FR-005`'s row and that spec's Verification plan for the abrupt-derailment finding) |
| `RB-VERIFY-001-FR-001..003` | `PHASE-0-REPLAY-INGEST` | `rb_domain::port::PhysicsStateSource`, `boxcars`+`subtr-actor` (see Cargo.toml comment) | `crates/rb_replay_ingest/src/{lib,convert}.rs` | `rb_replay_ingest` unit tests (14) + `corpus_check` bin (40/40 real owner replays) | replay-ingestion + corpus-check commits | Implemented, Verified (real fixture + 40-replay owner corpus) |
| `RB-VERIFY-001-NFR-003` (local corpus gate) | `PHASE-0-REPLAY-INGEST` | `AGENTS.md` local corpus convention | `crates/rb_replay_ingest/src/bin/corpus_check.rs` | manual run against `baileyrd/replays` (40/40 clean) | corpus-check commit | Implemented, Verified |
| `RB-VERIFY-001-FR-004` (input attachment) | `PHASE-0-REPLAY-INGEST` | `rb_domain::ControllerInput` (ADR-0005) | `crates/rb_replay_ingest/src/convert.rs` | 4 new `convert::tests` cases | capture-ingestion commit | Implemented, Verified |
| `RB-VERIFY-002-FR-001` (BakkesMod plugin) | `PHASE-0-CAPTURE-INGEST` | ADR-0005 (JSON-Lines format), grounded against a real `BakkesModSDK` clone (exact `ControllerInput`/`RBState`/`ArrayWrapper` fields, `HookEventWithCallerPost<CarWrapper>`); car enumeration switched from `ServerWrapper::GetPRIs()`+`PriWrapper::GetCar()` to `ServerWrapper::GetCars()` after a real capture showed the former returns a frozen, non-live car in freeplay | `bakkesmod-plugin/rusty_bullet_capture/` (`RustyBulletCapturePlugin.h/.cpp`, `CMakeLists.txt`, `README.md`) | 2 real captures (9,358 and 2,818 lines) schema-validated against ADR-0005 and parsed end-to-end via `rb_capture_ingest` | write-the-plugin commit, build-and-fix-GetCars commit | Implemented, Verified (built with MSVC/CMake, loaded into a real Rocket League + BakkesMod session, run in freeplay; manual BakkesMod-overlay single-timestamp cross-check still open, see spec) |
| `RB-VERIFY-002-FR-002` (capture parsing) | `PHASE-0-CAPTURE-INGEST` | `rb_domain::port::PhysicsStateSource`, ADR-0005 (JSON-Lines format) | `crates/rb_capture_ingest/src/{lib,wire}.rs` | `rb_capture_ingest` unit tests (10, synthetic fixture); scratch integration test against a real 2,818-line capture (not kept in the repo) | capture-ingestion commit | Implemented, Verified (synthetic fixture + real capture) |
| `RB-VERIFY-002-NFR-001` | `PHASE-0-CAPTURE-INGEST` | `rb_domain::error::IngestError` | `crates/rb_capture_ingest/src/lib.rs` | `malformed_line_reports_malformed_not_a_panic`, `missing_file_reports_io_error_not_a_panic` | capture-ingestion commit | Implemented, Verified |
| `RB-VERIFY-002-NFR-002` (recording overhead) | `PHASE-0-CAPTURE-INGEST` | depends on FR-001 | not started | not started | — | Not Started |
| `RB-PHYSICS-001-FR-001..003` | `PHASE-1-PHYSICS-CORE-V0` | ADR-0003 (fidelity target), ADR-0004 (Bullet3 port) | `crates/rb_physics_bullet` (`integrate`, `collision`, `solver`, `world`) | `rb_physics_bullet` unit tests (181, incl. sphere-path regression coverage) | bullet3-port + car-box + ball-vs-car + box-vs-box + multi-car + driven-input + boost + handbrake + jump + air-control + double-jump + wall-jump + dodge + variable-jump-height + flip-cancel + wall-jump-dodge + landing-auto-orientation + modeled-arena-footprint + curved-wall-transitions + curved-corner-wall-transitions + corner-wall-vertical-edge-fillets commits | Implemented, Verified (v0 scope; not yet scored against real data) |
| `RB-PHYSICS-001-FR-004` (car boxes, general inertia, multi-contact, ball-vs-car collision) | `PHASE-1-PHYSICS-CORE` | `RigidBody`/`Shape` (body.rs), `Mat3` (mat3.rs), `Quat::conjugate` (rb_domain) | `crates/rb_physics_bullet` (`body`, `mat3`, `collision`, `solver`, `world`) | `rb_physics_bullet` unit tests (32 new: inertia, box-vs-plane contact counts, multi-contact settling, sphere-vs-box contact generation, two-body solver momentum/restitution, end-to-end ball-vs-car bounce) | car-box + ball-vs-car commits | Implemented, Verified (unit tests only; not yet scored against real data) |
| `RB-PHYSICS-001-FR-005` (constant calibration) | `PHASE-1-PHYSICS-CORE` | no longer depends on `PHASE-0-EXIT` (closed); `RB-VERIFY-003-FR-004`'s diagnostic has run against `FR-076`/`FR-077`'s real score, and `FR-079`'s isolated replay has refined the resulting hypothesis | not started | not started | — | Not Started, but now actionable: the derailment reproduces standalone from a clean seed (`FR-079`), ruling out compounded drift; the evidence points to an orientation-rate divergence that begins during the grounded jump hold, *before* the dodge fires, amplified by the dodge's own orientation-relative impulse, plus a likely-separate post-dodge spin-rate mismatch matching `FR-069` — see `FR-079`'s own entry for the full evidence chain |
| `RB-PHYSICS-001-FR-076` (candidate-engine plumbing: seed + simulate recorded input) | `PHASE-1-PHYSICS-CORE` | `world::simulate`'s own doc comment (named this exact next step); RocketSim's own real source for `CAR_MASS`/`CAR_HALF_EXTENTS`/`BALL_MASS` | `crates/rb_physics_bullet/src/{body,world}.rs` (`RigidBody::standard_car`/`standard_ball`, `PhysicsWorld::from_frame`, `world::simulate_recorded`) | 13 new unit tests (6 in `body.rs`, 7 in `world.rs`); full workspace `fmt`/`clippy`/`test` green (335 `rb_physics_bullet` tests total) | implement-fr-076 commit | Implemented, Verified (hand-built-value tests only; a real-capture run is `FR-077`'s job) |
| `RB-PHYSICS-001-FR-077` (wire candidate engine into `rb_verify_cli`, first real run) | `PHASE-1-PHYSICS-CORE` | depends on `FR-076`; uses the real capture from `RB-VERIFY-002-FR-001` | `crates/rb_verify_cli/src/{lib,main}.rs` (`score_capture_against_candidate`, `is_grounded_and_neutral`, `rb-verify --self`) | 3 new unit tests (happy path against the synthetic capture fixture, missing-file, no-qualifying-frame `Malformed`); full workspace `fmt`/`clippy`/`test` green (388 tests workspace-wide); real-capture run (owner's machine, 2,818 frames): mean ball distance 2206.08 uu, mean car position/rotation/velocity distance 4508.71 uu / 2.12 rad / 1421.73 uu/s | implement-fr-077 commit | Implemented, Verified (real-capture run done; large divergence consistent with near-total trajectory decorrelation, not yet actionable for FR-005's own calibration — see spec's Interpretation note) |
| `RB-PHYSICS-001-FR-078` (car hitbox calibration: retune existing tests to the real `CAR_HALF_EXTENTS`) | `PHASE-1-PHYSICS-CORE` | `body::CAR_HALF_EXTENTS` (`FR-076`'s own confirmed real value, deliberately left unused at existing call sites at the time) | `crates/rb_physics_bullet/src/{body,collision,drive,net,solver,world}.rs` (every real-car test helper switched from the old `Vec3::new(60.0, 30.0, 18.0)` placeholder to `CAR_HALF_EXTENTS`) | No new tests (constant-correctness change, matching `FR-036`'s own precedent); all 335 pre-existing `rb_physics_bullet` tests pass unchanged; full workspace `fmt`/`clippy`/`test` green (388 tests workspace-wide) | implement-fr-078 commit | Implemented, Verified |
| `RB-PHYSICS-001-FR-079` (isolated dodge-derailment investigation) | `PHASE-1-PHYSICS-CORE` | No production decision — a real-data investigation confirming `RB-VERIFY-003-FR-004`'s abrupt-derailment finding and refining its hypothesis (pre-dodge orientation-rate drift amplified by the dodge's own orientation-relative impulse, plus a likely-separate post-dodge spin-rate mismatch matching `FR-069`) | `crates/rb_capture_ingest/fixtures/dodge-derailment.capture.jsonl` (new 347-frame real fixture, excerpted from `FR-077`'s own `test2.jsonl`); no production code changed | `rb_verify_cli::tests::isolated_replay_of_the_real_dodge_still_diverges_sharply` (new, 11 `rb_verify_cli` tests total) asserting `frames_compared == 347`, `pairs_compared == 347`, and the known-bad divergence bounds; full workspace `fmt`/`clippy`/`test` green (396 tests workspace-wide) | isolated-dodge-investigation commit | Findings recorded, no fix yet — see `FR-079`'s own spec entry for the full evidence chain |
| `RB-PHYSICS-001-FR-006` (car-vs-car collision, wired into a multi-car `PhysicsWorld`) | `PHASE-1-PHYSICS-CORE` | `collision::box_vs_box` (15-axis SAT, face-clip/edge-edge manifold); `PhysicsWorld.cars: Vec<RigidBody>` | `crates/rb_physics_bullet` (`collision`, `solver`, `world`) | `rb_physics_bullet` unit tests (4 for `box_vs_box`: no-contact, 4-point flat manifold, argument-order antisymmetry, partial rotated manifold; a generalized `resolve_contacts_between` no-net-rotation test; 3 for multi-car `PhysicsWorld`: builder appends cars, sequential `player_id`s, end-to-end two-cars-bounce-off-each-other) | box-vs-box + multi-car commits | Implemented, Verified (unit tests only, but wired into a real N-car scene — not just detection in isolation; not yet scored against real data) |
| `RB-PHYSICS-001-FR-007` (driven car input — ground throttle/steering) | `PHASE-1-PHYSICS-CORE` | `drive::apply_driven_forces`; `PhysicsWorld.car_inputs`/`set_car_input` | `crates/rb_physics_bullet` (`drive`, `world`) | `rb_physics_bullet` unit tests (8 in `drive`: neutral input is a no-op, throttle accelerates/caps/reverses/is grounded-only, steer is speed-gated and sign-correct; 2 in `world`: end-to-end throttle-driven forward motion + `frame()` input reporting, and a no-input-set regression guard) | driven-input commit | Implemented, Verified (ground throttle/steering; the dodge directional impulse/torque and wall jump not implemented; unit tests only, not yet scored against real data) |
| `RB-PHYSICS-001-FR-008` (boost) | `PHASE-1-PHYSICS-CORE` | `drive::apply_driven_forces` (boost force + drain); `PhysicsWorld.car_boost`/`set_car_boost` | `crates/rb_physics_bullet` (`drive`, `world`) | `rb_physics_bullet` unit tests (4 in `drive`: boost accelerates regardless of ground contact, drains over time and clamps at zero, no effect when empty, still drains at max speed; 2 in `world`: end-to-end airborne boost-driven forward motion + `frame()` boost reporting, and a new-car-starts-full regression guard) | boost commit | Implemented, Verified (unit tests only; the dodge directional impulse/torque and wall jump not implemented; not yet scored against real data) |
| `RB-PHYSICS-001-FR-009` (handbrake) | `PHASE-1-PHYSICS-CORE` | `drive::apply_driven_forces` (temporary friction reduction); `PhysicsWorld.car_base_friction` | `crates/rb_physics_bullet` (`drive`, `world`) | `rb_physics_bullet` unit tests (3 in `drive`: reduces friction while grounded, no effect while airborne, releasing restores base friction; 2 in `world`: end-to-end a-sliding-car-retains-more-slide-under-handbrake, and a releasing-handbrake-restores-a-custom-non-default-base-friction regression guard) | handbrake commit | Implemented, Verified (unit tests only; the dodge directional impulse/torque and wall jump not implemented; not yet scored against real data) |
| `RB-PHYSICS-001-FR-010` (single ground jump) | `PHASE-1-PHYSICS-CORE` | `drive::apply_driven_forces` (rising-edge impulse); `PhysicsWorld.car_jump_held` | `crates/rb_physics_bullet` (`drive`, `world`) | `rb_physics_bullet` unit tests (4 in `drive`: jump gives a grounded car upward velocity, no effect while airborne, holding doesn't re-fire, releasing-then-re-pressing fires again; 2 in `world`: end-to-end a-car-with-jump-input-leaves-the-ground, and a holding-jump-does-not-repeatedly-relaunch-the-car regression guard) | jump commit | Implemented, Verified (unit tests only; the dodge directional impulse/torque and wall jump not implemented; not yet scored against real data) |
| `RB-PHYSICS-001-FR-011` (air control) | `PHASE-1-PHYSICS-CORE` | `drive::apply_driven_forces` (pitch/yaw/roll torque, airborne-gated) | `crates/rb_physics_bullet` (`drive`, `world`) | `rb_physics_bullet` unit tests (4 in `drive`: no effect while grounded, a stationary airborne car can pitch/yaw/roll, `None` analog value is treated as neutral, opposite-sign yaw spins the opposite way; 2 in `world`: end-to-end a-car-with-air-control-input-reorients-itself-midair, and an air-control-does-not-reorient-a-grounded-car regression guard) | air-control commit | Implemented, Verified (unit tests only; the dodge directional impulse/torque and wall jump not implemented; not yet scored against real data) |
| `RB-PHYSICS-001-FR-012` (double jump) | `PHASE-1-PHYSICS-CORE` | `drive::apply_driven_forces` (second rising-edge impulse, gated on `double_jump_available`); `PhysicsWorld.car_double_jump_available` | `crates/rb_physics_bullet` (`drive`, `world`) | `rb_physics_bullet` unit tests (4 in `drive`: gives upward velocity when available, no effect when unavailable, consumed after use and doesn't refire, landing restores availability; 2 in `world`: end-to-end double-jump-after-a-ground-jump-gives-a-second-upward-kick, and a double-jump-is-not-available-again-mid-air-after-being-used regression guard) | double-jump commit | Implemented, Verified (unit tests only; the directional dodge variant is FR-014 (itself flip-cancelable via FR-016), and its variable-height hold window is FR-015; not yet scored against real data) |
| `RB-PHYSICS-001-FR-013` (arena walls and wall jump) | `PHASE-1-PHYSICS-CORE` | `PhysicsWorld.walls`/`with_wall`; `resolve_plane_contact` (renamed from `resolve_ground_contact`); `drive::apply_driven_forces` (`wall_normal` parameter, outward-plus-upward impulse, priority over the double jump, restores `double_jump_available` on mere contact) | `crates/rb_physics_bullet` (`drive`, `world`) | `rb_physics_bullet` unit tests (4 in `drive`: pushes outward and upward when touching a wall, no effect while grounded, takes priority over the double jump without consuming it, mere wall contact restores double-jump availability; 3 in `world`: end-to-end a-car-touching-a-wall-wall-jumps-outward-and-upward, end-to-end a-ball-bounces-off-a-wall-instead-of-passing-through (the same physical proof ball-vs-car collision already has, now for walls), and a double-jump-still-fires-when-a-wall-exists-but-is-not-touched regression guard) | wall-jump commit | Implemented, Verified (unit tests only; a modeled arena footprint beyond generic flat walls not implemented; the wall jump's own dodge variant is now FR-017; not yet scored against real data) |
| `RB-PHYSICS-001-FR-014` (dodge) | `PHASE-1-PHYSICS-CORE` | `drive::apply_driven_forces` (`pitch`/`roll`-gated branch inside the double jump's fresh press, `DODGE_DEADZONE`/`DODGE_SPEED`/`DODGE_ANGULAR_SPEED`, direct `RigidBody.angular_velocity` write) | `crates/rb_physics_bullet` (`drive`, `world`) | `rb_physics_bullet` unit tests (7 in `drive`: forward (pitch) dodge gives velocity+spin, lateral (roll) dodge gives velocity+spin, below-deadzone still gives a plain double jump, a dodge spends `double_jump_available`, opposite pitch dodges the opposite direction, a diagonal dodge combines both axes, no effect while grounded; 1 in `world`: end-to-end a-car-dodges-forward-after-a-ground-jump-when-pitched-in-the-air) | dodge commit | Implemented, Verified (unit tests only; its own variable-height hold window is FR-015, its spin is flip-cancelable via FR-016, the wall jump's own dodge variant is FR-017 — the drive/world regression tests originally proving "wall jump fires instead of a dodge" were repurposed for FR-017, since that's exactly the premise it reverses — and the landing auto-orientation assist is now FR-018; not yet scored against real data) |
| `RB-SIM-001-FR-001..002` | `PHASE-2-DETERMINISM` | — | not started | not started | — | Not Started |
| `RB-NET-001-FR-001` | `PHASE-3-NETCODE` | ADR-0001 (server-authoritative, client-predicted) | not started | not started | — | Not Started |
| `RB-NET-001-FR-002` | `PHASE-3-NETCODE` | — | not started | not started | — | Not Started |

## ADR traceability

| ADR | Decision | Traces to |
|---|---|---|
| ADR-0001 | Server-authoritative simulation with client-side prediction | `RB-NET-001-FR-001`, `PHASE-3-NETCODE` |
| ADR-0002 | Verification pipeline precedes physics implementation | `PHASE-0-*` gating `PHASE-1-PHYSICS-CORE`, all `RB-VERIFY-*` specs |
| `RB-PHYSICS-001-FR-015` (variable jump height) | `PHASE-1-PHYSICS-CORE` | `drive::apply_driven_forces` (`jump_hold_time_remaining` hold-window check/decrement before the ground jump's own re-arm, `JUMP_HOLD_MAX_DURATION`/`JUMP_HOLD_ACCELERATION`); `PhysicsWorld.car_jump_hold_time_remaining` | `crates/rb_physics_bullet` (`drive`, `world`) | `rb_physics_bullet` unit tests (4 in `drive`: holding jump adds more upward velocity than tapping it, releasing jump early stops the extra acceleration immediately, the extra acceleration stops accruing once the hold window expires, a double jump after a held ground jump is not boosted by the hold window; 2 in `world`: end-to-end a-held-ground-jump-reaches-greater-height-than-a-tapped-one, and a double-jump-after-a-held-ground-jump-still-gives-exactly-one-more-jump-speed-kick regression guard) | variable-jump-height commit | Implemented, Verified (unit tests only; scoped to the ground jump alone — the double jump, a dodge, and the wall jump remain fixed-impulse, matching real Rocket League; not yet scored against real data) |
| `RB-PHYSICS-001-FR-016` (flip-cancel) | `PHASE-1-PHYSICS-CORE` | `drive::apply_driven_forces` (`dodge_flip_active` flag: set by the dodge branch, explicitly cleared by the plain-double-jump branch, spent by a further fresh press to zero `RigidBody.angular_velocity`); `PhysicsWorld.car_dodge_flip_active` | `crates/rb_physics_bullet` (`drive`, `world`) | `rb_physics_bullet` unit tests (4 in `drive`: a second jump press cancels a dodge's spin outright and spends the flag, flip-cancel leaves linear velocity and `double_jump_available` untouched, a plain double jump clears a stale flag from an earlier dodge, a wall jump still takes priority over flip-cancel; 2 in `world`: end-to-end a-second-jump-press-cancels-a-dodges-spin-in-a-live-world, and a landing-and-a-new-double-jump-clears-a-stale-dodge-flip-flag-in-a-live-world regression guard — both stale-flag regression tests independently verified to fail without the explicit-clear fix) | flip-cancel commit | Implemented, Verified (unit tests only; applies equally to a wall-jump dodge's spin (FR-017); the landing auto-orientation assist is now FR-018; not yet scored against real data) |
| `RB-PHYSICS-001-FR-017` (wall-jump dodge) | `PHASE-1-PHYSICS-CORE` | `drive::apply_driven_forces` (`pitch`/`roll`-gated branch inside the wall jump's fresh press, reusing `DODGE_DEADZONE`/`DODGE_SPEED`/`DODGE_ANGULAR_SPEED`, consuming `double_jump_available` and arming `dodge_flip_active`) | `crates/rb_physics_bullet` (`drive`, `world`) | `rb_physics_bullet` unit tests (6 in `drive`: a wall-jump dodge consumes the double jump unlike a plain wall jump, its spin can be flip-cancelled, a below-deadzone stick deflection still gives a plain wall jump, opposite pitch dodges the opposite direction, a diagonal dodge combines both axes, plus the repurposed a-wall-jump-dodges-outward-and-upward-with-a-flip-when-touching-a-wall-with-stick-input (formerly asserting the reversed premise); 2 in `world`: end-to-end a-wall-jump-dodges-outward-and-upward-with-a-flip-in-a-live-world (also repurposed from the reversed-premise test) and a-wall-jump-dodges-spin-can-be-flip-cancelled-in-a-live-world) | wall-jump-dodge commit | Implemented, Verified (unit tests only; wall-jump dodge consuming `double_jump_available` — unlike a plain wall jump — is a documented simplification, since gating it on that flag would be vacuous given wall touch always restores it first; not yet scored against real data) |
| `RB-PHYSICS-001-FR-018` (landing auto-orientation assist) | `PHASE-1-PHYSICS-CORE` | `drive::apply_driven_forces` (continuous `up_axis(car).cross(&world_up) * LANDING_AUTO_UPRIGHT_TORQUE` restoring torque, gated on no active `pitch`/`roll` and no fresh `jump` press this step) | `crates/rb_physics_bullet` (`drive`, `world`) | `rb_physics_bullet` unit tests (4 in `drive`: a tilted airborne car gets a corrective torque, an already-upright airborne car gets none, no effect while grounded, no effect while actively pitch-air-controlling; 1 in `world`: end-to-end an-airborne-car-gradually-rights-itself-with-no-input-in-a-live-world over 120 steps) | landing-auto-orientation commit | Implemented, Verified (unit tests only; no ground-proximity trigger condition modeled — applies continuously whenever airborne instead, see spec Open questions; not yet scored against real data) |
| `RB-PHYSICS-001-FR-019` (modeled arena footprint) | `PHASE-1-PHYSICS-CORE` | new `arena` module (`standard_ground`/`standard_walls`, built from FR-013's `StaticPlane`/`with_wall`); `PhysicsWorld::standard_arena` convenience constructor | `crates/rb_physics_bullet` (`arena`, `world`) | `rb_physics_bullet` unit tests (7 in `arena`: exactly 9 planes, the center is on the playable side of every wall, opposing side/back walls share one offset magnitude, a point past a side wall is outside, the ceiling bounds from above, a corner wall cuts off the true rectangular corner, all four corner walls share one offset magnitude; 3 in `world`: `standard_arena` carries exactly 9 walls and the standard ground, a ball bounces off the standard arena's side wall, a ball fired at the true corner is stopped by the diagonal corner wall well before reaching either cardinal wall's own position) | modeled-arena-footprint commit | Implemented, Verified (unit tests only; `SIDE_WALL_X`/`BACK_WALL_Y`/`CEILING_Z` are commonly-cited field dimensions, `CORNER_LENGTH` is this project's own uncalibrated placeholder with no real-mesh reference; curved wall-to-floor/wall-to-ceiling transitions, goal cutouts, and corner-touch disambiguation for wall-jump purposes remain not implemented — see spec Non-goals/Open questions; not yet scored against real data) |
| `RB-PHYSICS-001-FR-020` (curved wall-to-floor/wall-to-ceiling transitions) | `PHASE-1-PHYSICS-CORE` | new `body::StaticQuarterPipe` shape + `between_planes` constructor; `collision::contacts_vs_quarter_pipe` (sphere-only); `solver::resolve_contacts`'s `&StaticPlane` param generalized to plain `restitution`/`friction`; `PhysicsWorld.curves`/`with_curve`/`resolve_curve_contact`; `arena::standard_curves` | `crates/rb_physics_bullet` (`body`, `collision`, `solver`, `world`, `arena`) | `rb_physics_bullet` unit tests (4 in `body`: derived fillet geometry sits radius-in from both planes with correct perpendicular unit sector vectors and on-plane tangent points; 5 in `collision`: a sphere deep inside a fillet has no contact, touching it has zero penetration, pushed past it has positive penetration pushing back toward the axis, outside the 90-degree sector has no contact, and a box always returns no contact; 3 in `arena`: `standard_curves` returns exactly 8 fillets, each sitting radius-in from the floor/ceiling and from a cardinal wall; 3 in `world`: `standard_arena` carries exactly 8 curves, a ball resting within a curve's footprint at flat-floor height gets pushed up off it, and a car in the same position stays unaffected) | curved-wall-transitions commit | Implemented, Verified (unit tests only; only the ball is deflected — a box/car always gets no contact from a curve; the 4 diagonal corner walls' own fillets are now FR-021 — `between_planes`' real correctness requirement (the two bridged planes being mutually perpendicular) never actually excluded them, only this spec's own earlier doc claim did; `FILLET_RADIUS` is this project's own uncalibrated placeholder with no real-mesh reference, same status as `CORNER_LENGTH` — see spec Non-goals/Open questions; not yet scored against real data) |
| `RB-PHYSICS-001-FR-021` (curved corner-wall-to-floor/wall-to-ceiling transitions) | `PHASE-1-PHYSICS-CORE` | `arena::standard_curves` extended to all 9 walls (16 fillets total), reusing `body::StaticQuarterPipe::between_planes` unchanged; new `arena::corner_wall_plane` helper factoring out `standard_walls`' existing corner-wall construction; a corner wall's `axis_direction` computed via `floor.normal.cross(&wall.normal)` (no `.normalize()`/`.unwrap()` needed — already unit length by construction) | `crates/rb_physics_bullet` (`arena`, `world`) | `rb_physics_bullet` unit tests (3 in `arena`: `standard_curves` returns exactly 16 fillets, every fillet's axis sits radius-in from some vertical wall (cardinal or corner), a corner wall's own derived fillet axis sits radius-in from both the corner wall and the floor with correctly perpendicular unit sector vectors, and each of the 4 quadrants' cross-product `axis_direction` is exactly unit length; 1 in `world`: end-to-end a ball resting within a *diagonal*-normal wall's fillet footprint at flat-floor height gets pushed up off it, proving `between_planes` genuinely generalizes beyond axis-aligned walls) | curved-corner-wall-transitions commit | Implemented, Verified (unit tests only; only the ball is deflected, same as FR-020; a fillet at a corner wall's own vertical edges is now FR-022; `FILLET_RADIUS` is reused as-is rather than a separate corner-specific value; goal cutouts remain unimplemented — see spec Non-goals/Open questions; not yet scored against real data) |
| `RB-PHYSICS-001-FR-022` (curved corner-wall vertical-edge fillets) | `PHASE-1-PHYSICS-CORE` | `body::StaticQuarterPipe::between_planes` generalized to any two non-parallel planes (a 2x2 linear solve for the axis point, self-correcting `axis_direction` handedness); `collision::sphere_vs_quarter_pipe`'s sector-membership test generalized from a two-dot-products shortcut to a signed-cross-product test valid for any sector up to 180 degrees; `arena::standard_curves` extended with 8 vertical-edge fillets (2 per corner wall, `axis_direction = (0,0,1)`) | `crates/rb_physics_bullet` (`body`, `collision`, `arena`, `world`) | `rb_physics_bullet` unit tests (5 in `body`, using a synthetic non-perpendicular fixture independent of the arena's own geometry: axis sits radius-in from both planes with tangent points exactly on each, the derived sector angle matches the angle between the two planes' normals, the sharp corner the fillet replaces sits outside its own radius but within its sector, and passing either of the two opposite `axis_direction` choices produces the same correctly-oriented sector; 3 in `arena`: `standard_curves` returns exactly 24 fillets, every vertical-edge fillet's `axis_direction` runs purely along Z, and a corner wall's own vertical-edge fillet sits radius-in from both the corner wall and its neighboring side wall with a 45-degree sector; 1 in `world`: end-to-end a ball embedded past a vertical-edge fillet's own radius, at a wall-to-wall angle that isn't a right angle, gets pushed meaningfully back toward the axis) | corner-wall-vertical-edge-fillets commit | Implemented, Verified (unit tests only; only the ball is deflected, same as FR-020/FR-021; the compound corner where a vertical-edge fillet meets a floor- or ceiling-seam fillet is now FR-023; `FILLET_RADIUS` is reused as-is rather than a separate, smaller radius for these visibly shallower edges; goal cutouts remain unimplemented — see spec Non-goals/Open questions; not yet scored against real data) |
| `RB-PHYSICS-001-FR-023` (compound-corner fillets) | `PHASE-1-PHYSICS-CORE` | New static shape `body::StaticCornerFillet` (an immovable sphere blending three flat planes at a vertex) with constructor `between_three_planes`, solving the fillet's center as the common intersection of the three planes' pairwise `StaticQuarterPipe::between_planes` axis lines via the cross-product form of Cramer's rule; new `collision::sphere_vs_corner_fillet`/`contacts_vs_corner_fillet`, generalizing a `StaticQuarterPipe`'s 2-sided sector test to a 3-bound "spherical triangle" containment test (each bound a sign-corrected, non-normalized cross product of a pair of the three normals); new `arena::standard_corner_fillets` building 16 fillets (4 per corner wall, times 4 corner walls); `PhysicsWorld` gains `corner_fillets: Vec<StaticCornerFillet>` and `with_corner_fillet`, resolved for the ball and every car exactly like `curves` | `crates/rb_physics_bullet` (`body`, `collision`, `arena`, `world`, `lib`) | `rb_physics_bullet` unit tests (4 in `body`, using a synthetic fixture combining a perpendicular floor with the same 45-degree non-perpendicular wall pair `between_planes`'s own fixture uses: the center sits radius-in from all three planes with tangent points exactly on each, and the derived `bounds` correctly include the direction toward the sharp corner and exclude the direction pointing away from it; 5 in `collision`: a sphere deep inside the fillet has no contact, a sphere touching the fillet's own radius has zero penetration, a sphere pushed past that radius has positive penetration pushing back toward the center, a sphere outside the fillet's bounds has no contact, and a box always gets no contact; 2 in `arena`: `standard_corner_fillets` returns exactly 16 fillets, and every fillet's center sits radius-in from a floor/ceiling plane, a side/back wall, and a corner wall simultaneously; 2 in `world`: `standard_arena` carries exactly 16 corner fillets, and an end-to-end test proving a ball embedded past a compound-corner fillet's own radius gets pushed meaningfully back toward the center) | compound-corner-fillets commit | Implemented, Verified (unit tests only; only the ball is deflected, same as FR-020/FR-021/FR-022; `FILLET_RADIUS` is reused as-is rather than a separate corner-specific value; the fillet, its neighboring edge fillet, and the seam fillet it meets remain independent, additive contact sources, not a single continuously-blended surface; goal cutouts are now FR-024; not yet scored against real data) |
| `RB-PHYSICS-001-FR-024` (goal cutouts) | `PHASE-1-PHYSICS-CORE` | New static shape `body::StaticGoalWall` (a `StaticPlane` plus a rectangular window in the plane's own local `u_axis`/`v_axis` frame) with `contains_in_window` testing a point's projection onto that frame; new `collision::sphere_vs_goal_wall`/`contacts_vs_goal_wall`, where a sphere gets no contact inside the window (passes through) but a box's path falls straight through to plain `contacts_vs_plane` against the wrapped plane, deliberately ignoring the window; `arena::standard_walls` drops its 2 back-wall `StaticPlane`s (now 7 planes); new `arena::standard_goal_walls` returns them as 2 windowed `StaticGoalWall`s (new constants `GOAL_HALF_WIDTH`/`GOAL_HEIGHT`); new `arena::standard_goal_cutout_fillets` builds 6 more `StaticQuarterPipe`s (two posts and a crossbar per goal, added to the same `curves` list) via `between_planes` against purely-geometric post/crossbar planes never themselves added as real walls; `PhysicsWorld` gains `goal_walls: Vec<StaticGoalWall>` and `with_goal_wall`, resolved for the ball *and* every car (unlike `curves`/`corner_fillets`) since the box path is a no-op change from the prior plain-plane behavior | `crates/rb_physics_bullet` (`body`, `collision`, `arena`, `world`, `lib`) | `rb_physics_bullet` unit tests (4 in `body`, using a synthetic fixture: `contains_in_window` is true at the window's own center and just inside each of its four edges, false just outside them, and unaffected by a point's distance from the plane; 4 in `collision`: a sphere embedded in the window has no contact, a sphere outside the window behaves exactly like an ordinary plane contact both embedded and resting exactly at the surface, and a box's contact through the windowed wall is bit-for-bit identical to plain `contacts_vs_plane` against the same wrapped plane; 5 in `arena`: `standard_walls` returns exactly 7 planes, `standard_goal_walls` returns exactly 2 sharing one offset magnitude with each window centered correctly, `standard_goal_cutout_fillets` returns exactly 6 fillets each sitting radius-in from a real back wall and a post/crossbar plane; 4 in `world`: `standard_arena` carries exactly 2 goal walls, a ball fired through a goal-mouth window's center passes the back wall's own position while a car aimed at the same spot is still stopped by it, and an end-to-end test proving a ball embedded past a goal-post fillet's own radius gets pushed meaningfully back toward the axis) | goal-cutouts commit | Implemented, Verified (unit tests only; only the ball can pass through the window or be deflected by a fillet — a car sees the exact same solid wall it always has; the goal-mouth window opens onto open space, not a modeled interior/net; the two compound corners per goal where a post's fillet meets the crossbar's were left unblended at the time this requirement shipped — now rounded by a dedicated `StaticCornerFillet`, see FR-026; not yet scored against real data) |
| `RB-PHYSICS-001-FR-025` (corner-wall floor/ceiling arch radius) | `PHASE-1-PHYSICS-CORE` | New constant `arena::CORNER_ARCH_RADIUS` (750.0, distinctly larger than `arena::FILLET_RADIUS`'s 292.0), enforced via a compile-time `const _: () = assert!(CORNER_ARCH_RADIUS > FILLET_RADIUS);`; `arena::standard_curves` switches the 8 of its 24 entries bridging a diagonal corner wall to the floor or ceiling to `CORNER_ARCH_RADIUS`; `arena::standard_corner_fillets` switches all 16 compound-corner fillets to `CORNER_ARCH_RADIUS` too, since `body::StaticCornerFillet::between_three_planes` needs one shared radius across the three planes it blends to still meet those arches exactly where their axes cross; the 8 vertical corner-edge fillets (`FR-022`) and 6 goal-cutout edge fillets (`FR-024`) are unaffected and keep `FILLET_RADIUS` | `crates/rb_physics_bullet` (`arena`, `world`) | `rb_physics_bullet` unit tests: `arena.rs`'s `every_floor_or_ceiling_seam_curve_bridges_a_wall_to_the_floor_or_ceiling` now checks the first 8 of `standard_curves()`'s 24 entries against `FILLET_RADIUS` and the next 8 against `CORNER_ARCH_RADIUS` separately (previously all 16 checked against one radius), `every_standard_curve_sits_radius_in_from_a_vertical_wall` now accepts either radius, and `every_standard_corner_fillets_center_sits_radius_in_from_a_floor_or_ceiling_a_side_or_back_wall_and_a_corner_wall` now checks `CORNER_ARCH_RADIUS`; 1 new end-to-end `world.rs` test, `a_ball_embedded_in_a_corner_walls_floor_arch_footprint_is_pushed_toward_the_axis`, proving a ball embedded past `CORNER_ARCH_RADIUS` (deep enough to sit outside a plain `FILLET_RADIUS` fillet's own footprint entirely) still gets pushed meaningfully back toward the axis, and asserting `CORNER_ARCH_RADIUS > FILLET_RADIUS` directly (net +1 test over 0.24.0's 211, since one new `arena.rs` test idea became the compile-time const-assert instead of a runtime test) | corner-wall-floor-ceiling-arch-radius commit | Implemented, Verified (unit tests only; only the ball is deflected, same as FR-020/FR-021/FR-022/FR-023; `CORNER_ARCH_RADIUS` is this project's own uncalibrated placeholder with no real-mesh reference, same status as `FILLET_RADIUS`; while validating this change, the pre-existing FR-024 end-to-end test `a_ball_shot_through_the_goal_mouth_passes_the_standard_arenas_back_wall` was found to fail because the bigger radius sharpens a pre-existing, already-documented `StaticQuarterPipe` "infinite along its own axis" limitation into a solver-destabilizing correction closer to the goal — fixed by shortening that test's simulated flight duration from 3.0s to 1.8s, a test-scoping fix rather than a new capability or Non-goal; not yet scored against real data) |
| `RB-PHYSICS-001-FR-026` (goal post-crossbar corner fillets) | `PHASE-1-PHYSICS-CORE` | New `arena::standard_goal_corner_fillets` returning 4 `body::StaticCornerFillet`s (one per goal post per goal), each built via `StaticCornerFillet::between_three_planes` directly on the back wall/post/crossbar planes (no new shape or collision code — `StaticCornerFillet`/`collision::sphere_vs_corner_fillet` are already generic to any three non-parallel planes), reusing `arena::FILLET_RADIUS` unchanged since both edge fillets meeting at that vertex already share it; `PhysicsWorld::standard_arena` wires them into the same `corner_fillets: Vec<StaticCornerFillet>` field via the existing `with_corner_fillet` builder, bringing it to 20 entries total (16 arena-corner plus 4 goal-corner) | `crates/rb_physics_bullet` (`arena`, `world`) | `rb_physics_bullet` unit tests: 2 new in `arena.rs` — `standard_goal_corner_fillets_has_four_fillets` and `every_goal_corner_fillets_center_sits_radius_in_from_a_back_wall_a_post_and_the_crossbar` (every fillet's center sits radius-in from some back wall, some post plane, and the crossbar plane simultaneously — the same "prove the real triple intersection, not an arbitrary point" style test FR-023's own arena-corner test used); the pre-existing `world.rs` test `standard_arena_has_sixteen_compound_corner_fillets` was renamed `standard_arena_has_twenty_compound_corner_fillets` and its assertion updated to 20; 1 new end-to-end `world.rs` test, `a_ball_embedded_in_a_goal_corner_fillets_footprint_is_pushed_toward_the_center`, proving a ball embedded past a goal corner fillet's own radius (a synthetic back-wall/post/crossbar fixture, not the real arena's own numbers) gets pushed meaningfully back toward the center, the same "moved meaningfully" claim every other fillet's own equivalent test makes (net +3 tests over 0.25.0's 212, bringing the crate to 215) | goal-corner-fillets commit | Implemented, Verified (unit tests only; only the ball is deflected, same as FR-020/FR-021/FR-022/FR-023; `FILLET_RADIUS` is reused as-is, not `CORNER_ARCH_RADIUS`, since — unlike the arena's own compound corners — both edge fillets meeting at a goal's post-crossbar vertex already share `FILLET_RADIUS`; the goal's other two corners, where a post meets the floor, are explicitly not treated the same way, since the window's own bottom edge sits exactly at floor level and a post's own fillet there already ends flush with the ground; not yet scored against real data) |
| `RB-PHYSICS-001-FR-027` (car deflection by curved fillets) | `PHASE-1-PHYSICS-CORE` | New `collision::box_vs_quarter_pipe(position, orientation, half_extents, pipe)` and `collision::box_vs_corner_fillet` reduce a box-vs-curve test to the same "test every corner" technique `collision::box_vs_plane` already uses for a flat plane: each of the box's 8 corners is checked as a zero-radius sphere via the existing `sphere_vs_quarter_pipe`/`sphere_vs_corner_fillet` call, and every corner that reports a contact contributes one to the manifold, with each surviving contact's `point` overwritten to the corner's own world position (not the fillet-surface point those functions compute) for the same rel_pos/torque-accuracy reason `box_vs_plane`'s own doc comment documents; `contacts_vs_quarter_pipe`/`contacts_vs_corner_fillet` now dispatch a `Shape::Box` to these new functions instead of returning `Vec::new()`. Documented as an approximation, not a full convex-vs-curved-surface narrow phase (no GJK/EPA support-mapping machinery added) — a face resting flush against a shallow curve could under-detect. No `world.rs` step-loop changes needed: `PhysicsWorld::step` already called `resolve_curve_contact`/`resolve_corner_fillet_contact` for every car (wired in since FR-023), previously a silent no-op. `StaticGoalWall`/`contacts_vs_goal_wall` explicitly unaffected, since a goal wall isn't a curved fillet | `crates/rb_physics_bullet` (`collision`, `world`, `arena`) | `rb_physics_bullet` unit tests: 4 in `collision.rs` replacing the 2 pre-existing "always empty for a box" regression tests (`box_vs_quarter_pipe_is_always_empty`, `box_vs_corner_fillet_is_always_empty`) — `box_embedded_in_the_quarter_pipes_footprint_has_contact` (a box centered on a quarter-pipe's own surface reports at least one contact, every normal pointing back toward the axis), `box_far_from_the_quarter_pipe_has_no_contact` (a box deep on the opposite side of the fillet's own angular sector, not just far away radially, reports none), and the equivalent `box_embedded_in_the_corner_fillets_footprint_has_contact`/`box_outside_the_corner_fillets_bounds_has_no_contact` pair for `StaticCornerFillet` (net +2); 2 in `world.rs` replacing the pre-existing `a_car_is_not_deflected_by_a_curved_transition` regression test whose premise this requirement reverses — `a_car_resting_within_a_curved_transitions_footprint_is_pushed_up_off_the_flat_floor_height` (mirrors the ball's own equivalent live-physics proof) and `a_car_embedded_in_a_compound_corner_fillets_footprint_has_its_penetration_reduced` (checks the fillet's own worst/maximum corner penetration shrinks after the solver runs, deliberately not that the box's center of mass moves closer to the fillet's center the way the equivalent ball test does — an earlier center-of-mass version failed empirically, since a box's multiple simultaneously-penetrating corners can rotate it away from the fillet's center even as every corner's own overlap shrinks) (net +1) (net +3 tests over 0.26.0's 215, bringing the crate to 218) | car-deflection-by-curved-fillets commit | Implemented, Verified (unit tests only; an approximation — 8-corner testing, not a full convex-vs-curved-surface narrow phase; `StaticGoalWall` itself isn't a curved fillet so was unaffected by this requirement — a car actually driving into a goal is now implemented, see FR-028; not yet scored against real data) |
| `RB-PHYSICS-001-FR-028` (car actually driving into a goal) | `PHASE-1-PHYSICS-CORE` | New `collision::box_vs_goal_wall(position, orientation, half_extents, wall)` tests each of the box's 8 corners against `StaticGoalWall::contains_in_window` before falling through to an ordinary `box_vs_plane`-style corner test — the same "test every corner" technique `collision::box_vs_quarter_pipe`/`box_vs_corner_fillet` (FR-027) established for curved fillets, applied here to a flat windowed plane instead; `contacts_vs_goal_wall` now dispatches a `Shape::Box` to it instead of plain `contacts_vs_plane` against the wrapped plane. No `world.rs` step-loop changes needed: `PhysicsWorld::step`'s `resolve_goal_wall_contact` already ran for every car (it always needed the wall's plain-plane collision), so this is a pure dispatch-function change | `crates/rb_physics_bullet` (`collision`, `world`, `body`, `arena`, `lib`) | `rb_physics_bullet` unit tests: 3 in `collision.rs` replacing the old `box_vs_goal_wall_ignores_the_window_entirely` regression test whose premise this requirement reverses — `box_squarely_inside_the_goal_window_has_no_contact` (every corner inside the window gives an empty manifold), `box_straddling_the_goal_window_edge_only_collides_on_the_corners_still_outside_it` (a car centered on the window's own edge gets exactly 2 contacts, only on the corners outside it — the real proof of the partial-block behavior this requirement's per-corner test produces), and `box_entirely_outside_the_goal_window_behaves_like_an_ordinary_plane` (bit-for-bit identical to plain `contacts_vs_plane`) (net +2); 2 in `world.rs` replacing the old `a_car_is_still_stopped_by_the_standard_arenas_back_wall_at_the_goal_mouth` test whose premise this requirement reverses — `a_car_shot_through_the_goal_mouth_passes_the_standard_arenas_back_wall` (a live end-to-end proof mirroring the pre-existing ball test's own, reusing its already-solved 1.8s flight-duration bound) and a new regression guard `a_car_aimed_away_from_the_goal_mouth_is_still_stopped_by_the_back_wall` (a car aimed at the wall's still-solid portion is unaffected) (net +1) (net +3 tests over 0.27.0's 218, bringing the crate to 221) | car-driving-into-a-goal commit | Implemented, Verified (unit tests only; an approximation — 8-corner testing per corner, same per-test-point caveat FR-027's fillet corner-testing has; a modeled goal interior/net beyond the cutout itself remains unimplemented at the time this requirement shipped — a bounded interior volume is now implemented, see `RB-PHYSICS-001-FR-029`, though a genuine net mesh still isn't; not yet scored against real data) |
| `RB-PHYSICS-001-FR-029` (modeled goal interior) | `PHASE-1-PHYSICS-CORE` | New static shape `body::StaticBoundedWall` (a `StaticPlane` plus a rectangular *bound* in the plane's own local `u_axis`/`v_axis` frame, `contains_in_bound` testing a point's projection onto that frame — the opposite gate convention from `StaticGoalWall`'s window, colliding only *inside* the bound instead of everywhere *except* inside a window); new `collision::sphere_vs_bounded_wall`/`box_vs_bounded_wall`/`contacts_vs_bounded_wall`, where `box_vs_bounded_wall` reuses the "test every corner" technique FR-027/FR-028 established but skips a corner *outside* the bound (the opposite of `box_vs_goal_wall`'s per-corner window test); new constant `arena::GOAL_DEPTH` (880.0, an uncalibrated placeholder with no public reference at all, unlike `GOAL_HALF_WIDTH`/`GOAL_HEIGHT`); new `arena::goal_back_wall_plane`/`standard_goal_back_walls` (2 plain, unbounded `StaticPlane`s per goal, `GOAL_DEPTH` behind the real back wall, added to `PhysicsWorld.walls` via the existing `with_wall` builder — exact, not an approximation, since nothing can reach this plane except by first passing through the goal-mouth window), `arena::goal_side_wall`/`standard_goal_side_walls` (4 `StaticBoundedWall`s total, each reusing `goal_post_plane` unchanged, bounded to the goal's own depth and height), and `arena::goal_roof`/`standard_goal_roofs` (2 `StaticBoundedWall`s total, each reusing `goal_crossbar_plane` unchanged, bounded to the goal's own width and depth); `PhysicsWorld` gains `bounded_walls: Vec<StaticBoundedWall>` and `with_bounded_wall` (mirroring `with_goal_wall`), resolved for the ball and every car via a new `resolve_bounded_wall_contact` exactly like `goal_walls` | `crates/rb_physics_bullet` (`body`, `collision`, `arena`, `world`) | `rb_physics_bullet` unit tests: 4 in `body.rs` for `StaticBoundedWall::contains_in_bound` (`contains_in_bound_is_true_for_the_bounds_own_center`, `contains_in_bound_is_true_just_inside_each_edge`, `contains_in_bound_is_false_just_outside_each_edge`, `contains_in_bound_ignores_distance_from_the_plane_itself`), mirroring the pre-existing `StaticGoalWall::contains_in_window` tests with the boolean gate inverted; 5 in `collision.rs` against a synthetic fixture (`sphere_inside_the_bound_behaves_like_an_ordinary_plane`, `sphere_outside_the_bound_has_no_contact`, `box_squarely_inside_the_bound_behaves_like_an_ordinary_plane`, `box_straddling_the_bounds_edge_only_collides_on_the_corners_still_inside_it`, `box_entirely_outside_the_bound_has_no_contact`), mirroring the `StaticGoalWall` collision tests with the gate inverted; 8 in `arena.rs` proving the geometry functions place things correctly (`standard_goal_back_walls_has_two_walls`, `every_goal_back_wall_sits_goal_depth_behind_the_real_back_wall`, `standard_goal_side_walls_has_four_walls`, `every_goal_side_walls_plane_matches_some_goal_post_plane`, `every_goal_side_walls_bound_covers_the_real_goal_depth_and_height`, `standard_goal_roofs_has_two_roofs`, `every_goal_roofs_plane_is_the_goal_crossbar_plane`, `every_goal_roofs_bound_covers_the_real_goal_width`); and 4 in `world.rs` — 1 wiring-count test (`standard_arena_has_six_bounded_walls`) and 3 live end-to-end `PhysicsWorld` proofs (`a_ball_shot_into_the_goal_is_stopped_by_the_goal_back_wall`, `a_ball_shot_sideways_inside_the_goal_is_stopped_by_a_goal_side_wall`, `a_ball_shot_upward_inside_the_goal_is_stopped_by_the_goal_roof`), each deliberately isolated to a minimal `PhysicsWorld` (`PhysicsWorld::new` plus `with_wall`/`with_bounded_wall`, not `PhysicsWorld::standard_arena`) after an earlier full-`standard_arena` version got flung to wildly wrong positions, root-caused to the standard arena's own goal-cutout fillets spuriously triggering `StaticQuarterPipe`'s pre-existing "sector membership is angle-only, not radially bounded" limitation, and after an earlier version zeroing only the ball's own restitution got nondeterministic roof-test results, root-caused to the wall's own default `StaticPlane::new` restitution (0.5) still applying regardless (net +21 tests over 0.28.0's 221, bringing the crate to 242; the pre-existing `world.rs` test `standard_arena_has_seven_walls_and_the_standard_ground` was also renamed `standard_arena_has_nine_walls_and_the_standard_ground` to match `PhysicsWorld.walls` growing from 7 to 9 real entries, a test-count correction not counted in the net +21) | modeled-goal-interior commit | Implemented, Verified (unit tests only; the interior is a solid bounding box, not a springy/catching net — no cloth/soft-body simulation was added, a deliberate scoping decision; a genuine net mesh for the ball is now implemented, see `RB-PHYSICS-001-FR-033` (a car's own contact against a net remains a separate, documented Non-goal); `arena::GOAL_DEPTH` has no public reference at all, a further step removed even from `GOAL_HALF_WIDTH`/`GOAL_HEIGHT`; not yet scored against real data) |
| `RB-PHYSICS-001-FR-030` (combined multi-body solve) | `PHASE-1-PHYSICS-CORE` | New `solver::resolve_dynamic_manifolds(bodies, manifolds, dt)` shares one interleaved `SOLVER_ITERATIONS`-iteration budget across every dynamic-vs-dynamic manifold touching in a step (every iteration processes every manifold once, applying each body's accumulated delta to its velocity only once, after all iterations finish), replacing the old approach of resolving each ball-vs-car/car-vs-car manifold with its own fully independent `solver::resolve_contacts_between`-style solve; new `solver::delta_pair_mut(deltas, a, b)` is a `split_at_mut`-based disjoint-borrow helper letting manifolds that share a body index share that body's single `DeltaVelocity` accumulator, generalizing the `b == a + 1` special case `PhysicsWorld::step`'s old car-vs-car loop used; the old `TwoBodyDelta` struct is removed in favor of two separate `DeltaVelocity` parameters on `resolve_two_body_row`; `resolve_contacts`/`resolve_contacts_between`'s own public behavior is unchanged (the latter's internals adjusted to match, a pure refactor); `PhysicsWorld::step` now builds one `Vec<RigidBody>` (ball plus every car), collects every non-empty ball-vs-car/car-vs-car manifold into one list, and resolves the whole scene's dynamic contacts with a single `resolve_dynamic_manifolds` call per step, replacing the two old independent loops and their now-deleted private `resolve_dynamic_contact` helper; static contacts (ground, arena walls, curves, corner fillets, goal walls, bounded walls) are deliberately untouched, since each depends on only one body | `crates/rb_physics_bullet` (`solver`, `world`) | `rb_physics_bullet` unit tests: 1 new in `solver.rs`, `resolve_dynamic_manifolds_keeps_more_of_every_bodys_contact_than_resolving_pairs_independently` (a symmetric ball-between-two-closing-cars "pinch," mass 1 vs. two mass-180 cars, restitution zero, true answer near-zero velocity for all three bodies: resolving each pair independently, as the old approach did, leaves the ball at ~98-99% of a closing car's own speed (~98.9 units/s); the combined solve leaves it noticeably slower (~89.5 units/s at this crate's `SOLVER_ITERATIONS = 10`) — confirmed, during test design only, to converge much closer to zero at 300 iterations, while the old approach's result doesn't change at all regardless of iteration count, proving its error is structural, not an iteration-count shortfall); 1 new in `world.rs`, `a_ball_pinched_between_two_closing_cars_is_resolved_by_a_shared_multi_body_solve` (the same pinch scenario proven live through a real `PhysicsWorld::step`); both tests deliberately assert the direction/magnitude of the improvement, not exact convergence to zero, matching this spec's practice of not overclaiming what a test proves (net +2 tests over 0.29.0's 242, bringing the crate to 244) | combined-multi-body-solve commit | Implemented, Verified (unit tests only; scoped to dynamic-vs-dynamic contacts only — static contacts, split impulse, warm-starting/sleeping, and the average-not-max restitution/friction combine mode are unaffected and remain exactly as documented before; does not fully converge to the true simultaneous-solve answer for an extreme mass-ratio "sandwiched" body at the crate's current `SOLVER_ITERATIONS = 10`, only measurably closer than the old independent-pairwise approach; not yet scored against real data) |
| `RB-PHYSICS-001-FR-031` (constant-calibration audit) | `PHASE-1-PHYSICS-CORE` | Every uncalibrated placeholder constant in `drive.rs`/`arena.rs`/`world.rs` sourced against three independently-written community reverse-engineering references — the RocketSim project (`ZealanL/RocketSim`, `src/RLConst.h`/`Car.cpp`), RLUtilities (`samuelpmish/RLUtilities`, `src/simulation/car.cc`/`ball.cc`, `src/mechanics/jump.cc`/`dodge.cc`), and the RLBot community wiki's "Useful Game Values" page — not blog paraphrases. Corrected with code changes: `drive::JUMP_SPEED` (`292.0` → `875.0/3.0`, ≈291.667) and `drive::JUMP_HOLD_ACCELERATION` (`1400.0` → `4375.0/3.0`, ≈1458.33) to their precise real values; new `drive::UNBOOSTED_MAX_CAR_SPEED = 1410.0` split out from `drive::MAX_CAR_SPEED` (2300, confirmed correct as boost's own cap) — a genuine behavioral fix, since throttle alone previously could reach the boosted top speed. Confirmed correct, no change: `drive::JUMP_HOLD_MAX_DURATION` (0.2), `drive::BOOST_ACCELERATION` (991.667), `drive::MAX_BOOST` (100), gravity (-650, `world.rs`), `arena::GOAL_DEPTH` (880) — `arena.rs`/`world.rs` were reviewed/confirmed but not modified for these. Explicitly flagged as audited-but-still-uncalibrated (a real reference exists but doesn't safely port into this port's own unit system/mechanic shape, or no reference exists at all): `drive::DODGE_SPEED`, `drive::DODGE_ANGULAR_SPEED`, `drive::WALL_JUMP_HORIZONTAL_SPEED`, `drive::STEER_TORQUE`, `drive::AIR_CONTROL_TORQUE`, `drive::HANDBRAKE_FRICTION_MULTIPLIER`, `drive::LANDING_AUTO_UPRIGHT_TORQUE`, `arena::FILLET_RADIUS`, `arena::CORNER_ARCH_RADIUS`. Two open ambiguities surfaced but deliberately not acted on (recorded in the spec's Open questions, not guessed at): ball radius (this port's `92.75` vs. `91.25`, which RocketSim/RLUtilities/the current RLBot wiki all converge on — not changed, load-bearing across a large fraction of existing tests) and `arena::CEILING_Z` (`2044.0` vs. RocketSim's cited `ARENA_HEIGHT = 2048.f` — unclear if the same reference point). Explicitly does NOT close `FR-005` (real-data calibration against recorded ground truth), which stays blocked on `PHASE-0-EXIT` | `crates/rb_physics_bullet/src/drive.rs` (`arena.rs`/`world.rs` reviewed and confirmed correct for the constants living there, not modified) | `rb_physics_bullet` unit tests: `drive::tests::throttle_stops_accelerating_at_max_speed` renamed to `throttle_stops_accelerating_at_unboosted_max_speed` and repointed at the new `UNBOOSTED_MAX_CAR_SPEED` instead of `MAX_CAR_SPEED`; 1 new test, `drive::tests::throttle_alone_cannot_reach_the_boosted_top_speed` (holds throttle for 20 simulated seconds, confirms the car plateaus near `UNBOOSTED_MAX_CAR_SPEED`, well short of the boosted `MAX_CAR_SPEED` — the real bug this audit fixed, made concrete as a regression test). The `JUMP_SPEED`/`JUMP_HOLD_ACCELERATION` precision refinements needed no new tests, since every existing assertion involving either already used a tolerance wide enough to absorb the small precision differences (net +1 test over FR-030's 244, bringing the crate to 245) | constant-calibration-audit commit | Implemented, Verified (unit tests only; a sourcing/documentation audit against community reverse-engineering references, not against this project's own recorded ground truth — does not close FR-005; not yet scored against real data) |
| `RB-PHYSICS-001-FR-032` (GJK/EPA narrow-phase investigation, reverted) | `PHASE-1-PHYSICS-CORE` | Set out to replace `collision::box_vs_quarter_pipe`/`box_vs_corner_fillet`'s per-corner (8-corner) testing technique with a genuine GJK/EPA convex-vs-curved-surface narrow phase, motivated by a claim in `FR-027`'s own doc comments that a box face resting flush against a shallow curve could have every corner still clear of the fillet while the face's middle already overlapped it. A full GJK closest-points implementation was built and wired in; doing so broke two pre-existing, previously-passing tests. Investigation found the GJK replacement answered the wrong question: a quarter-pipe/corner-fillet's contact test is a *containment* question (is the box's farthest point from the axis/center at or beyond radius), not a nearest-point question, and distance-from-a-line/point is a convex function of position whose maximum over a convex polytope (a box) is always attained at one of its extreme points (its 8 corners) — never a face's interior; the original per-corner technique is mathematically exact for this question, not an approximation, so the claimed under-detection bug does not exist. The GJK code was reverted in full, including the `gjk.rs` module (no remaining consumer); `box_vs_quarter_pipe`/`box_vs_corner_fillet` are byte-for-byte unchanged from `FR-027`. What shipped: corrected doc comments across the crate and its own spec file reflecting the verified-exact understanding in place of the old unverified "approximation"/"under-detection" claim, plus one new regression test. Does not close `FR-005` (real-data calibration), unrelated and no longer blocked on `PHASE-0-EXIT` (now closed), but not itself started | `crates/rb_physics_bullet/src/collision.rs` (doc comments only; `gjk.rs` added then fully removed, no net change to shipped collision code) | `rb_physics_bullet` unit tests: 1 new test, `collision::tests::no_point_on_a_boxs_face_is_ever_farther_from_a_quarter_pipes_axis_than_its_own_corners` (densely samples a 50×50 grid per face across all 6 faces of a car-sized box positioned exactly like the two previously-broken end-to-end tests, confirming empirically that no face-interior point ever exceeds the box's own 8 corners' maximum distance from the axis) (net +1 test over FR-031's 245, bringing the crate to 246) | narrow-phase-investigation commit | Implemented, Verified (unit tests only; a resolved investigation and corrected doc comments, not a new narrow-phase algorithm — GJK/EPA is explicitly NOT used in the shipped collision code; does not close FR-005; not yet scored against real data) |
| `RB-PHYSICS-001-FR-033` (genuine net mesh) | `PHASE-1-PHYSICS-CORE` | New module `net.rs` (`net::NetMesh`): a rectangular grid of point masses, each a real `RigidBody::sphere` (deliberately tiny and light), reusing this crate's existing rigid-body/collision/solver machinery instead of a bespoke penalty-force system — every perimeter point is anchored (fixed, representing the net's attachment to the rigid goal frame: crossbar, both posts, ground/back line), every interior point is free and connected to its neighbors by structural (horizontal/vertical) and shear (diagonal) springs (Hooke's law plus velocity damping, the one genuinely new physics-math addition this requirement needed, since Bullet's own soft-body code was never part of this port's scope per ADR-0004); `NetMesh::step` sub-steps its own internal physics (`net::NET_SUBSTEPS`) for numerical stability, then resolves the ball's contact against every free point it overlaps via a new `collision::sphere_vs_sphere` function (this crate's first real sphere-vs-sphere contact test — previously `contacts_between` returned empty for that shape pairing, ball being the only sphere in this port) plus the existing `solver::resolve_contacts_between` two-body sequential-impulse path — no new solver code was needed; new `arena::standard_nets` builds one `net::NetMesh` per goal, positioned at new constant `arena::NET_DEPTH` (`GOAL_DEPTH * 0.5`) behind the real back wall, well in front of `FR-029`'s own rigid back-of-net `StaticPlane` (completely unchanged, still the car's real backstop); `PhysicsWorld` gains `nets: Vec<net::NetMesh>` and a `with_net` builder, wired into `PhysicsWorld::standard_arena`, resolved after every other contact each step by calling `net.step(&mut self.ball, self.gravity, dt)` per net (mutating the ball's velocity directly rather than going through `solver::resolve_dynamic_manifolds`' shared multi-body solve, since a net's own points aren't part of that scene-wide body list); new constants, all uncalibrated placeholders (real Rocket League net material properties have never been published): `net::NET_POINT_MASS`, `net::NET_POINT_RADIUS`, `net::NET_SPRING_CONSTANT`, `net::NET_SPRING_DAMPING`, `net::NET_LINEAR_DAMPING`, `net::NET_RESTITUTION`, `net::NET_FRICTION`, `net::NET_SUBSTEPS`, plus `arena::NET_DEPTH`, `arena::NET_COLS` (7), `arena::NET_ROWS` (5). Explicit Non-goals: a car's own contact against a net (still passes straight through the panel's spatial footprint, stopped by `FR-029`'s pre-existing machinery instead); a full 3D "sock" shape billowing backward from the goal mouth (this models a single flat rest-shape panel that still deforms dynamically under real ball impact via its own springs, just not a pre-shaped pocket); bending stiffness (only structural + shear springs, no anti-folding springs); manifold richness beyond one contact per overlapping point. Does not close `FR-005` (real-data calibration), unrelated and no longer blocked on `PHASE-0-EXIT` (now closed), but not itself started | `crates/rb_physics_bullet` (`net`, `collision`, `arena`, `world`) | `rb_physics_bullet` unit tests: 5 new in `net.rs` (perimeter anchoring correctness, springs start at zero stretch, anchored points never move under gravity, an undisturbed net settles instead of oscillating forever, and the real catching proof — a ball fired at the net's center loses over half its speed within 1 simulated second vs. free flight); `collision.rs` replaced the old `contacts_between_two_spheres_is_empty` regression test (whose entire premise this reverses) with 2 new tests proving `sphere_vs_sphere`'s correctness (net +1 over that one test); 2 new in `arena.rs` (`standard_nets` returns exactly 2, and each net sits exactly `NET_DEPTH` behind the real back wall spanning exactly the goal mouth's own `GOAL_HALF_WIDTH`/`GOAL_HEIGHT` footprint); 2 new in `world.rs` (a wiring-count test — `standard_arena` carries exactly 2 nets — plus a live end-to-end proof: a ball fired at a lone net panel in an isolated minimal `PhysicsWorld` scene loses at least half its speed vs. the identical shot through the same scene with no net added) (net +10 tests over FR-032's 246, bringing the crate to 256) | genuine-net-mesh commit | Implemented, Verified (unit tests only; ball-only — a car's own contact against a net is an explicit, documented Non-goal, not an oversight; does not close FR-005; does not close FR-029's back-wall/side-wall/roof bounding volume, which is unchanged and still the car's real backstop; not yet scored against real data) |
| `RB-PHYSICS-001-FR-034` (split impulse) | `PHASE-1-PHYSICS-CORE` | Every contact's normal row now also solves a second, entirely separate "push" pseudo-velocity channel (new `solver::resolve_push_row`/`resolve_two_body_push_row`), fed only by that contact's own positional (penetration/ERP) error — `ConstraintRow`/`TwoBodyRow` gained a new `rhs_penetration` field, split out of the single combined `rhs` this port used before this requirement — never the velocity/restitution error, which stays on the real `rhs`/`applied_impulse` channel exactly as before (a friction row's `rhs_penetration` is always `0.0`, matching Bullet's own split-impulse resolve). Each solver iteration resolves both channels for every contact's normal row, the push channel's own accumulator (`applied_push_impulse`) entirely separate scratch state from the real channel's `applied_impulse`; after the loop, the real delta is applied to the body's velocity exactly as before, and the new push delta is applied directly to the body's position/orientation via a new `solver::apply_push_delta` (built on the existing `integrate::integrate_transform`) — mirroring Bullet's own `btSolverBody::writebackVelocity`. Wired into all three of this module's resolve entry points — `resolve_contacts`, `resolve_contacts_between`, and `resolve_dynamic_manifolds` (the last via a new per-body-index `push_deltas` accumulator, shared across manifolds the same way its pre-existing real `deltas` already is) — with zero call-site changes anywhere outside `solver.rs`. Non-goal: warm-starting/sleeping remain exactly as documented before this requirement, an independent fix for a different symptom, still open | `crates/rb_physics_bullet/src/solver.rs` | `rb_physics_bullet` unit tests: 2 new in `solver.rs` — `split_impulse_corrects_deep_penetration_via_position_not_velocity` (`resolve_contacts`, one body vs. a static plane) and `split_impulse_corrects_deep_penetration_via_position_not_velocity_between_two_bodies` (`resolve_contacts_between`, two dynamic bodies) — each starts a body deeply overlapping with zero restitution and zero incoming velocity, resolves once, and checks the real velocity along the contact normal stayed near zero while position/separation moved measurably; all 12 of `solver.rs`'s pre-existing tests pass unchanged, confirming the `rhs`/`rhs_penetration` split is behavior-preserving for every case they already covered; 4 pre-existing `world.rs` live end-to-end curved-fillet tests (`a_ball_embedded_in_a_vertical_corner_edges_fillet_footprint_is_pushed_toward_the_axis`, `a_ball_embedded_in_a_compound_corner_fillets_footprint_is_pushed_toward_the_center`, `a_ball_embedded_in_a_goal_posts_fillet_footprint_is_pushed_toward_the_axis`, `a_ball_embedded_in_a_goal_corner_fillets_footprint_is_pushed_toward_the_center`) had their assertions strengthened from checking the ball moved "meaningfully" toward the resting surface to checking it settles at (not past) its exact resting distance, since the push channel no longer leaves residual velocity for the ball to coast on (net +2 tests over FR-033's 256, bringing the crate to 258) | split-impulse commit | Implemented, Verified (unit tests only; does not close FR-005; warm-starting/sleeping remain exactly as documented before this requirement — an independent fix for a different symptom, still open (see spec's own Non-goals and Open questions); the two ambiguities `FR-031`'s audit surfaced (ball radius `92.75` vs. `91.25`; `arena::CEILING_Z` `2044.0` vs. `2048.0`) are untouched by this requirement and remain open; not yet scored against real data) |
| `RB-PHYSICS-001-FR-035` (warm-starting, `resolve_dynamic_manifolds` only) | `PHASE-1-PHYSICS-CORE` | New `solver::ContactCache` carries a manifold's converged real-channel impulses (normal plus both friction rows) from one call to the next, matched by each contact's approximate world position via a new constant `CONTACT_MATCH_DISTANCE = 4.0` (an uncalibrated placeholder mirroring Bullet's own fixed `gContactBreakingThreshold`-style point matching, since this port's narrow phase re-derives every contact from scratch with no genuine per-point stable ID to track instead). New private `warm_start_two_body_row` applies each row's cached impulse directly into the manifold's shared `DeltaVelocity` accumulators before any solver iteration runs — merely setting `TwoBodyRow::applied_impulse` to the cached value would do nothing observable on its own in this port (`GLOBAL_CFM` is always `0.0`, so that field never otherwise enters the per-iteration correction math), so the seed has to be baked into the starting velocity delta itself, mirroring how Bullet's own real warm-start applies the cached impulse to the solver body's temporary velocity at setup time, before any iteration runs. `solver::resolve_dynamic_manifolds` gained a new required `caches: &mut HashMap<(usize, usize), ContactCache>` parameter, key normalized `(min(a,b), max(a,b))` so either argument order finds the same entry; every call rebuilds `caches` from scratch using only that call's own manifolds (replacing, not merging), so a body pair no longer touching is dropped automatically with no separate eviction pass needed. `PhysicsWorld` gains one new persistent field, `dynamic_manifold_caches: HashMap<(usize, usize), ContactCache>`, initialized empty in `PhysicsWorld::new` and passed into the crate's one `resolve_dynamic_manifolds` call site inside `PhysicsWorld::step`. Non-goal (documented, not an oversight): `resolve_contacts`/`resolve_contacts_between` do NOT take a `ContactCache` — this port's fixed `SOLVER_ITERATIONS = 10` already fully converges every one-body and two-body scenario the crate currently tests, so warm-starting those paths would have zero observable effect on any existing behavior; `resolve_dynamic_manifolds` was chosen specifically because it already had a known, documented under-convergence case from `FR-030` (an extreme mass-ratio "sandwiched" body that doesn't fully converge within one call's 10-iteration budget), exactly the situation warm-starting helps with. Warm-starting does NOT fix this crate's separately-documented "a bouncy resting contact never actually settles" limitation — that symptom is caused by restitution re-triggering off a fresh gravity-induced closing velocity every frame, independent of where the solver's iteration starts; warm-starting just converges that same wrong-looking bounce faster, it doesn't stop it from recurring — sleeping (still completely unimplemented) is the actual fix, and is now the sole remaining item under what used to be a single combined "no warm-starting or sleeping" doc-comment bullet in `solver.rs` | `crates/rb_physics_bullet/src/solver.rs`, `crates/rb_physics_bullet/src/world.rs` | `rb_physics_bullet` unit tests: 1 new in `solver.rs`, `warm_starting_a_sandwiched_ball_across_two_calls_converges_closer_than_a_repeated_cold_start` — reuses the existing `symmetric_pinch` test fixture (the same extreme-mass-ratio scenario `FR-030`'s own `resolve_dynamic_manifolds_keeps_more_of_every_bodys_contact_than_resolving_pairs_independently` already uses): call 1 (cold, empty cache) partially converges and populates a cache; from that identical post-call-1 state, call 2 is then run twice on independent copies of the same state — once warm (reusing call 1's cache) and once cold (a fresh empty map) — with identical positions, contacts, velocities, and iteration budget both times, isolating exactly what the warm seed itself contributes; the warm run's ball ends up measurably slower (closer to the true zero-velocity equilibrium) than the cold repeat's, proving the mechanism has genuine, measurable effect; all of `solver.rs`'s pre-existing tests pass unchanged when given an empty cache, confirming this requirement is behavior-preserving for every case they already covered (net +1 test over FR-034's 258, bringing the crate to 259; full workspace 308 -> 309) | warm-starting commit | Implemented, Verified (unit tests only; scoped to `resolve_dynamic_manifolds` only — `resolve_contacts`/`resolve_contacts_between` are NOT warm-started, a deliberate scoping decision, not an oversight (see spec's own Non-goals); does not close FR-005; does not fix the separately-documented "bouncy resting contact never settles" limitation, which needs sleeping (still unimplemented) instead — sleeping is now the sole remaining item under what used to be a single combined "no warm-starting or sleeping" limitation; the two ambiguities `FR-031`'s audit surfaced (ball radius `92.75` vs. `91.25`; `arena::CEILING_Z` `2044.0` vs. `2048.0`) are untouched by this requirement and remain open; not yet scored against real data) |
| `RB-PHYSICS-001-FR-036` (constant-ambiguity resolution) | `PHASE-1-PHYSICS-CORE` | A dedicated follow-up to `RB-PHYSICS-001-FR-031`'s own constant-calibration audit, resolving the two genuine ambiguities that audit surfaced but deliberately didn't act on, using real source-level research (cloning and reading RocketSim's and RLUtilities' own source, plus the current RLBot wiki, rather than guessing from prior training-data recall). Ball radius: `FR-031` had framed this as a straight "`92.75` vs. `91.25`" choice, but the real games actually split the ball into two separate radii — a smaller inertia radius (`91.25`) and a distinctly larger collision radius (`93.15`, the mesh's own collision margin) — a distinction this port's `RigidBody::sphere` has no room for, since it carries a single unified radius used for both roles and this port has no separate Bullet-style collision margin of its own; the mathematically correct single-constant analog for that unified field is therefore the collision radius, so every `92.75` literal across `crates/rb_physics_bullet/src/solver.rs`, `world.rs`, `net.rs`, and `collision.rs` became `93.15`, not `91.25` — switching to `91.25` would have been a regression, not a fix. `arena::CEILING_Z`: confirmed, via both RocketSim's own `ARENA_HEIGHT = 2048.f` and an independent reconstruction from real extracted collision-mesh geometry, to describe the same reference point this port's `CEILING_Z` does, so `2044.0` became `2048.0`. Two further corrections made as a low-risk byproduct of the same research pass, not new findings requiring their own requirement: `arena::CORNER_LENGTH` (the octagon corner-cut inset) was wrongly documented, by `FR-031` and `FR-019` before it, as an uncalibrated placeholder with no public reference at all — it's actually confirmed exact against real extracted collision-mesh data, so only its doc comments changed, not its value (`1152.0`, already correct); likewise `arena::GOAL_DEPTH` was wrongly documented, by `FR-029`, as an uncalibrated invention with no reference — it's confirmed against the current RLBot wiki's own cited value, so again only its doc comments changed (`880.0`, already correct). Before the mechanical substitution, a targeted `grep -c "892\.75"` across the four non-`arena.rs` files confirmed zero matches in each, ruling out any risk of corrupting `arena::GOAL_HALF_WIDTH`'s unrelated `892.755` literal (which contains `92.75` as a substring but lives only in `arena.rs`, deliberately excluded from the substitution and edited by hand instead). Non-goal: `arena::FILLET_RADIUS`/`CORNER_ARCH_RADIUS` remain untouched and still genuinely uncalibrated — the research this requirement relied on found no analytic single-number reference for either, the same conclusion `FR-031`'s own audit already reached; closing that gap for real would mean ingesting an actual dumped collision mesh, a genuinely different and more involved follow-up deliberately left for later. Does not touch `RB-VERIFY-002` real-data calibration, no longer blocked on `PHASE-0-EXIT` (now closed), but not itself started | `crates/rb_physics_bullet/src/{solver,world,net,collision,arena}.rs` | No new tests: this is a constant-only correction with no new behavior to characterize, the same precedent `FR-031` established for its own constant changes. All 259 of `rb_physics_bullet`'s pre-existing tests pass unchanged (full workspace 309, unchanged from `FR-035`), since every affected test used the ball-radius/`CEILING_Z` constants themselves rather than a hardcoded duplicate of the old values, confirming the correction is behavior-preserving | constant-ambiguity-resolution commit | Implemented, Verified (constant-only correction, no new tests needed per `FR-031`'s own precedent; does not close `FR-005`; `arena::FILLET_RADIUS`/`CORNER_ARCH_RADIUS` remain untouched and still genuinely uncalibrated, a separate mesh-ingestion follow-up; not yet scored against real data) |
| `RB-PHYSICS-001-FR-037` (sleeping) | `PHASE-1-PHYSICS-CORE` | Closes the "no sleeping" half of `solver`'s own documented gap `RB-PHYSICS-001-FR-035` left open (warm-starting closed the other half — see that entry) — the actual fix for a *bouncy* resting contact (restitution > 0) never settling, since restitution re-triggers off a fresh gravity-induced closing velocity every frame regardless of where the solver's own iteration starts, so nothing about warm-starting or split impulse could ever stop the residual bounce, only refusing to integrate it at all once it's small and old enough to call "at rest" does. New `body::RigidBody` fields `is_sleeping: bool` (public) and `sleep_timer: f32` (private), and two new methods: `update_sleep_state(&mut self, dt: f32)` — accumulates `sleep_timer` while both `linear_velocity.length()` and `angular_velocity.length()` stay under new constants `body::LINEAR_SLEEP_VELOCITY_THRESHOLD` (`20.0`) and `body::ANGULAR_SLEEP_VELOCITY_THRESHOLD` (`0.5`); once `sleep_timer` reaches `body::SLEEP_TIME_THRESHOLD` (`0.5` seconds), sets `is_sleeping = true` and forcibly zeroes both velocities (repeated every subsequent call while still under threshold); crossing either threshold resets the timer and clears `is_sleeping` immediately — and `wake(&mut self)`, the same reset unconditionally, independent of velocity. `PhysicsWorld::step` calls `update_sleep_state` for the ball and every car right after every other contact that step is resolved (including the net panels) but before the transform integrates, so a body newly asleep this step also freezes in place this same step. `drive::apply_driven_forces` calls `car.wake()` unconditionally, before anything else in that call runs, whenever a new private helper `input_is_active(input: &ControllerInput) -> bool` finds the car's current input genuinely active (nonzero throttle/steer, a nonzero analog pitch/yaw/roll via `.unwrap_or(0.0) != 0.0`, or `jump`/`boost`/`handbrake` set) — necessary because a resultant-velocity-only wake check isn't enough for a driven body: a car accelerating from rest under a small per-frame driving force whose own one-frame velocity delta is itself smaller than the sleep threshold would otherwise have that delta zeroed right back out every frame, permanently stranding it. `input_is_active` deliberately treats an unrecovered analog channel (`None`) the same as a recovered-but-literally-neutral one (`Some(0.0)`) — both mean "no analog input this tick" — rather than the simpler `*input != ControllerInput::default()`, which would treat any `Some(0.0)` as active purely because it isn't `None`, keeping a car fed a real recorded input stream that always resolves every channel (even at rest) from ever sleeping at all. Non-goals: no persistent-island sleeping the way real Bullet's own architecture does it (this crate's solver already treats each body independently, so per-body sleeping is the natural fit, not a simplified version of something richer this crate lacks); no kinematic/deactivation-disabled body concept to exempt (this crate has no kinematic bodies); all three new threshold constants are this project's own uncalibrated placeholders (no public reference exists for what, if any, real Rocket League's own physics engine uses internally for this purely implementation-internal stabilization detail); a sleeping body only wakes from a contact impulse or active input, never from a sustained-but-below-threshold external push (e.g. a very slow car nudging it) — a deliberate modeling choice matching real Bullet's own behavior, not a bug. Does not touch `RB-PHYSICS-001-FR-005`'s real-data calibration, no longer blocked on `PHASE-0-EXIT` (now closed), but not itself started | `crates/rb_physics_bullet/src/{body,drive,world}.rs` | 8 new tests, bringing `rb_physics_bullet` to 267 total (+8 over `FR-036`'s 259; full workspace 309 -> 317). 5 new tests in `body.rs` exercise `update_sleep_state`/`wake` directly and in isolation: under-threshold velocity doesn't sleep before the time threshold elapses; sustained under-threshold velocity does sleep and zeroes both velocities; velocity above either threshold alone never sleeps; a sleeping body regaining speed above threshold wakes immediately without calling `wake()`; and `wake()` itself clears both `is_sleeping` and the timer regardless of velocity. 3 new tests in `world.rs` prove the same claims through a live `PhysicsWorld`: `a_bouncy_resting_ball_actually_settles_once_asleep` (a nonzero-restitution ball/ground pair run long enough to fall asleep, asserting both `is_sleeping` and exactly-zero velocity — the direct fix for the limitation `resting_ball_stays_at_rest`'s own comment used to only document), `a_sleeping_car_wakes_up_the_instant_throttle_is_applied` (a car seeded already asleep via direct field assignment, isolating the wake-on-input claim), and `a_sleeping_ball_wakes_up_when_a_moving_car_hits_it` (a ball put to sleep before the car even exists in the scene, then a fast-moving car added and driven into it). All pre-existing tests pass unchanged | sleeping commit | Implemented, Verified (unit + live-world tests; closes the "no sleeping" half of the solver-simplification gap `FR-035` left open; a sleeping body only wakes from a contact impulse or active input, never a sustained-but-below-threshold external push, a deliberate modeling choice matching real Bullet's own behavior; does not close `FR-005`; not yet scored against real data) |
| `RB-PHYSICS-001-FR-038` (car-vs-net contact) | `PHASE-1-PHYSICS-CORE` | Closes this port's own former Non-goal that "a car still passes straight through a `net::NetMesh`'s spatial footprint untouched" (`RB-PHYSICS-001-FR-033`'s own entry). `net::NetMesh::step` changed from taking the ball alone (`&mut RigidBody`) to taking every body that can touch the net (`&mut [RigidBody]`) — a single-element slice for the ball alone behaves identically to how this function behaved before this requirement, so every one of its own pre-existing unit tests updates only its call syntax (`std::slice::from_mut(&mut ball)`), not its own assertions. Its inner contact-resolution loop now iterates every body in the slice against each free net point, instead of just the one `ball` parameter. No new collision code was needed at all: `collision::contacts_between` already dispatches to `sphere_vs_box` for a box-vs-sphere pair (a car against a net point) the exact same way it always has for ball-vs-car collision. `PhysicsWorld::step` (in `world.rs`) was refactored to reuse the same ball-plus-cars `bodies` snapshot that `solver::resolve_dynamic_manifolds` already resolves earlier in the same step, passing that same `bodies` vec into each net's own `step` call, and only syncing back to `self.ball`/`self.cars` once every net has had its turn — instead of syncing back immediately after the dynamic-manifold solve and then rebuilding a second snapshot just for the net loop (the old approach, which only ever had `self.ball` to pass since nets were ball-only). Doc comments updated throughout `net.rs` and `world.rs` (the `PhysicsWorld` struct's own `nets` field doc comment, `with_net`'s own doc comment, and the module-level doc comment near the top of `world.rs`) to reflect that nets now catch cars too, not just the ball. Non-goals: everything `RB-PHYSICS-001-FR-033`'s own Non-goals already scoped out for the ball stays out for a car too — manifold richness beyond one contact per overlapping point per body (no clipped-face-style manifold), a full 3D "sock" shape billowing backward from the goal mouth, and bending stiffness (only structural + shear springs). No new per-body distinction exists in how a net treats a car versus the ball — same restitution/friction, same point-mass contact resolution — since nothing about a real net's own physical behavior toward a car should differ from toward a ball at this level of fidelity, and this port has no evidence (real or cited) suggesting otherwise. Does not touch `RB-PHYSICS-001-FR-005`'s real-data calibration, no longer blocked on `PHASE-0-EXIT` (now closed), but not itself started | `crates/rb_physics_bullet/src/{net,world}.rs` | 3 new tests, bringing `rb_physics_bullet` to 271 total (+3 over `FR-039`'s 268; full workspace 318 -> 321). 2 new tests in `net.rs`: `a_car_shot_into_the_net_is_measurably_slowed_compared_to_free_flight` (the direct car analog of the pre-existing `a_ball_shot_into_the_net_is_measurably_slowed_compared_to_free_flight`, proving a car shot into the net loses measurably more speed than the same car in free flight) and `a_ball_and_a_car_are_both_resolved_against_the_same_net_step` (both a ball and a car passed into one `step` call, positioned far enough apart along the net's own width that they can't touch each other, proving the slice's own iteration genuinely resolves every element, not just the first — a claim the old single-body signature couldn't even represent). 1 new test in `world.rs`: `a_car_shot_at_a_goal_net_is_caught_instead_of_passing_through_untouched`, the live-`PhysicsWorld` "caught vs. free flight" proof mirroring the pre-existing ball version exactly — floated near the net panel's own vertical center (z=300, matching the ball version's own positioning) rather than resting on the ground, since a car sized to rest at ground height would only ever reach the panel's anchored bottom row, which `NetMesh::step`'s own contact-resolution loop deliberately skips (anchored points are excluded from contact resolution, not just from force/integration) — a real trap this test's own first draft fell into (it initially floated the car at ground height and got a false failure where both the "with net" and "without net" runs showed zero speed loss, since the car only ever touched the net's own immovable anchored perimeter) before being corrected to float at the panel's vertical middle instead. All pre-existing tests pass unchanged | car-vs-net-contact commit | Implemented, Verified (unit + live-world tests; closes this port's own former "a car still passes through untouched" Non-goal from `FR-033`; no new per-body distinction in how a net treats a car versus the ball; does not close `FR-005`; not yet scored against real data) |
| `RB-PHYSICS-001-FR-039` (wall-jump corner disambiguation) | `PHASE-1-PHYSICS-CORE` | `PhysicsWorld::step`'s per-car `car_wall_normal` computation (feeding `drive::apply_driven_forces`'s `wall_normal: Option<Vec3>` parameter) now collects the normals of every wall a car is touching this step (`.filter(...).map(|wall| wall.normal).collect()`), sums them, and calls `.normalize()` on the sum — falling back to the first touched wall's own normal via `.or_else(|| touched_normals.first().copied())` in the geometrically-impossible-for-a-convex-arena case where the sum is exactly zero — replacing the old `Iterator::find`, which returned only the first wall in `self.walls` a car touched. A car touching exactly one wall gets that same wall's own unit normal back unchanged (summing one unit vector and normalizing it is a no-op), so the single-wall case, covered by many pre-existing tests, is unaffected; a car touching two walls at a corner now gets a normalized diagonal blend of both walls' normals instead of firing along only one of them depending on iteration order. No new collision-detection code was needed: `resolve_plane_contact` already resolved a car touching two walls simultaneously correctly (each wall resolved independently every step, unaffected by this change) — only the wall-jump *direction picker*, `drive::apply_driven_forces`'s own input computed in `world.rs`, was affected | `crates/rb_physics_bullet/src/world.rs` | `rb_physics_bullet` unit tests: 1 new test in `world.rs`, `a_car_touching_two_walls_at_a_corner_wall_jumps_diagonally_outward` (two perpendicular walls, normals `(1,0,0)` and `(0,1,0)`, a car positioned via zero-gap contact to touch both simultaneously, jump pressed while airborne with gravity zeroed to isolate the wall jump; asserts both `vx > 0.0` and `vy > 0.0` — proving the push-off isn't purely along one axis, which the old bug would have zeroed one of — `|vx - vy| < 1.0` since the two walls are symmetric so a true normalized blend gives equal components, and the same roughly-`JUMP_SPEED` vertical-velocity check every wall-jump test already makes); all pre-existing wall-jump tests, including `a_car_touching_a_wall_wall_jumps_outward_and_upward`, pass unchanged (net +1 test over FR-037's 267, bringing the crate to 268; full workspace 317 -> 318) | wall-jump-corner-disambiguation commit | Implemented, Verified (unit tests only; closes the "first wall in `self.walls`" simplification `FR-013` originally documented and `FR-019`'s own new diagonal corner walls made reachable in the standard arena for the first time; no genuine multi-wall solid-angle weighting is modeled — an unweighted sum, same as summing unit-length contact normals in `collision`'s own multi-contact manifolds elsewhere in this crate; does not close `FR-005`; not yet scored against real data) |
| `RB-PHYSICS-001-FR-040` (fillet-radius calibration research) | `PHASE-1-PHYSICS-CORE` | A dedicated research pass, matching `RB-PHYSICS-001-FR-036`'s own real-source-research method, specifically targeting the two remaining uncalibrated placeholder constants `RB-PHYSICS-001-FR-036` itself deliberately left untouched: `arena::FILLET_RADIUS` and `arena::CORNER_ARCH_RADIUS`. Searched this port's established reference tier — RocketSim's and RLUtilities' own source, the RLBot wiki, and RLGym's own documented game-value list — for any named constant or cited measurement describing either radius; RocketSim itself has no such constant to find at all, since it uses a raw dumped triangle mesh rather than a parameterized radius. Found exactly one candidate, on the RLBot wiki's "Useful Game Values" page: "Wall bottom ramp radius: Aprox. 256 (but they are not circular)". Deliberately NOT adopted for either constant: it carries no citation or attribution at all (unlike RocketSim's own named `ARENA_HEIGHT = 2048.f` constant `FR-036` read directly from source), it doesn't distinguish a cardinal wall's `FILLET_RADIUS` from a diagonal corner wall's distinctly bigger `CORNER_ARCH_RADIUS` (this project's two separate constants since `RB-PHYSICS-001-FR-025`), and its own wording explicitly disclaims describing a true circular arc at all. Cross-checking RLGym's own documented game values surfaced a further reason for caution: RLGym separately documents `RAMP_HEIGHT = 256` — the corner boost-pad ramp's vertical height from the ground, a completely different geometric quantity from a floor-seam curve's radius — the same numeral as the wiki's "ramp radius" entry, suggesting the wiki page may conflate the two rather than independently measuring a radius at all. Adopting `256` for either constant would trade one honestly-labeled uncalibrated placeholder for a differently-uncertain number dressed up as a real citation, a worse outcome than leaving the honest placeholder in place. Both `arena::FILLET_RADIUS`/`CORNER_ARCH_RADIUS` remain completely unchanged (`292.0`/`750.0`) and genuinely uncalibrated. Doc comments on both constants, plus this spec's Non-goals and Open Questions, were updated to record this finding so a future contributor doesn't re-tread the same search — genuinely closing this gap needs actual extracted collision-mesh geometry (e.g. via `ZealanL/RLArenaCollisionDumper`'s real triangle-mesh dump), which needs the owner's own Windows/Rocket League environment, the same blocker `RB-VERIFY-002-FR-001` already documents. Does not touch `RB-PHYSICS-001-FR-005`'s real-data calibration, no longer blocked on `PHASE-0-EXIT` (now closed), but not itself started | `crates/rb_physics_bullet/src/arena.rs` (doc comments only, no value changed), `docs/specifications/physics/RB-PHYSICS-001-physics-core-port.md` | No new tests: this is a documentation/research-only correction with no new runtime behavior to characterize, the same precedent `FR-031`/`FR-036` established for their own constant-audit findings that didn't change a value. All 271 of `rb_physics_bullet`'s pre-existing tests pass unchanged (full workspace 321, unchanged from `FR-038`) | fillet-radius-calibration-research commit | Investigated, Verified (documentation/research-only; a search against this port's established reference tier — RocketSim/RLUtilities source, the RLBot wiki, RLGym's game-value list — found no adoptable value for either constant; `arena::FILLET_RADIUS`/`CORNER_ARCH_RADIUS` remain unchanged (`292.0`/`750.0`) and genuinely uncalibrated; genuinely closing this needs actual extracted collision-mesh geometry, blocked on the owner's own Windows/Rocket League environment, the same blocker `RB-VERIFY-002-FR-001` already documents; does not close `FR-005`; not yet scored against real data) |
| `RB-PHYSICS-001-FR-041` (sandwiched-solve convergence) | `PHASE-1-PHYSICS-CORE` | Investigated whether anything short of real recorded ground-truth data could narrow `RB-PHYSICS-001-FR-030`'s own documented extreme-mass-ratio "sandwiched" under-convergence gap at this crate's fixed `SOLVER_ITERATIONS = 10`. An experiment tried a naive global SOR-style relaxation factor first, scaling every manifold's normal-row impulse update by a fixed `omega` before clamping: factors above `1.0` (over-relaxation) made `FR-030`'s own `symmetric_pinch` test scenario measurably *diverge* — the sandwiched ball ended up faster than even the pre-`FR-030` independent-pairwise approach, not just under-converged — while factors below `1.0` (under-relaxation) made it monotonically *better* the smaller they got, with no instability observed down to `0.1`, matching standard PGS/SOR theory for a tightly-coupled multi-body constraint system (a body touched by `k` other bodies has each of those `k` manifolds independently apply its own full correction against that body's own accumulating delta velocity every iteration, over-correcting by roughly a factor of `k`). Rather than adopt a tuned magic `omega` (the kind of unvalidated calibration `RB-PHYSICS-001-FR-031`/`FR-036`/`FR-040`'s own precedent treats as needing real data to justify), `solver::resolve_dynamic_manifolds` now counts how many manifolds reference each body index this step and scales each manifold's velocity-row impulse (normal plus both friction directions; the split-impulse push row via `resolve_two_body_push_row` is untouched) by a parameter-free `1 / k`, where `k` is the largest manifold-count either of that manifold's two bodies has — the same "fair share" weighting position-based-dynamics solvers use for a point mass under several simultaneous constraints, via a new `resolve_two_body_row_relaxed` helper (`resolve_two_body_row` now a thin `impulse_scale = 1.0` wrapper around it). This is mathematically dominant rather than a fidelity trade-off — it can only reduce, never increase, a shared body's per-iteration overshoot — so it needed no real recorded data to justify adopting, unlike raising `SOLVER_ITERATIONS` itself (a real added per-step cost, confirmed manually to converge much closer at 300 iterations but not adopted here). Measured directly on `FR-030`'s own `symmetric_pinch` scenario (ball mass 1 sandwiched between two mass-180 cars closing from opposite sides): the combined solve's result narrows from ~89.5 to ~32 units/s (independent-pairwise stays ~98.9), a real, further reduction of the gap to the true zero-velocity answer, at zero added iteration cost. Does not achieve full convergence within one call's fixed `SOLVER_ITERATIONS` — narrowed, not closed. Does not touch `resolve_contacts`/`resolve_contacts_between` (per `RB-PHYSICS-001-FR-035`'s own Non-goals, neither has shared-body contention to correct). Does not touch `RB-PHYSICS-001-FR-005`'s real-data calibration, no longer blocked on `PHASE-0-EXIT` (now closed), but not itself started | `crates/rb_physics_bullet/src/solver.rs` | `rb_physics_bullet` unit tests: 2 new in `solver.rs` — `resolve_dynamic_manifolds_relaxes_a_shared_bodys_impulse_by_its_own_contact_degree` reuses `FR-030`'s own `symmetric_pinch` scenario and asserts the combined-solve ball speed lands below `50.0` units/s (comfortably below the measured ~32, well below the pre-`FR-041` ~89.5); `resolve_dynamic_manifolds_with_one_manifold_per_body_matches_resolve_contacts_between` builds an ordinary single ball-vs-car manifold and asserts `resolve_dynamic_manifolds` matches `resolve_contacts_between` called directly to within `1e-4`, proving the `k == 1` case (the overwhelming majority of contacts) is a genuine bit-for-bit no-op rather than merely "close enough". All 271 of `rb_physics_bullet`'s pre-existing tests (as of `FR-040`) pass unchanged (net +2 tests, bringing the crate to 273; full workspace 321 -> 323) | sandwiched-solve-convergence commit | Implemented, Verified (unit tests only; a naive global over-relaxation factor was tried and deliberately rejected — provably diverges `FR-030`'s own scenario above `omega = 1.0` — in favor of the parameter-free `1 / k` shared-body scale that shipped instead; narrows, does not close, `FR-030`'s own sandwiched under-convergence gap within one call's fixed `SOLVER_ITERATIONS`; raising `SOLVER_ITERATIONS` itself remains a real added per-step cost not adopted without real recorded data; does not close `FR-005`; not yet scored against real data) |
| `RB-PHYSICS-001-FR-042` (box-vs-box reference validation) | `PHASE-1-PHYSICS-CORE` | This spec's own Open Questions flagged `collision::box_vs_box`'s edge-edge contact-point derivation and its face-clipping degenerate ("zero points") fallback as "reasonable, tested choices" never actually validated against Bullet's own `btBoxBoxDetector::dBoxBox` output. Fetched and read that reference source directly (not guessed at, matching `RB-PHYSICS-001-FR-036`'s own real-source-research method) and checked three specific things against it. (1) Edge-edge contact point: `dBoxBox`'s own reference computes it via `dLineClosestApproach` — closest approach between two *infinite lines*, with the resulting `alpha`/`beta` offsets applied with no clamping to the finite edge length anywhere (confirmed directly in the fetched source: `dLineClosestApproach(pa, ua, pb, ub, &alpha, &beta); pa[i] += ua[i]*alpha; pb[i] += ub[i]*beta;` — no bounds check nearby). This port's own `closest_points_on_segments` instead implements Ericson's proper finite-*segment* closest-point construction (clamping `s`/`t` to `[0, 1]` with the two-pass re-projection correctness requires) — strictly more rigorous than the reference it ports from, not merely equivalent, a genuine sourced improvement; no code change needed. (2) Face-clipping degenerate fallback: `dBoxBox`'s own reference contains the exact same undocumented judgment call this port's own code comment already made — two separate `// this should never happen` comments (one after its own `intersectRectQuad2` clipping call, one after filtering clipped points to penetrating-only) with zero geometric justification given either time, confirming this port's own "reasonable, tested choice, not rigorously proven" framing wasn't a weaker position than the reference author's own. Where the two diverge is *policy*, not algorithm: `dBoxBox`'s own fallback there is `return 0` (drop the collision entirely), while this port's own fallback synthesizes one contact at a clamped-center point instead — a deliberate, favorable divergence, since SAT has already confirmed real geometric overlap along this axis by the time either fallback triggers, so silently dropping a confirmed collision (as the reference does) risks a car tunneling through in a rare grazing configuration, while reporting a synthesized contact does not; kept as-is, no code change. (3) Edge-edge tangent sign-selection heuristic: picking which one of a box's 4 candidate parallel edges (along the SAT-chosen axis) is "near" requires a heuristic either way — this port's own `edge_contact` picks it via the raw center-to-center vector `d`; `dBoxBox`'s own reference instead uses the actual resolved collision-normal direction. A candidate fix (swap `d` for the already-available `normal`) was built and empirically tested against a true brute-force ground truth (all 16 sign combinations per configuration, minimum segment-to-segment distance, across 50,000 randomized two-box configurations): genuinely mixed, not a clean win either way — the current `d`-based heuristic matches the true optimal edge pair more often at large/arbitrary penetration depths (~11.6% vs. ~8.7%) and is rarely worse head-to-head (1,019 cases vs. 6,653 the other way), while the `normal`-based candidate matches optimal far more often at realistic near-first-contact depths under 0.5 units (~93% vs. ~77%, the regime this port's solver actually operates in every substep), but both still have individual outlier cases many units off the true optimum. With no real recorded car-vs-car contact data to judge which regime matters more for this project's fidelity goals, the candidate fix was not adopted — `d`-based selection kept unchanged, left as a documented still-open item (a truly rigorous fix needs a non-heuristic nearest-edge-pair selection, not a swap between two different imperfect heuristics). Zero production code changed anywhere in this requirement — only doc comments in `collision.rs`, matching the established precedent of `RB-PHYSICS-001-FR-032` (built and reverted a GJK narrow-phase replacement after finding it answered the wrong question) and `RB-PHYSICS-001-FR-040` (fillet-radius calibration research, found no adoptable value): a rigorously investigated negative/mixed result is real, valuable work, not a failure to record. Does not touch `RB-PHYSICS-001-FR-005`'s real-data calibration, no longer blocked on `PHASE-0-EXIT` (now closed), but not itself started | `crates/rb_physics_bullet/src/collision.rs` (doc comments only, on `closest_points_on_segments`, `face_contact`'s `if contacts.is_empty()` fallback, and `edge_contact` — no production code changed), `docs/specifications/physics/RB-PHYSICS-001-physics-core-port.md` | No new tests: this is a documentation/research-only correction with no adopted runtime-behavior change, the same precedent `RB-PHYSICS-001-FR-032`/`FR-040` established for a rigorously investigated negative/mixed result being real, valuable work in its own right. The candidate fix's own empirical comparison (a temporary, deterministic pseudo-random probe test comparing the current heuristic, the candidate fix, and a brute-force ground truth across 50,000 configurations) was built and run during investigation, not shipped, the same "confirmed during test design, not shipped" precedent `RB-PHYSICS-001-FR-030`'s own 300-iteration manual check established. All 273 of `rb_physics_bullet`'s pre-existing tests (as of `FR-041`) pass unchanged (full workspace 323, unchanged from `FR-041`) | box-vs-box-reference-validation commit | Investigated, Verified (documentation/research-only; no production code changed, only doc comments; edge-edge contact point confirmed strictly more rigorous than Bullet's own `dBoxBox` reference; face-clipping synthesize-rather-than-drop fallback confirmed a deliberate, favorable divergence from the reference's own `return 0`; a normal-based candidate fix for the edge-edge tangent sign-selection heuristic was built and empirically tested but found genuinely regime-dependent and not adopted, left as a still-open item; does not close `FR-005`; not yet scored against real data) |
| `RB-PHYSICS-001-FR-043` (restitution/friction combine-mode reference validation) | `PHASE-1-PHYSICS-CORE` | This spec's own "Restitution/friction combine mode" Open Question asserted, without ever having checked, that Bullet's actual default is `btMax` for both. Fetched and read Bullet's real `btManifoldResult::calculateCombinedRestitution`/`calculateCombinedFriction` reference source directly (matching `RB-PHYSICS-001-FR-036`/`FR-042`'s own method — `btManifoldResult.h` and `btManifoldResult.cpp` read in full, not a partial or summarized read) and found that claim simply wrong: the actual default for both is an unclamped **product** (`a * b`; friction's own version additionally clamps the result to `[-10, 10]`), with no `max` mode, no `sqrt`/geometric-mean, and no per-pair combine-mode override anywhere in the reference short of a custom `gContactAddedCallback`. This port's own `solver::combine_restitution`/`combine_friction` already use average (`(a + b) * 0.5`), previously justified by the now-corrected wrong claim; re-examined against the real default and kept anyway, now for a genuine, positive reason: unlike the reference's product, average preserves the identity `combine(a, a) == a` (`0.5` and `0.5` average to `0.5`; the reference's own product gives `0.25`) — this matters specifically for this port, since every `RigidBody`/`StaticPlane`/`StaticQuarterPipe`/`StaticCornerFillet`/`StaticGoalWall` variant's `Default` impl currently assigns the same uncalibrated placeholder `0.5` for both coefficients (see `body.rs`), so under the reference's real product default, the overwhelming majority of this port's own contacts today would silently combine to `0.25`, a value nobody chose. Whether either formula is what real Rocket League itself actually does remains genuinely unknown either way, and stays exactly as open as it was before this requirement — this requirement only corrected which of the two known quantities (this port's choice, and Bullet's real default) was being compared, not which one is game-truthful. Does not change `combine_restitution`/`combine_friction`'s formula (average kept, not switched to product). Does not calibrate `RB-PHYSICS-001-FR-005`'s real-data question of which combine mode (if either) matches real Rocket League, no longer blocked on `PHASE-0-EXIT` (now closed), but not itself started. Does not touch any other Bullet-reference claim elsewhere in this spec — only the one this requirement's own Open Questions bullet made about combine mode | `crates/rb_physics_bullet/src/solver.rs` (doc comments plus `combine_restitution`/`combine_friction`'s own justification), `crates/rb_physics_bullet/src/body.rs` (field doc comment only — no production code changed anywhere in this requirement), `docs/specifications/physics/RB-PHYSICS-001-physics-core-port.md` | 2 new `solver.rs` tests — `combine_restitution_preserves_a_uniform_coefficients_identity` and `combine_friction_preserves_a_uniform_coefficients_identity`, each asserting `combine(0.5, 0.5) == 0.5` (and a second same-value pair) and explicitly asserting the result differs from the reference's own `0.5 * 0.5` product, pinning the exact identity property this requirement's justification for keeping average now rests on. All 273 of `rb_physics_bullet`'s pre-existing tests (as of `FR-042`) pass unchanged (net +2 tests over `FR-042`'s 273, bringing the crate to 275; full workspace 323 -> 325) | restitution-friction-combine-mode-reference-validation commit | Investigated, Verified (documentation/research-only; zero production behavior changed, only doc comments and 2 new dedicated identity-preserving unit tests; this spec's own prior "Bullet's default is `btMax`" claim confirmed wrong and corrected everywhere it appeared — the real default is an unclamped product; this port's own average combine mode is unchanged, now justified by the identity-preservation property instead of the old wrong claim; whether either formula matches real Rocket League itself remains genuinely open; does not close `FR-005`; not yet scored against real data) |
| `RB-PHYSICS-001-FR-044` (stale Non-goals correction) | `PHASE-1-PHYSICS-CORE` | This spec's own top-level "Non-goals (this increment)" section carried a "**Split impulse.** This port always takes Bullet's non-split contact-resolution branch" bullet that had gone stale — `RB-PHYSICS-001-FR-034` implemented split impulse (a second, separate "push" pseudo-velocity channel per contact's normal row, fed only by positional/penetration error, applied directly to position/orientation rather than folding into real velocity) well before this requirement, and that Requirements entry, the version 0.34.0 Change History entry, and `rb_physics_bullet::solver`'s own module doc comment all already correctly describe it as implemented — only this one Non-goals bullet had never been updated to match. Confirmed the implementation is genuinely present (not merely documented) by locating `solver::resolve_push_row`/`resolve_two_body_push_row`/`apply_push_delta` directly in `solver.rs`, and confirmed via a repo-wide `grep` that this was the only occurrence of the stale claim anywhere in code or docs. Corrected the bullet to a strikethrough-and-close note, matching the same convention this section already uses for its own two other resolved Non-goals items (the wall-jump-corner disambiguation, closed via `FR-039`; the curved-geometry Non-goal, closed progressively via `FR-026` through `FR-033`) — a pattern this spec had already established for exactly this situation, just not yet applied here. Does not change any production code — `solver::combine_restitution`/`combine_friction`, `resolve_push_row`/`resolve_two_body_push_row`/`apply_push_delta`, and every other function this bullet touches are unchanged; only the Non-goals bullet's own text was wrong, not the implementation it described. Does not re-audit the rest of this spec's Non-goals section for other staleness beyond the one bullet this requirement found — every other bullet in that section was checked against its own referenced FR and confirmed still accurate as of this requirement | `docs/specifications/physics/RB-PHYSICS-001-physics-core-port.md` (Non-goals bullet corrected to a strikethrough-and-close note, Requirements entry added, header status paragraph and Change History updated; version bumped to 0.44.0) — no source files touched, `solver.rs`/`body.rs`/every other file in `crates/rb_physics_bullet` unchanged | No new tests: this is a pure documentation correction with no runtime behavior to characterize, the same precedent `RB-PHYSICS-001-FR-032`/`FR-040`/`FR-042`/`FR-043` established for documentation-only findings being real, valuable work. All 275 of `rb_physics_bullet`'s pre-existing tests (as of `FR-043`) pass unchanged (total unchanged from `FR-043`) | stale-split-impulse-non-goal-correction commit | Investigated, Verified (documentation-only; zero production code changed, no new tests; this spec's own stale "Split impulse" Non-goals bullet — contradicted by `FR-034`'s own already-shipped implementation — corrected to a strikethrough-and-close note, matching the same convention already used for two other resolved Non-goals items; does not close `FR-005`; not yet scored against real data) |
| `RB-PHYSICS-001-FR-045` (`integrate.rs` reference validation) | `PHASE-1-PHYSICS-CORE` | `integrate.rs`'s own doc comments claim close fidelity to three real Bullet functions — `btRigidBody::applyDamping`, `btRigidBody::integrateVelocities`, and `btTransformUtil::integrateTransform` — never actually checked against Bullet's own source. Fetched and read all three reference files directly (`btRigidBody.cpp`/`.h`, `btTransformUtil.h`, plus `btQuaternion.h`/`btScalar.h` for the constants each depends on), matching `RB-PHYSICS-001-FR-036`/`FR-042`/`FR-043`'s own method, and checked every specific claim against it. (1) `apply_damping`'s "Bullet's default" claim: confirmed `BT_USE_OLD_DAMPING_METHOD` is never `#define`d anywhere in the fetched reference, so the `#else` branch (`pow(1 - damping, dt)` for both linear and angular) is genuinely what an unmodified Bullet build runs, not an assumption — the formula itself matches exactly too. (2) `integrate_velocities`'s `MAX_ANGVEL` clamp: confirmed `#define MAX_ANGVEL SIMD_HALF_PI` directly in the reference (`SIMD_HALF_PI == PI / 2`, matching this port's `FRAC_PI_2`), and the clamp formula (`angular_velocity *= (MAX_ANGVEL / dt) / angvel`) matches byte-for-byte. (3) `integrate_transform`'s exponential-map math: confirmed `ANGULAR_MOTION_THRESHOLD` (`0.5 * SIMD_HALF_PI`), the small-angle Taylor coefficient (`1 / 48`, written `0.020833333333` in the reference), and the sinc-based rotation-axis formula all match byte-for-byte against `btTransformUtil.h`. (4) A genuine, minor numeric difference found, not adopted: this function's own degenerate-quaternion guard uses `length_squared() > 1e-12`; the reference's equivalent guard (inside `safeNormalize` and again in the caller) compares against `SIMD_EPSILON`, which is `FLT_EPSILON` — about `1.19e-7` for `f32`, roughly 5 orders of magnitude larger than this port's own threshold — not adopted, since both values are far below any physically realistic quaternion magnitude, behaviorally indistinguishable for every scenario this crate's test suite (or any plausible simulation state) can reach, the same standard `RB-PHYSICS-001-FR-031`/`FR-036`/`FR-040` already hold uncalibrated constants to. (5) A more significant finding: the fallback branch is load-bearing, not defensive theater. The reference's own `integrateTransform` calls `predictedOrn.safeNormalize()` (itself internally guarded — it only normalizes when `length2() > SIMD_EPSILON`, otherwise leaves the quaternion untouched), then only accepts the result if it still clears that same threshold; otherwise it keeps the transform's pre-existing basis, i.e. the body's old orientation. This function's own `else { orientation }` branch mirrors that exact choice. Had this function instead called `predicted.normalize()` unconditionally, `rb_domain::Quat::normalize`'s own generic internal guard would have silently substituted `IDENTITY` for a degenerate result instead — a real, observable divergence from Bullet's actual reference behavior, which never resets to identity here, only ever falls back to the orientation already in hand. Does not change `apply_damping`'s or `integrate_velocities`'s formulas or constants — both confirmed already exact. Does not change `integrate_transform`'s degenerate-quaternion epsilon threshold — kept at `1e-12` for lack of a concrete reason to adopt Bullet's own `SIMD_EPSILON` value instead. Does not touch `RB-PHYSICS-001-FR-005`'s real-data calibration, no longer blocked on `PHASE-0-EXIT` (now closed), but not itself started | `crates/rb_physics_bullet/src/integrate.rs` (doc comments only — no production code changed), reference source checked: Bullet's real `btRigidBody.cpp`/`.h`, `btTransformUtil.h`, `btQuaternion.h`, `btScalar.h` (fetched and read, not part of this repo), `docs/specifications/physics/RB-PHYSICS-001-physics-core-port.md` | 1 new `integrate.rs` test — `integrate_transform_preserves_a_degenerate_orientation_instead_of_snapping_to_identity` — passes a deliberately degenerate (all-zero) orientation as input and asserts the function returns that same degenerate value back unchanged, not `Quat::IDENTITY`, confirming the fallback branch's real, load-bearing purpose rather than merely confirming it doesn't panic. All 275 of `rb_physics_bullet`'s pre-existing tests (as of `FR-044`) pass unchanged (net +1 test over `FR-044`'s 275, bringing the crate to 276) | integrate-reference-validation commit | Investigated, Verified (documentation/research-only; zero production code changed, only doc comments and 1 new dedicated test; every Bullet-reference claim in `integrate.rs`'s doc comments confirmed byte-for-byte accurate; one minor numeric epsilon difference found and not adopted, behaviorally indistinguishable; the check-then-normalize fallback branch confirmed load-bearing — matches Bullet's own real fallback to preserve prior orientation rather than reset to identity; does not close `FR-005`; not yet scored against real data) |
| `RB-PHYSICS-001-FR-046` (`body.rs`/`mat3.rs` reference validation) | `PHASE-1-PHYSICS-CORE` | `body.rs`'s `Shape::local_inertia`/`RigidBody::update_inertia_tensor` and `mat3.rs`'s `Mat3::scaled_columns`/`Mat3::from_quat` all claim close fidelity to specific real Bullet functions, never actually checked against Bullet's own source. Fetched and read every one directly (`btSphereShape.cpp`, `btBoxShape.cpp`, `btRigidBody.cpp`/`.h`, `btMatrix3x3.h`), matching `RB-PHYSICS-001-FR-036`/`FR-042`/`FR-043`/`FR-045`'s own method, and checked every specific claim against it. (1) `local_inertia`'s sphere/box formulas: confirmed `btSphereShape::calculateLocalInertia`'s `0.4 * mass * margin^2` (Bullet's sphere shape uses its own collision margin as the radius for this purpose, the same analog `RB-PHYSICS-001-FR-036` already established this port's own single radius field plays) and `btBoxShape::calculateLocalInertia`'s `mass / 12 * (ly^2 + lz^2)` for x (and the corresponding cyclic permutations for y/z) both match this port's own formulas byte-for-byte, including axis ordering. (2) `update_inertia_tensor`: confirmed `btRigidBody::updateInertiaTensor` (`m_invInertiaTensorWorld = basis.scaled(invInertiaLocal) * basis.transpose()`) matches this function's own implementation exactly, and that `Mat3::scaled_columns` matches `btMatrix3x3::scaled`'s own per-column scaling (`m_el[row][col] * s[col]`) byte-for-byte. (3) A genuine difference found in `Mat3::from_quat`, not adopted: the reference's own `btMatrix3x3::setRotation` (non-SIMD branch) computes `s = 2 / q.length2()` and scales every cross term by `s`, self-correcting for a quaternion that isn't already unit length; this function instead hardcodes the `2` (`x2 = x + x` etc.), assuming exact unit length. Confirmed empirically (new test) that feeding this function a deliberately scaled, non-unit-length quaternion produces a matrix whose rows are no longer unit length — unlike Bullet's own self-correcting version, which would still produce a valid rotation matrix for the same input. Not adopted as a fix: this function's only production call site (`RigidBody::update_inertia_tensor`) always receives `orientation` immediately after `integrate::integrate_transform`'s own renormalization (see that function's own `RB-PHYSICS-001-FR-045` doc comment), so the input here is never meaningfully non-unit-length in practice — unlike `btMatrix3x3::setRotation`, a general-purpose utility called from many places in real Bullet with far less controlled inputs, adding the reference's own self-correction here would be pure defensive theater for an unreachable case. Does not change `local_inertia`/`update_inertia_tensor`/`scaled_columns` — all three confirmed already exact. Does not add self-correction to `Mat3::from_quat` for a non-unit-length input, for lack of a reachable production scenario that would exercise it. Does not touch `RB-PHYSICS-001-FR-005`'s real-data calibration, no longer blocked on `PHASE-0-EXIT` (now closed), but not itself started | `crates/rb_physics_bullet/src/body.rs` (doc comments on `Shape::local_inertia`/`RigidBody::update_inertia_tensor` only), `crates/rb_physics_bullet/src/mat3.rs` (doc comments on `Mat3::scaled_columns`/`Mat3::from_quat` only — no production code changed anywhere in this requirement), reference source checked: Bullet's real `btSphereShape.cpp`, `btBoxShape.cpp`, `btRigidBody.cpp`/`.h`, `btMatrix3x3.h` (fetched and read, not part of this repo), `docs/specifications/physics/RB-PHYSICS-001-physics-core-port.md` | 1 new `mat3.rs` test — `from_quat_does_not_self_correct_a_non_unit_length_quaternion` — builds a known unit quaternion and a scaled (length² = 4) copy of it, and asserts the unit quaternion's own resulting matrix has unit-length rows while the scaled quaternion's own resulting matrix does not, confirming the exact distinction from Bullet's own self-correcting reference. All 276 of `rb_physics_bullet`'s pre-existing tests (as of `FR-045`) pass unchanged (net +1 test over `FR-045`'s 276, bringing the crate to 277) | body-mat3-reference-validation commit | Investigated, Verified (documentation/research-only; zero production code changed, only doc comments and 1 new dedicated test; every Bullet-reference claim in `body.rs`/`mat3.rs`'s doc comments confirmed byte-for-byte accurate; one genuine difference found in `Mat3::from_quat`'s hardcoded `s = 2` factor vs. Bullet's own self-correcting `s = 2 / q.length2()`, not adopted since the difference is unreachable given `integrate_transform`'s always-renormalized input; does not close `FR-005`; not yet scored against real data) |
| `RB-PHYSICS-001-FR-047` (`collision.rs` remaining closed-form shape pairings reference validation) | `PHASE-1-PHYSICS-CORE` | `collision.rs`'s `sphere_vs_plane`, `box_vs_plane`, `sphere_vs_box`, and `sphere_vs_sphere` all claim to be closed-form reductions of specific real Bullet collision algorithms, never actually checked against Bullet's own source (`box_vs_box` was already checked this way via `RB-PHYSICS-001-FR-042`). Fetched and read every one of the remaining four's own real references directly (`btConvexPlaneCollisionAlgorithm.cpp`/`.h`, `btSphereBoxCollisionAlgorithm.cpp`, `btSphereSphereCollisionAlgorithm.cpp`, `btManifoldPoint.h`), matching `RB-PHYSICS-001-FR-036`/`FR-042`/`FR-043`/`FR-045`/`FR-046`'s own method, and checked every specific claim against it. (1) `sphere_vs_plane` and `sphere_vs_sphere` confirmed exact: a sphere's GJK support vertex along `-planeNormal` is exactly `center - radius * planeNormal`, so `sphere_vs_plane` is Bullet's own `btConvexPlaneCollisionAlgorithm::processCollision` reduced analytically, not an approximation of it; `sphere_vs_sphere`'s `diff`/`normalOnSurfaceB`/`pos1`/`dist` construction matches real `btSphereSphereCollisionAlgorithm::processCollision` line for line. (2) `sphere_vs_box`'s deep-penetration face selection confirmed to reproduce Bullet's own exact tie-break order: real `btSphereBoxCollisionAlgorithm::getSpherePenetration` initializes its running minimum to the `+x` face and checks `+x, -x, +y, -y, +z, -z` in that fixed order, only overriding on a strictly smaller distance, so an exact tie always resolves to whichever face is checked earliest — this function's own per-axis-margin-then-sign approach was worked through several non-symmetric tied cases by hand and confirmed to reproduce the identical resolution, not merely a different mathematically-valid alternative. Two harmless, unadopted numeric-epsilon differences also found, in the same category as `RB-PHYSICS-001-FR-045`'s degenerate-quaternion-guard finding: this function's outside/inside-branch threshold is linear (`outside_distance > 1e-6`) versus real Bullet's squared (`dist2 <= SIMD_EPSILON`, roughly 2.5 orders of magnitude looser as a linear bound), and `sphere_vs_sphere`'s degenerate-coincident-centers threshold (`1e-6`) versus Bullet's `SIMD_EPSILON` (~1.19e-7). (3) A genuine, deliberate divergence found in `box_vs_plane`, not adopted: real `btConvexPlaneCollisionAlgorithm` does not compute every extreme corner in one pass — `processCollision` calls a single GJK support-vertex query along `-planeNormal`, producing exactly one contact point per frame, relying on real Bullet's own default configuration (`m_numPerturbationIterations = 1`, `m_minimumPointsPerturbationThreshold = 0`, making its optional multi-point "perturbation" path never trigger) and several frames of persistent-manifold accumulation to reach a resting box's full 4-corner manifold gradually. This function's own instantaneous, exact 4-corner computation is confirmed a deliberate, more rigorous simplification of that real single-vertex-plus-persistence dance — the same favorable divergence already established for `box_vs_box` against `dBoxBox` (`RB-PHYSICS-001-FR-042`) — not adopted, since replicating Bullet's own frame-by-frame settling behavior would only reintroduce several frames of a box visibly "sinking in" before all 4 corners register, with no compensating benefit. Does not change `sphere_vs_plane`/`sphere_vs_box`/`sphere_vs_sphere`'s behavior — all confirmed already exact or an already-correct favorable divergence, nothing to change. Does not change `box_vs_plane` to match Bullet's real single-contact-plus-persistence behavior (finding 3; this port's own instantaneous exact computation is deliberately kept). Does not touch `RB-PHYSICS-001-FR-005`'s real-data calibration, no longer blocked on `PHASE-0-EXIT` (now closed), but not itself started | `crates/rb_physics_bullet/src/collision.rs` (doc comments only — no production code changed), reference source checked: Bullet's real `btConvexPlaneCollisionAlgorithm.cpp`/`.h`, `btSphereBoxCollisionAlgorithm.cpp`, `btSphereSphereCollisionAlgorithm.cpp`, `btManifoldPoint.h` (fetched and read, not part of this repo), `docs/specifications/physics/RB-PHYSICS-001-physics-core-port.md` | 1 new `collision.rs` test — `sphere_embedded_at_an_axis_tie_prefers_the_lower_axis_like_bullets_own_face_check_order` — embeds a sphere in a box at a position chosen so the box's own `-x` and `+y` faces are exactly tied at margin 3.0, and asserts the resulting contact picks `-x` (the face Bullet's own fixed `+x, -x, +y, -y, +z, -z` check order would settle on), not `+y`. All 277 of `rb_physics_bullet`'s pre-existing tests (as of `FR-046`) pass unchanged (net +1 test over `FR-046`'s 277, bringing the crate to 278) | collision-reference-validation commit | Investigated, Verified (documentation/research-only; zero production code changed, only doc comments and 1 new dedicated test; `sphere_vs_plane`/`sphere_vs_sphere` confirmed byte-for-byte exact, `sphere_vs_box`'s deep-penetration face-tie-break order confirmed to reproduce Bullet's own real order; a genuine, deliberate divergence found in `box_vs_plane` (instantaneous exact 4-corner computation vs. Bullet's real single-support-vertex-plus-persistence default) confirmed favorable, in the same spirit as `FR-042`'s own `box_vs_box` finding, and not adopted; does not close `FR-005`; not yet scored against real data) |
| `RB-PHYSICS-001-FR-048` (`solver.rs` constraint-row setup/resolve reference validation) | `PHASE-1-PHYSICS-CORE` | `solver.rs`'s constraint-row setup/resolve functions (`restitution_curve`, `plane_space`, `setup_rows`, `resolve_row`) and `btContactSolverInfo`'s own cited default constants had never been checked against Bullet's own source, extending the same reference-validation series `RB-PHYSICS-001-FR-036`/`FR-042`/`FR-043`/`FR-045`/`FR-046`/`FR-047` already ran against other files. Fetched and read Bullet's real `btSequentialImpulseConstraintSolver.cpp`/`.h`, `btContactSolverInfo.h`, and `btVector3.h` reference source directly, matching that same method, and checked every specific claim against it. (1) `plane_space` confirmed byte-for-byte exact against real `btPlaneSpace1`. (2) `restitution_curve` confirmed behaviorally exact: real Bullet's `restitutionCurve` can return a raw negative value, but its one caller (`setupContactConstraint`) immediately clamps a non-positive result to exactly `0` before it's used; this port's own `restitution_curve` folds that call-site clamp inline via `.max(0.0)` instead of as a separate step, so the final value used is identical either way — a confirmed-equivalent restructuring, not a divergence. (3) `setup_rows` confirmed exact against real `setupContactConstraint`/`setupFrictionConstraint` — also corrected a stale doc-comment citation: the doc previously cited a differently-named, unrelated function `setFrictionConstraintImpulse`, which in real Bullet only resets a cached impulse to zero; the actual friction-row-setup function is `setupFrictionConstraint`, reached via `addFrictionConstraint`'s own default `desiredVelocity = 0`/`cfmSlip = 0` arguments. (4) `resolve_row` confirmed a behaviorally-equivalent unification: this port uses one function (checking both lower and upper bounds) for every row, where real Bullet dispatches to two different functions (`resolveSingleConstraintRowLowerLimit`, lower-bound-only, for the normal row; `resolveSingleConstraintRowGeneric`, both bounds, for friction rows); confirmed this changes nothing since the normal row's own `upper_limit` (`1e10`, matching real Bullet's own value exactly) is astronomically larger than any real impulse, making the extra upper check unreachable there. (5) All 6 of `btContactSolverInfo`'s cited default constants (`ERP2 = 0.2`, `GLOBAL_CFM = 0.0`, `LINEAR_SLOP = 0.0`, `RESTITUTION_VELOCITY_THRESHOLD = 0.2`, `RELAXATION = 1.0`, `SOLVER_ITERATIONS = 10`) confirmed exact against real `btContactSolverInfoData`'s own constructor. (6) A genuine, significant divergence was found in `setup_rows` and not adopted: this port always derives both friction directions from a fixed, velocity-independent `plane_space(&contact.normal)` basis, while real Bullet's actual default (in `convertContact`) instead derives friction direction 1 from the tangential component of the current relative sliding velocity itself, falling back to `btPlaneSpace1`'s own fixed basis only when that tangential velocity is negligible — physically meaningful, not cosmetic, since each friction row is independently clamped to `[-mu*N, +mu*N]`, so two fixed orthogonal rows approximate the true circular friction cone with a square in tangent space, letting up to `sqrt(2)` times the correct friction magnitude be achieved at the square's diagonal for a body sliding along neither fixed axis; deliberately left as open follow-up work for a dedicated future FR, the same scoping this port already used for `FR-030`/`FR-034`/`FR-035`/`FR-037`, rather than folded into this reference-validation pass. Does not change `plane_space`/`restitution_curve`/`setup_rows`/`resolve_row`'s behavior — all confirmed already exact or already-correct-equivalent, nothing to change. Does not adopt the velocity-aligned friction-direction fix (finding 6; left open for a dedicated future FR). Does not touch `RB-PHYSICS-001-FR-005`'s real-data calibration, no longer blocked on `PHASE-0-EXIT` (now closed), but not itself started | `crates/rb_physics_bullet/src/solver.rs` (doc comments only — no production code changed), reference source checked: Bullet's real `btSequentialImpulseConstraintSolver.cpp`/`.h`, `btContactSolverInfo.h`, `btVector3.h` (fetched and read, not part of this repo), `docs/specifications/physics/RB-PHYSICS-001-physics-core-port.md` | 1 new `solver.rs` test — `restitution_curve_clamps_a_fast_separating_relative_velocity_to_zero` — calls `restitution_curve` with a fast separating relative velocity (well past the restitution-velocity threshold) that would make real Bullet's own unclamped `restitutionCurve` return a negative value, and asserts this port's own function returns exactly `0.0`, pinning the call-site-clamp-folded-in equivalence. All 278 of `rb_physics_bullet`'s pre-existing tests (as of `FR-047`) pass unchanged (net +1 test over `FR-047`'s 278, bringing the crate to 279) | solver-reference-validation commit | Investigated, Verified (documentation/research-only; zero production code changed, only doc comments and 1 new dedicated test; `plane_space` confirmed byte-for-byte exact, `restitution_curve` confirmed behaviorally exact via a confirmed-equivalent call-site-clamp restructuring, `setup_rows` confirmed exact (plus one stale doc-comment citation corrected), `resolve_row` confirmed a behaviorally-equivalent unification, and all 6 of `btContactSolverInfo`'s cited default constants confirmed exact; a genuine, significant divergence found in `setup_rows`'s fixed friction-direction basis (vs. Bullet's real velocity-aligned default) and deliberately not adopted, left as open follow-up work for a dedicated future FR; does not close `FR-005`; not yet scored against real data) |
| `RB-PHYSICS-001-FR-049` (velocity-aligned friction-direction fix) | `PHASE-1-PHYSICS-CORE` | `RB-PHYSICS-001-FR-048`'s reference-validation pass found one genuine, significant divergence and left it open: `solver.rs`'s `setup_rows`/`setup_two_body_rows` always derived both friction directions from a fixed, velocity-independent `plane_space(&contact.normal)` basis, while real Bullet's actual default instead derives friction direction 1 from the tangential component of the current relative sliding velocity itself, falling back to the fixed basis only when that tangential velocity is negligible. This requirement closes that divergence: a new `solver::friction_directions(normal, relative_velocity)` helper computes the tangential component of `relative_velocity` (subtracting out its component along `normal`), normalizes it to get friction direction 1, and completes a right-handed orthonormal basis for direction 2 via `dir1.cross(normal)` — falling back to the old `plane_space` fixed basis in two cases: (1) negligible tangential velocity, matching real Bullet's own `SIMD_EPSILON` threshold, and (2) a second, genuinely new case found while implementing this fix — a near-head-on-collision catastrophic floating-point cancellation that can produce a degenerate tangential vector; real Bullet's own unguarded `normalize()` would silently mishandle this, but this crate's own `Option`-returning `Vec3::normalize()` instead falls back gracefully. Wired into both `setup_rows` (one-body contacts) and `setup_two_body_rows` (two-body contacts), with no other call-site changes | `crates/rb_physics_bullet/src/solver.rs` (only file touched in the crate) | `rb_physics_bullet` unit tests (3 new: `friction_directions` aligns with the tangential component of relative sliding velocity, falls back to the fixed `plane_space` basis for negligible tangential velocity, and a dedicated isotropic-friction regression test — asserting friction now resists sliding along the true direction of motion rather than a fixed axis-aligned approximation — independently verified to fail under the old fixed-basis behavior, proving the fix has real behavioral bite). All 279 of `rb_physics_bullet`'s pre-existing tests (as of `FR-048`) pass unchanged (net +3 tests over `FR-048`'s 279, bringing the crate to 282) | friction-direction-fix commit | Implemented, Verified (unit tests only; closes `RB-PHYSICS-001-FR-048`'s own left-open friction-direction divergence, matching real Bullet's own default sliding-velocity-aligned friction basis, falling back to the fixed basis for negligible tangential velocity or a newly-found degenerate-cancellation case; does not close `FR-005`; not yet scored against real data) |
| `RB-PHYSICS-001-FR-050` (net-point contact combined multi-body solve) | `PHASE-1-PHYSICS-CORE` | `RB-PHYSICS-001-FR-030`'s own combined multi-body solve (`solver::resolve_dynamic_manifolds`) closed an order-dependent under-convergence bug for ball-vs-car/car-vs-car contacts, but `net::NetMesh::step` still resolved every body-vs-net-point contact independently and sequentially via `solver::resolve_contacts_between`, and `net.rs`'s own doc comment waved this off as safe since a net point's own mass is "tiny enough" relative to a real ball or car — an untested claim. This requirement checks that claim and finds it false: `NET_POINT_MASS` (`0.5`) is exactly half a typical ball's own mass (`1.0`), not a lopsided ratio, and a ball or car pressing into the net commonly overlaps two or more free net points at once given `NET_POINT_RADIUS`'s generous coverage-radius sizing. A dedicated single-shot test confirms the underlying mechanism is genuinely order-dependent, not merely slow to converge: for a ball placed exactly symmetrically between two net-point-like bodies, resolving each point fully independently in one order leaves the ball with a nonzero sideways velocity, and the opposite order leaves the mirror-image velocity — a purely arbitrary artifact of iteration order with no physical basis. A `NetMesh::step`-level test measures the real-world size of the bias directly: a ball fired squarely at the net's own center, straddling two symmetric free interior points, is measurably deflected sideways by ~0.25 units/s out of a 2000 units/s impact under the old sequential loop. Fixed by adopting `solver::resolve_dynamic_manifolds`'s combined multi-body solve for every body-vs-point contact detected within a sub-step (bodies and free net points combined into one temporary array for the call, since `RigidBody` is `Copy`), instead of resolving each pair immediately and independently — measured directly to reduce the squarely-centered-impact residual from ~0.25 units/s to ~0.016 units/s, roughly a 15-fold improvement. Warm-starting was deliberately NOT added as part of this fix — `NetMesh::step` still passes a fresh, empty `ContactCache` every sub-step, cold-starting every call exactly as before — left as the same kind of open follow-up work `RB-PHYSICS-001-FR-035` already scoped out for `resolve_contacts`/`resolve_contacts_between` generally | `crates/rb_physics_bullet/src/net.rs` (only file touched in the crate) | `rb_physics_bullet` unit tests (2 new: a single-shot test confirming the independent-pairwise resolution order is genuinely order-dependent for a ball symmetrically pinched between two net-point-like bodies, and a `NetMesh::step`-level regression test measuring the combined-solve fix reduces a squarely-centered net impact's sideways-deflection residual from ~0.25 to ~0.016 units/s). All 282 of `rb_physics_bullet`'s pre-existing tests (as of `FR-049`) pass unchanged (net +2 tests over `FR-049`'s 282, bringing the crate to 284) | net-combined-solve-fix commit | Implemented, Verified (unit tests only; closes the order-dependent under-convergence gap `RB-PHYSICS-001-FR-030` left open in `net::NetMesh::step`'s own independent-pairwise contact resolution, adopting the same `solver::resolve_dynamic_manifolds` combined multi-body solve; warm-starting deliberately not added, left open per `RB-PHYSICS-001-FR-035`'s own scoping; does not close `FR-005`; not yet scored against real data) |
| `RB-PHYSICS-001-FR-051` (static-shape contact combined multi-body solve) | `PHASE-1-PHYSICS-CORE` | `RB-PHYSICS-001-FR-030`/`FR-050`'s own combined multi-body solve (`solver::resolve_dynamic_manifolds`) closed an order-dependent under-convergence bug for dynamic-vs-dynamic and net-point contacts, but `PhysicsWorld::step` still resolved a body's contact against each different static shape type (the ground, then every wall, then every curve, then every corner fillet, then every goal wall, then every bounded wall) via one independent `solver::resolve_contacts` call per shape, fully resolved and applied before the next shape's setup even reads the body's velocity. This requirement checks the same bug against that case — a body touching two different static surfaces at once (a car driving near a wall close to the ground, or wedged into a corner, a routine gameplay occurrence, not an edge case) — and finds it present. A dedicated single-shot test confirms the underlying mechanism is genuinely order-dependent, not merely slow to converge: a ball wedged symmetrically into a corner formed by two static walls (perpendicular normals, identical restitution/friction), moving diagonally into both at once, is left biased toward whichever wall is resolved last under one order (mirror-image results under the opposite order) instead of the true, by-symmetry equal-velocity-component answer. Fixed by a new `solver::resolve_static_manifolds`, generalizing `resolve_contacts` to combine every static-shape manifold a body touches into one shared solve (each manifold group still computes its own combined restitution/friction against the body's own material properties, but all groups now share one DeltaVelocity/push-delta accumulator across the whole `SOLVER_ITERATIONS` loop), and a new `StaticScene` struct (bundling the six static-shape slices into one parameter to stay under clippy's `too_many_arguments` limit) plus a new `resolve_static_contacts` function wired into `PhysicsWorld::step`, gathering every one of a body's contacts across every static shape into one manifold list and resolving them all together — replacing the old five separate per-shape-type helper functions (`resolve_plane_contact`, `resolve_curve_contact`, `resolve_corner_fillet_contact`, `resolve_goal_wall_contact`, `resolve_bounded_wall_contact`, all removed) and their sequential per-shape call sequence in `step`, for both the ball and every car | `crates/rb_physics_bullet/src/solver.rs`, `crates/rb_physics_bullet/src/world.rs` (only files touched in the crate) | `rb_physics_bullet` unit tests (2 new: a single-shot test confirming the independent-pairwise resolution order is genuinely order-dependent for a ball wedged symmetrically into a two-static-wall corner, and a `PhysicsWorld::step`-level regression test — a ball fired diagonally into a real two-wall corner via an actual `PhysicsWorld` with two `with_wall` calls — verified to fail under the old sequential per-shape loop before the rewire). All 284 of `rb_physics_bullet`'s pre-existing tests (as of `FR-050`) pass unchanged (net +2 tests over `FR-050`'s 284, bringing the crate to 286) | static-combined-solve-fix commit | Implemented, Verified (unit tests only; closes the order-dependent under-convergence gap `RB-PHYSICS-001-FR-030`/`FR-050` left open for static-shape contacts in `PhysicsWorld::step`'s own independent per-shape-type contact resolution, adopting the same `solver::resolve_dynamic_manifolds`-style combined multi-body solve via a new `solver::resolve_static_manifolds`; does not close `FR-005`; not yet scored against real data) |
| `RB-PHYSICS-001-FR-052` (static+dynamic manifold combined multi-body solve) | `PHASE-1-PHYSICS-CORE` | `RB-PHYSICS-001-FR-051`'s own new `solver::resolve_static_manifolds` closed order-dependent under-convergence for a body's own static-shape contacts, and `RB-PHYSICS-001-FR-030`'s own `solver::resolve_dynamic_manifolds` separately closed it for a body's own dynamic-vs-dynamic contacts, but `PhysicsWorld::step` still ran those two combined solves as two separate sequential calls — a body's static contacts fully resolved and applied via `resolve_static_manifolds` before `resolve_dynamic_manifolds`'s own setup for that same body (e.g. a car also touching another car) ever read the result. This requirement checks whether that remaining boundary between the two existing combined solves is itself the same independent-pairwise-resolution bug `FR-030`/`FR-050`/`FR-051` already found elsewhere, and finds it present. A dedicated single-shot test reuses `FR-051`'s own symmetric two-wall corner setup (a ball wedged into a corner formed by two perpendicular static walls with identical restitution/friction, moving diagonally into both), but replaces one wall with a very-heavy dynamic body (mass = 1e9) positioned so its own contact against the ball is geometrically identical to that wall's — as immovable as a real wall for practical purposes, but routed through the dynamic-manifold code path instead of the static one; resolving the static wall fully first, then the dynamic body (`step`'s own pre-fix order), leaves the ball biased toward whichever channel is resolved last, the reversed order gives the exact mirror image, and neither matches the true symmetric answer. Fixed by a new `solver::resolve_manifolds` folding a step's static manifolds and dynamic manifolds into one shared solve — every body's own static rows and dynamic-manifold rows now share one DeltaVelocity/push-delta accumulator for the whole `SOLVER_ITERATIONS` loop; `RB-PHYSICS-001-FR-041`'s own `1/k` relaxation deliberately keeps counting `k` from dynamic manifolds only — extending that relaxation to a body's static rows was tried and found to regress `FR-051`'s own two-static-wall test's convergence, so it was not adopted, a body's static rows are never relaxed, matching `resolve_static_manifolds`'s own existing unscaled behavior exactly. `PhysicsWorld::step` was rewired: the old `resolve_static_contacts` function was renamed to `static_contact_manifolds` and changed to return a body's gathered `(restitution, friction, contacts)` groups instead of resolving them directly; `step` now builds the `bodies` array first, gathers every body's static manifolds and every ball-vs-car/car-vs-car dynamic manifold, then makes one `solver::resolve_manifolds` call instead of two separate ones | `crates/rb_physics_bullet/src/solver.rs`, `crates/rb_physics_bullet/src/world.rs` (only files touched in the crate) | `rb_physics_bullet` unit tests (2 new: `resolving_a_bodys_static_and_dynamic_contact_together_avoids_the_order_dependent_bias_sequential_resolution_has` in `solver.rs`, and `a_ball_wedged_between_a_wall_and_a_heavy_car_settles_symmetrically_instead_of_favoring_one` in `world.rs` — a ball fired diagonally into a real wall-and-heavy-car corner, one real `StaticPlane` wall via `with_wall` and one real heavy-mass car via `with_car`, settling with nearly equal x/y velocity components after one real `step` call, confirmed to fail under the old two-call sequential form before the rewire). All 286 of `rb_physics_bullet`'s pre-existing tests (as of `FR-051`) pass unchanged (net +2 tests over `FR-051`'s 286, bringing the crate to 288) | static-dynamic-combined-solve-fix commit | Implemented, Verified (unit tests only; closes the order-dependent under-convergence gap left between `RB-PHYSICS-001-FR-030`/`FR-051`'s own two already-combined solves, folding a step's static and dynamic manifolds into one shared solve via a new `solver::resolve_manifolds`; does not close `FR-005`; not yet scored against real data) |
| `RB-PHYSICS-001-FR-053` (friction combine-mode clamp reference conformance) | `PHASE-1-PHYSICS-CORE` | `RB-PHYSICS-001-FR-043`'s own investigation fetched and read Bullet's real `btManifoldResult::calculateCombinedFriction`/`calculateCombinedRestitution` reference source to correct a wrong claim about the reference's default combine mode (an unclamped product, not `btMax`), but never separately examined one more detail visible in that same fetched source: real Bullet's own `calculateCombinedFriction` additionally clamps its product result to `[-10.0, 10.0]` (a plain `if` clamp, not `btClamped`) — `calculateCombinedRestitution` has no such clamp at all. This requirement re-fetched and re-read `btManifoldResult.cpp` directly (matching `FR-036`/`FR-042`/`FR-043`/`FR-045`/`FR-046`/`FR-047`/`FR-048`'s own method) to confirm that clamp's exact mechanics, then checked it against every friction coefficient this crate itself ever actually sets — every `RigidBody`/`StaticPlane`/`StaticQuarterPipe`/`StaticCornerFillet`/`StaticGoalWall`/`StaticBoundedWall` this crate constructs uses a value in `0.1..=0.9` (uncalibrated placeholders and constants like `net::NET_FRICTION`/`drive::HANDBRAKE_FRICTION_MULTIPLIER`-scaled values), nowhere near either bound, so the clamp is currently inert everywhere in this crate today. Adopted anyway for reference conformance, not because any live bug demanded it: every one of those types' own `friction` field is a public, unvalidated `f32` with zero range enforcement anywhere in the crate, so a future caller or bug could set an extreme or negative value with no defense today, unlike real Bullet. `solver::combine_friction`'s own average FORMULA `RB-PHYSICS-001-FR-043` already decided to keep is unchanged — this is purely an added clamp on that average's result, not a formula change; `solver::combine_restitution` is left unclamped, matching the reference's own choice not to clamp restitution. Does not calibrate `RB-PHYSICS-001-FR-005`'s real-data question of which combine mode (if either) matches real Rocket League, no longer blocked on `PHASE-0-EXIT` (now closed), but not itself started. Does not touch any other Bullet-reference claim elsewhere in this spec | `crates/rb_physics_bullet/src/solver.rs` (`combine_friction`'s own function body and doc comment only — no other production file changed anywhere in this requirement) | 1 new `solver.rs` test — `combine_friction_clamps_to_the_same_bound_real_bullet_uses`, asserting `combine_friction(15.0, 15.0) == 10.0`, `combine_friction(-15.0, -15.0) == -10.0`, and an in-range pair still returns the plain average. All 288 of `rb_physics_bullet`'s pre-existing tests (as of `FR-052`) pass unchanged (net +1 test over `FR-052`'s 288, bringing the crate to 289) | friction-combine-clamp-reference-conformance commit | Implemented, Verified (unit tests only; investigated and confirmed Bullet's real friction-combine clamp mechanics directly from `btManifoldResult.cpp`, matching `FR-043`'s own investigated-and-verified framing; the clamp is currently inert for every value this crate itself sets and is adopted for reference conformance against an unvalidated `f32` field rather than a live defect; does not close `FR-005`; not yet scored against real data) |
| `RB-PHYSICS-001-FR-054` (goal-wall/bounded-wall corner-testing overlap investigation) | `PHASE-1-PHYSICS-CORE` | `RB-PHYSICS-001-FR-028`'s own doc comment had left open whether `collision::box_vs_goal_wall`'s per-corner window test could under-detect a car's face resting flush against the goal window's own edge — every corner just clear of the window while the face's middle already overlapped it — the same category of concern `RB-PHYSICS-001-FR-032` investigated and resolved for a curved fillet, but explicitly not covered by that finding, since a goal window's boundary is a flat rectangle, not a curve. This requirement resolves it via a convex-hull argument distinct from `FR-032`'s own: a box's touching face is the convex hull of whichever corners penetrate the plane, so "every corner outside the convex window" is exactly equivalent to "the face doesn't fully fit through it" — the correct block condition — confirming no bug exists, matching `FR-032`'s own "further investigation found the suspected gap doesn't exist" precedent via a distinct argument. The same investigation also examined the structurally-identical `collision::box_vs_bounded_wall` (`RB-PHYSICS-001-FR-029`) and found its mirror-image case genuinely under-detects: a face larger than a bound and centered on it reports zero contacts despite genuinely resting on real bound material. Confirmed unreachable given this project's own car (60x30x18 half-extents) and ball (93.15 radius) dimensions against the standard arena's own `StaticBoundedWall` bound sizes (hundreds of units on their shortest side) — documented as a new Non-goals item in the spec rather than fixed. No production behavior changed: `box_vs_goal_wall`'s algorithm is unchanged, only proven correct; `box_vs_bounded_wall`'s gap is documented, not fixed | `crates/rb_physics_bullet/src/collision.rs` (two doc comments extended only — no other production file changed anywhere in this requirement) | 2 new `collision.rs` tests — `box_bigger_than_the_goal_window_and_centered_on_it_collides_on_all_four_corners_matching_an_unwindowed_plane` and `box_much_bigger_than_the_bound_and_centered_on_it_is_missed_entirely_a_known_gap`. All 289 of `rb_physics_bullet`'s pre-existing tests (as of `FR-053`) pass unchanged (net +2 tests over `FR-053`'s 289, bringing the crate to 291) | goal-wall-bounded-wall-corner-overlap-investigation commit | Implemented, Verified (unit tests only; resolves `FR-028`'s own open `box_vs_goal_wall` corner-testing question via a convex-hull argument, finding no bug; separately finds `box_vs_bounded_wall`'s mirror-image case a genuine but currently unreachable under-detection gap, documented as a new Non-goals item rather than fixed; does not close `FR-005`; not yet scored against real data) |
| `RB-PHYSICS-001-FR-055` (goal-geometry constant confirmation & stale open-question correction) | `PHASE-1-PHYSICS-CORE` | `RB-PHYSICS-001-FR-024`'s own introduction of `arena::GOAL_HALF_WIDTH` (`892.755`) and `arena::GOAL_HEIGHT` (`642.775`) carried a "commonly-cited community number, not independently confirmed" caveat that was never revisited. This requirement fetched and read the current RLBot wiki's "Useful Game Values" page directly (https://github.com/RLBot/RLBot/wiki/Useful-Game-Values — the same page `RB-PHYSICS-001-FR-036`'s own earlier research already used to confirm `arena::GOAL_DEPTH`) and confirmed both values exact against its own cited "Goal center-to-post: 892.755" / "Goal height: z=642.775" numbers — no value changed, a sourcing-status upgrade only from "commonly-cited, unconfirmed" to "confirmed", the same non-behavioral outcome `FR-036` already reached for `GOAL_DEPTH`/`CORNER_LENGTH`. The same investigation also found this spec's own "Open questions" section still describing `arena::GOAL_DEPTH` as an unconfirmed "uncalibrated invention" — directly contradicting `RB-PHYSICS-001-FR-036`'s own already-shipped Requirements entry and this spec's own Non-goals section, both of which already state `GOAL_DEPTH` is confirmed, simply never updated when `FR-036` shipped. Corrected (with a strikethrough over the superseded text) to state all three goal-geometry constants (`GOAL_HALF_WIDTH`, `GOAL_HEIGHT`, `GOAL_DEPTH`) are confirmed, leaving only `arena::NET_DEPTH` (`RB-PHYSICS-001-FR-033`, a distinct, still genuinely uncalibrated quantity — how far into the confirmed goal depth the net panel itself sits) as an open invention in that vicinity. No production behavior changed anywhere — only doc comments on `GOAL_HALF_WIDTH`, `GOAL_HEIGHT`, and `NET_DEPTH` in `arena.rs` (not `GOAL_DEPTH` itself, whose doc comment was already correct from `FR-036`), matching `FR-031`/`FR-036`'s own established "no new test needed for a pure constant/doc-correctness change" precedent | `crates/rb_physics_bullet/src/arena.rs` (doc comments only, on `GOAL_HALF_WIDTH`, `GOAL_HEIGHT`, and `NET_DEPTH` — no other source file changed anywhere in this requirement), `docs/specifications/physics/RB-PHYSICS-001-physics-core-port.md` (Open questions section corrected with a strikethrough-and-close note, Requirements entry added, header status paragraph and Change History updated; version bumped to 0.55.0) | No new tests: this is a pure constant-sourcing/doc-correctness change, matching `RB-PHYSICS-001-FR-031`/`FR-036`'s own established precedent that no new test is needed for such a change (also matching `FR-044`'s own precedent for a documentation-only correction). `cargo test --workspace` re-run clean. All 291 of `rb_physics_bullet`'s pre-existing tests (as of `FR-054`) pass unchanged (total unchanged from `FR-054`, still 291) | goal-geometry-constant-confirmation commit | Investigated, Verified (documentation/constant-sourcing-only; zero production behavior changed, no new tests; `arena::GOAL_HALF_WIDTH`/`arena::GOAL_HEIGHT` upgraded from "commonly-cited, unconfirmed" to confirmed against the current RLBot wiki, the same source `FR-036` already used for `arena::GOAL_DEPTH`; this spec's own stale "Open questions" `GOAL_DEPTH`-uncalibrated claim — contradicted by `FR-036`'s own already-shipped entry — corrected to a strikethrough-and-close note, matching the same convention `FR-044` already established; does not close `FR-005`; not yet scored against real data) |
| `RB-PHYSICS-001-FR-056` (boost acceleration ground/air split) | `PHASE-1-PHYSICS-CORE` | `drive::BOOST_ACCELERATION` was a single flat constant (`991.667`) applied identically by `apply_driven_forces` whether its own `on_ground` parameter was true or false. This port's own doc comments — including `RB-PHYSICS-001-FR-008`'s own Requirements entry — explicitly claimed boost "works identically airborne". This requirement fetched RocketSim's own `RLConst.h` directly (https://raw.githubusercontent.com/ZealanL/RocketSim/main/src/RLConst.h, matching `RB-PHYSICS-001-FR-036`/`FR-042`/`FR-043`/`FR-045`/`FR-046`/`FR-047`/`FR-048`/`FR-053`'s own method) and found that claim itself wrong: the reference defines two distinct constants, `BOOST_ACCEL_GROUND = 2975.f / 3.f` (≈991.667, exactly matching this port's existing flat value) and `BOOST_ACCEL_AIR = 3175.f / 3.f` (≈1058.333, about 6.5% higher) — a genuine ground/air split this port's single-constant model didn't capture, meaning every airborne boost this crate ever applied understated real Rocket League's own airborne boost strength. Fixed by splitting `BOOST_ACCELERATION` into `BOOST_ACCELERATION_GROUND` (unchanged value, rewritten as the exact `2975.0 / 3.0` fraction) and `BOOST_ACCELERATION_AIR` (`3175.0 / 3.0`, new), and wiring `apply_driven_forces`'s existing `on_ground` parameter to select between them (no new gating logic — boost already applied in both cases, only the magnitude changed). Also corrected every doc comment (this module's own doc comment, `RB-PHYSICS-001-FR-008`'s own Requirements entry, and this spec's own Non-goals/Open-questions mentions) that had claimed the two were identical. As a byproduct of the same fetch (not a new finding requiring its own fix), confirmed `BOOST_CONSUMPTION_RATE`/`MAX_BOOST` already match RocketSim's own `BOOST_USED_PER_SECOND = BOOST_MAX / 3` | `crates/rb_physics_bullet/src/drive.rs` (only file touched in the crate — constant split, one call-site change in `apply_driven_forces`, multiple doc comments corrected) | `rb_physics_bullet` unit tests (1 new: `boost_accelerates_an_airborne_car_faster_than_a_grounded_one`, confirming the exact ratio between grounded and airborne boost acceleration matches RocketSim's own ratio). All 291 of `rb_physics_bullet`'s pre-existing tests (as of `FR-055`) pass unchanged (net +1 test over `FR-055`'s 291, bringing the crate to 292) | boost-acceleration-ground-air-split commit | Implemented, Verified (unit tests only; genuine behavioral fix — airborne boost acceleration corrected from the erroneous shared `991.667` to the correct `1058.333` (`BOOST_ACCELERATION_AIR`), matching RocketSim's own `BOOST_ACCEL_AIR`; ground boost acceleration unchanged; corrects this port's own doc-comment claim — including `RB-PHYSICS-001-FR-008`'s own Requirements entry — that boost "works identically airborne"; does not close `FR-005`; not yet scored against real data) |
| `RB-PHYSICS-001-FR-057` (hard cap on car angular speed) | `PHASE-1-PHYSICS-CORE` | Nothing previously bounded how fast sustained air control torque (or a dodge's own kick, or the landing-orientation assist) could spin a car — holding full pitch/yaw/roll indefinitely spun it arbitrarily fast, unlike real Rocket League. This requirement fetched RocketSim's own `RLConst.h` a second time (https://raw.githubusercontent.com/ZealanL/RocketSim/main/src/RLConst.h, matching `RB-PHYSICS-001-FR-056`'s own method), this time targeting every `drive.rs` constant this crate's own module doc comment already listed as having "no public reference at all" (`STEER_TORQUE`, `HANDBRAKE_FRICTION_MULTIPLIER`, `AIR_CONTROL_TORQUE`, `WALL_JUMP_HORIZONTAL_SPEED`, `DODGE_SPEED`, `DODGE_ANGULAR_SPEED`, `JUMP_HOLD_MAX_DURATION`, `JUMP_HOLD_ACCELERATION`, `LANDING_AUTO_UPRIGHT_TORQUE`). The fetch surfaced `CAR_MAX_ANG_SPEED = 5.5f` (rad/s) — a hard "car can never exceed this angular velocity" ceiling this port had never modeled. Unlike the other constants the same fetch surfaced (dodge per-direction impulse scaling, auto-flip/landing-assistance thresholds, a ramping powerslide model, a steering-torque mapping, RocketSim's own per-axis `CAR_AIR_CONTROL_TORQUE`), which bound a *torque* and so repeat `RB-PHYSICS-001-FR-031`'s own "false precision" finding against this port's differently-calibrated car body/inertia tensor, `CAR_MAX_ANG_SPEED` bounds the *result* (angular velocity) independent of whatever torque/inertia combination produced it, so it transfers cleanly and was adopted — the other candidates were not, left as open items for a future requirement. Fixed by adding `drive::MAX_CAR_ANGULAR_SPEED = 5.5` and a new `drive::clamp_angular_speed`, which scales `RigidBody.angular_velocity` back down to the cap (preserving direction) when exceeded — a genuine clamp, unlike `MAX_CAR_SPEED`/`UNBOOSTED_MAX_CAR_SPEED`, which only ever gate new throttle/boost force and never reduce velocity already past their own cap — wired in once per step immediately after `integrate::integrate_velocities`. Also incidentally confirmed `drive::DODGE_ANGULAR_SPEED`'s pre-existing numeric equality to this same `5.5` value is a documented coincidence, not a second confirmation of it | `crates/rb_physics_bullet/src/drive.rs` (`MAX_CAR_ANGULAR_SPEED` constant, `clamp_angular_speed` function, and the test helper call site), `crates/rb_physics_bullet/src/world.rs` (`drive_and_integrate_velocities` call site — the two production/test call sites are the only places touched in the crate) | `rb_physics_bullet` unit tests (3 new: `clamp_angular_speed_is_a_no_op_below_the_cap` and `clamp_angular_speed_scales_an_over_cap_velocity_down_to_the_cap_preserving_direction` unit-test `clamp_angular_speed` directly; `sustained_full_roll_input_never_exceeds_the_hard_angular_speed_cap` holds full roll for 120 steps (2 simulated seconds at `1/60` `dt`) and asserts the resulting angular speed stays at or under the cap while actually reaching near it). All 292 of `rb_physics_bullet`'s pre-existing tests (as of `FR-056`) pass unchanged (net +3 tests over `FR-056`'s 292, bringing the crate to 295) | car-max-angular-speed-cap commit | Implemented, Verified (unit tests only; genuine behavioral fix — sustained full-axis air control input can no longer drive a car's angular speed past `MAX_CAR_ANGULAR_SPEED`, in production (`world.rs`) and in `drive.rs`'s own test helper alike; dodge per-direction impulse scaling, auto-flip/landing-assistance thresholds, the powerslide ramp model, the steering-torque mapping, and per-axis `AIR_CONTROL_TORQUE` splitting all explicitly not adopted, left open for a future requirement; does not close `FR-005`; not yet scored against real data) |
| `RB-PHYSICS-001-FR-058` (real speed-dependent throttle taper) | `PHASE-1-PHYSICS-CORE` | `drive::THROTTLE_ACCELERATION`'s own doc comment had named this exact gap since it was first introduced — "Rocket League's real throttle curve tapers off nonlinearly as speed rises toward `UNBOOSTED_MAX_CAR_SPEED`; this port uses one constant instead, a real simplification (not a taper)" — applying full flat acceleration right up to a hard cutoff at `UNBOOSTED_MAX_CAR_SPEED` instead of a genuine taper. This requirement fetched RocketSim's own `Car.cpp` directly (not just its `RLConst.h` constants, this time), matching `RB-PHYSICS-001-FR-036`/`FR-042`/`FR-043`/`FR-045`/`FR-046`/`FR-047`/`FR-048`/`FR-053`/`FR-056`/`FR-057`'s own fetch-and-read method, to find exactly how its own `THROTTLE_TORQUE_AMOUNT` is actually used, and surfaced the real mechanism: `driveEngineForce = engineThrottle * (THROTTLE_TORQUE_AMOUNT * UU_TO_BT) * driveSpeedScale`, where `driveSpeedScale` is `DRIVE_SPEED_TORQUE_FACTOR_CURVE.GetOutput(abs(forwardSpeed_UU))` — a genuine 3-point piecewise-linear curve (`{0, 1.0}, {1400, 0.1}, {1410, 0.0}`, confirmed exact against `RLConst.h`), not a flat value. `THROTTLE_TORQUE_AMOUNT` itself (`CAR_MASS_BT * 400.f`) is expressed in Bullet's own internal units and doesn't transfer to this port's own differently-calibrated car body the same clean way real acceleration constants like `BOOST_ACCEL_GROUND`/`AIR` do (repeating `RB-PHYSICS-001-FR-031`'s and `FR-057`'s own "false precision" finding) — but the taper curve itself is a pure, unitless ratio multiplying whatever peak acceleration applies at a standing start, so it transfers cleanly regardless of that mismatch, the same reasoning `FR-057` used to distinguish `MAX_CAR_ANGULAR_SPEED` (adoptable) from `AIR_CONTROL_TORQUE`'s own real per-axis split (not adoptable). Fixed by adding `drive::DRIVE_SPEED_TAPER_BREAKPOINTS` (the 3 confirmed breakpoints, the last reusing the pre-existing `UNBOOSTED_MAX_CAR_SPEED` constant rather than a second literal `1410.0`) and `drive::drive_speed_taper`, a small piecewise-linear interpolator evaluated against this port's own pre-existing signed `throttle.signum() * forward_speed` quantity (the same value `apply_driven_forces`'s throttle gate already computed), clamped to non-negative before lookup, full torque (`1.0`) below the curve's domain and zero beyond it; and rewiring `apply_driven_forces`'s throttle block to always compute `drive_speed_taper(...)` and scale `THROTTLE_ACCELERATION` by it instead of gating on `throttle.signum() * forward_speed < UNBOOSTED_MAX_CAR_SPEED` (the effective top speed throttle alone can reach is unchanged, still exactly `UNBOOSTED_MAX_CAR_SPEED`, the curve's own last breakpoint — only how acceleration approaches it changed). Also corrected the doc comments (`THROTTLE_ACCELERATION`'s own, and the module-level "commonly-cited constants" paragraph) that had described the curve itself, not just its magnitude, as unmodeled. Deliberately does not adopt RocketSim's own direction-agnostic `abs(forward speed)` curve input — this port's own pre-existing throttle gate was already direction-aware, and switching to direction-agnostic input is a second, independent behavioral change left out of scope; does not change `THROTTLE_ACCELERATION`'s own peak magnitude (`1600.0`), still an uncalibrated placeholder | `crates/rb_physics_bullet/src/drive.rs` (`DRIVE_SPEED_TAPER_BREAKPOINTS` constant, `drive_speed_taper` function, and the rewired throttle block in `apply_driven_forces`; only file touched in the crate) | `rb_physics_bullet` unit tests (2 new: `drive_speed_taper_matches_the_real_curve_breakpoints_exactly` unit-tests the interpolator directly at both breakpoints, both segment midpoints, and both out-of-domain clamps; `throttle_acceleration_tapers_well_before_reaching_unboosted_max_speed` confirms a car at exactly `1400` uu/s gains only ~10% of a full-strength step's velocity delta from one step of full throttle, the exact regression this requirement closes). All 295 of `rb_physics_bullet`'s pre-existing tests (as of `FR-057`) pass unchanged (net +2 tests over `FR-057`'s 295, bringing the crate to 297) | real-throttle-taper-curve commit | Implemented, Verified (unit tests only; genuine behavioral fix — throttle acceleration now measurably tapers before reaching `UNBOOSTED_MAX_CAR_SPEED` (10% strength at `1400` uu/s) instead of applying at full strength until a hard cutoff; the effective top speed throttle alone can reach is unchanged; RocketSim's own direction-agnostic curve input explicitly not adopted, left open as a separate question; does not close `FR-005`; not yet scored against real data) |
| `RB-PHYSICS-001-FR-059` (real forward-speed-dependent dodge impulse scaling) | `PHASE-1-PHYSICS-CORE` | `RB-PHYSICS-001-FR-031`'s own audit had already found that real Rocket League's dodge impulse has "direction/speed-dependent scaling" but didn't adopt it, since the audit only had `RLConst.h`'s constant declarations, not the formula they combine into — flagged as a candidate for "a later, more careful requirement" by `RB-PHYSICS-001-FR-057`'s own Non-goals. This requirement fetched RocketSim's own `Car.cpp` directly (`_UpdateDoubleJumpOrFlip`, the same file/technique `RB-PHYSICS-001-FR-058` used for the throttle taper), matching `RB-PHYSICS-001-FR-036`/`FR-042`/`FR-043`/`FR-045`/`FR-046`/`FR-047`/`FR-048`/`FR-053`/`FR-056`/`FR-057`/`FR-058`'s own fetch-and-read method, and surfaced the real mechanism: a dodge's base impulse (`FLIP_INITIAL_VEL_SCALE = 500.f`) is scaled per-axis by `((maxSpeedScale - 1) * forwardSpeedRatio) + 1`, where `forwardSpeedRatio = abs(forwardSpeed_UU) / CAR_MAX_SPEED` and `maxSpeedScale` is `FLIP_FORWARD_IMPULSE_MAX_SPEED_SCALE` (`1.f` — no change, ever), `FLIP_BACKWARD_IMPULSE_MAX_SPEED_SCALE` (`2.5f`, applied when the dodge direction opposes the car's own current velocity direction, per `shouldDodgeBackwards`), or `FLIP_SIDE_IMPULSE_MAX_SPEED_SCALE` (`1.9f`, applied to any side/roll dodge regardless of direction). Adopted the confirmed real *ratios* (`2.5`, `1.9`), not the real base magnitude (`500`) — since the real forward-dodge scale is exactly `1.0`, `DODGE_SPEED`'s own existing (still-uncalibrated) value doubles as the real forward-dodge case unchanged, the same "shape confirmed, magnitude not" split `RB-PHYSICS-001-FR-058` used for `THROTTLE_ACCELERATION`. Fixed by adding `drive::DODGE_BACKWARD_SPEED_SCALE = 2.5`, `drive::DODGE_SIDE_SPEED_SCALE = 1.9`, and `drive::DODGE_BACKWARD_CLASSIFICATION_SPEED_THRESHOLD = 100.0` (RocketSim's own `abs(forwardSpeed_UU) < 100.0f` fallback threshold), plus two new functions: `drive::dodge_speed_scale` (the confirmed linear interpolation from `1.0` to `scale_at_max_speed` as speed rises to `MAX_CAR_SPEED`, clamped beyond it) and `drive::dodge_pitch_is_backward` (the real backward classification, re-derived in this port's own pitch-sign convention rather than translated symbol-for-symbol from the reference's own stick-sign convention, with the `100.0` threshold as a low-speed fallback to stick-direction-only classification); and rewiring both `apply_driven_forces` dodge sites — the ground-dodge block and its wall-jump-dodge variant — to scale `DODGE_SPEED` by `dodge_speed_scale` before applying it, using `DODGE_BACKWARD_SPEED_SCALE` on the pitch axis when `dodge_pitch_is_backward` is true (`1.0` otherwise) and `DODGE_SIDE_SPEED_SCALE` on the roll axis always. Also corrected the doc comments (the module-level "commonly-cited constants" paragraph, and a new note on the module's own dodge description) that had described `DODGE_SPEED` as a flat magnitude regardless of direction or speed. Deliberately does not adopt RocketSim's own real base magnitude (`FLIP_INITIAL_VEL_SCALE = 500.f`) for `DODGE_SPEED` itself, still an independently uncalibrated placeholder; does not adopt RocketSim's own diagonal-dodge direction normalization (this port's own pre-existing, already-documented simplification where pitch and roll contribute independently rather than being normalized into one direction vector remains unchanged); does not adopt RocketSim's own continuous torque-over-`FLIP_TORQUE_TIME` spin model (`DODGE_ANGULAR_SPEED` remains a single instantaneous kick, a substantially larger redesign left for a future requirement); does not adopt real yaw input's contribution to dodge direction (this port's dodge direction remains pitch/roll only, matching its own pre-existing convention) | `crates/rb_physics_bullet/src/drive.rs` (`DODGE_BACKWARD_SPEED_SCALE`, `DODGE_SIDE_SPEED_SCALE`, and `DODGE_BACKWARD_CLASSIFICATION_SPEED_THRESHOLD` constants, `dodge_speed_scale` and `dodge_pitch_is_backward` functions, and the rewired ground-dodge and wall-jump-dodge blocks in `apply_driven_forces`; only file touched in the crate) | `rb_physics_bullet` unit tests (5 new: `dodge_speed_scale_matches_the_real_curve` and `dodge_pitch_is_backward_matches_the_real_classification` unit-test the two new functions directly; `a_backward_dodge_scales_up_with_current_forward_speed`, `a_forward_dodge_does_not_scale_with_current_forward_speed`, and `a_side_dodge_scales_up_with_current_forward_speed` are integration tests confirming the exact scaled magnitudes from a car already at `MAX_CAR_SPEED`). All 297 of `rb_physics_bullet`'s pre-existing tests (as of `FR-058`) pass unchanged (net +5 tests over `FR-058`'s 297, bringing the crate to 302) | dodge-speed-scaling commit | Implemented, Verified (unit tests only; genuine behavioral fix — a backward pitch-dodge or a side (roll) dodge made at `MAX_CAR_SPEED` now measurably scales up to `2.5x`/`1.9x` `DODGE_SPEED` respectively, instead of a flat `DODGE_SPEED` regardless of speed or direction; a forward pitch-dodge stays at plain `DODGE_SPEED` regardless of current speed, matching the real forward scale of exactly `1.0`; every pre-existing dodge test dodges from a standing start, where the new scale evaluates to `1.0` regardless of direction, a zero-regression-risk property confirmed by inspection before implementation, not merely by the suite passing afterward; does not close `FR-005`; not yet scored against real data) |
| `RB-PHYSICS-001-FR-060` (landing auto-orientation vs. real auto-flip/auto-roll — audit finding) | `PHASE-1-PHYSICS-CORE` | `RB-PHYSICS-001-FR-057`'s own Non-goals had flagged RocketSim's auto-flip constants (`CAR_AUTOFLIP_IMPULSE/TORQUE/TIME/NORMZ_THRESH/ROLL_THRESH`) as a possible reference for `drive::LANDING_AUTO_UPRIGHT_TORQUE`, but left open whether real auto-flip's conditional/threshold shape could map onto this port's own continuous-torque assist model "without further investigation." This requirement resolves that open investigation by fetching and reading RocketSim's actual `Car.cpp` source directly (matching `RB-PHYSICS-001-FR-058`/`FR-059`'s own method) and finds real Rocket League has no single mechanic matching this port's own "landing auto-orientation assistance" at all — it instead has two distinct, real, *grounded*, input-gated systems: auto-flip, a turtle-recovery flip firing only when the player presses jump while the car has ground contact on a roughly-horizontal surface (contact normal Z above `CAR_AUTOFLIP_NORMZ_THRESH`) and the car's current roll exceeds `CAR_AUTOFLIP_ROLL_THRESH`, applying a downward pinning impulse plus a forward-axis torque sustained over a timer (`CAR_AUTOFLIP_TIME * absRoll / PI`); and auto-roll, a continuous ground-alignment torque active only while throttle is held and the car has partial or full wheel contact — neither is airborne, and neither fires without a specific player input, both the opposite shape from this port's own continuous, input-free, airborne-only nudge — a "these don't map, confirmed" negative finding closing `FR-057`'s own open investigation rather than a value change. Corrected the `drive` module's doc comments (the "Landing auto-orientation assistance" section and the "commonly-cited constants" paragraph) to state this finding directly, replacing the prior speculation that real Rocket League "likely gates this on actual proximity to the ground"; also corrected the stale Open Questions bullet and `RB-PHYSICS-001-FR-057`'s own Non-goals bullet that had left this an open question. Does not implement either real auto-flip or real auto-roll — both need new grounded, input-gated state machinery this port doesn't have, left as a candidate for a future, dedicated requirement; does not change `LANDING_AUTO_UPRIGHT_TORQUE`'s value, trigger condition, or any other behavior — this is a pure audit/documentation finding, not a behavioral fix | `crates/rb_physics_bullet/src/drive.rs` (module doc comment only — "Landing auto-orientation assistance" section and the "commonly-cited constants" paragraph corrected; no production code changed), `docs/specifications/physics/RB-PHYSICS-001-physics-core-port.md` (Requirements entry added, Open Questions bullet and `FR-057`'s own Non-goals bullet corrected, Change History updated; version bumped to 0.60.0) | No new tests: this is a pure documentation-audit finding, matching `RB-PHYSICS-001-FR-044`/`FR-055`'s own established precedent that no new test is needed for such a change. All 302 of `rb_physics_bullet`'s pre-existing tests (as of `FR-059`) pass unchanged (total unchanged from `FR-059`, still 302) | landing-auto-orientation-vs-auto-flip-audit commit | Investigated, Verified (documentation-only; zero production behavior changed, no new tests; resolves `FR-057`'s own open "may not map... without further investigation" question with a concrete negative finding — real Rocket League's auto-flip/auto-roll are both grounded and input-gated, neither matches this port's own continuous, input-free, airborne-only assist; does not close `FR-005`; not yet scored against real data) |
| `RB-PHYSICS-001-FR-061` (hard caps on ball linear and angular speed) | `PHASE-1-PHYSICS-CORE` | Nothing previously bounded the ball's own linear or angular speed at all — `RigidBody.linear_damping`/`angular_damping` both default to `0.0` for the ball, and no other mechanism ever reduced its velocity, unlike the car, which has had a hard angular-speed cap (`drive::MAX_CAR_ANGULAR_SPEED`) since `RB-PHYSICS-001-FR-057`. This requirement fetched and read RocketSim's real `RLConst.h` and `Ball.cpp` source directly (matching `RB-PHYSICS-001-FR-036`/`FR-042`/`FR-043`/`FR-045`/`FR-046`/`FR-047`/`FR-048`/`FR-053`/`FR-056`/`FR-057`/`FR-058`/`FR-059`'s own fetch-and-read method) and found two confirmed real hard caps: `BALL_MAX_SPEED = 6000.f` (uu/s) and `BALL_MAX_ANG_SPEED = 6.f` (rad/s, "Ball can never exceed this angular velocity"), both enforced via a hard clamp (`if (vel.length2() > max*max) vel = vel.normalized() * max`) inside RocketSim's own `_FinishPhysicsTick()`, called after collision resolution, at the end of the physics tick. Both are pure velocity caps, not torque/force constants calibrated against a specific mass/inertia, so — like `drive::MAX_CAR_ANGULAR_SPEED` before it (`FR-057`'s own established category) — they transfer cleanly regardless of this port's own ball not being calibrated to real Rocket League's. Fixed by adding `world::BALL_MAX_SPEED = 6000.0` and `world::BALL_MAX_ANG_SPEED = 6.0`, plus a new private `world::clamp_ball_velocity(ball: &mut RigidBody)` that scales the ball's linear/angular velocity back down to each cap (preserving direction) if exceeded — generalizing `drive::clamp_angular_speed`'s own shape (car-specific) to both linear and angular speed, placed in `world.rs` rather than `drive.rs` since the ball has no drive-input-gated mechanic of its own — wired into `PhysicsWorld::step` right after that step's own contact resolution (right after `self.ball = bodies[0];`, before sleep-state evaluation and before transform integration), matching RocketSim's own "after collision resolution, end of tick" placement more precisely than `drive::clamp_angular_speed`'s own placement for the car (mid-pipeline, before that same step's own contact resolution). Explicitly not adopted: `BALL_DRAG = 0.03f` ("net-velocity drag multiplier") — RocketSim sets this once at ball construction as a per-match mutator-config default (`constructionInfo.m_linearDamping = mutatorConfig.ballDrag`), not a hardcoded system invariant like the two speed caps; this port's own `RigidBody::sphere` constructor (caller-constructed, not owned by `world.rs`) takes no opinion on a "real" ball's own damping default, and adopting `BALL_DRAG` would need a separate, deliberate design decision, left for a future requirement | `crates/rb_physics_bullet/src/world.rs` (only file touched in the crate — `BALL_MAX_SPEED`/`BALL_MAX_ANG_SPEED` constants, `clamp_ball_velocity` function, the `PhysicsWorld::step` wiring, and module/`step` doc-comment updates) | `rb_physics_bullet` unit tests (4 new: `clamp_ball_velocity_is_a_no_op_below_both_caps`, `clamp_ball_velocity_scales_an_over_cap_linear_velocity_down_to_the_cap_preserving_direction`, and `clamp_ball_velocity_scales_an_over_cap_angular_velocity_down_to_the_cap_preserving_direction` unit-test `clamp_ball_velocity` directly; `a_ball_launched_far_past_ball_max_speed_never_exceeds_it_after_a_step` is an integration test through the real `PhysicsWorld::step` public API). All 302 of `rb_physics_bullet`'s pre-existing tests (as of `FR-060`) pass unchanged (net +4 tests over `FR-060`'s 302, bringing the crate to 306) | ball-speed-caps commit | Implemented, Verified (unit tests; genuine behavioral fix — the ball's own linear speed can no longer exceed `BALL_MAX_SPEED` (6000 uu/s) nor its angular speed exceed `BALL_MAX_ANG_SPEED` (6 rad/s) after any step, where previously neither was bounded at all; `BALL_DRAG` explicitly not adopted, left open for a future requirement; does not close `FR-005`; not yet scored against real data) |
| `RB-PHYSICS-001-FR-062` (real ball material properties via a new `RigidBody::ball` constructor) | `PHASE-1-PHYSICS-CORE` | `RB-PHYSICS-001-FR-061`'s own Non-goals had explicitly deferred adopting RocketSim's `BALL_DRAG` constant, since this port's `RigidBody::sphere` constructor gives every caller (ball or otherwise) an identical generic `restitution = 0.5`/`friction = 0.5`/`linear_damping = 0.0` placeholder, with no way to say "this one is a real ball" — a separate, deliberate design decision `FR-061` left for a future requirement. This requirement resolves that exact gap. Fetched and read RocketSim's real `RLConst.h` source directly (matching `RB-PHYSICS-001-FR-036`/`FR-042`/`FR-043`/`FR-045`/`FR-046`/`FR-047`/`FR-048`/`FR-053`/`FR-056`/`FR-057`/`FR-058`/`FR-059`/`FR-061`'s own fetch-and-read method) and confirmed three real material-property constants: `BALL_RESTITUTION = 0.6f` ("Bounce factor"), `BALL_FRICTION = 0.35f`, and `BALL_DRAG = 0.03f` ("Net-velocity drag multiplier"). None of the three is a torque or force calibrated against a specific mass/inertia (the same "false precision" category `RB-PHYSICS-001-FR-031` ruled out for other constants) — restitution and friction are dimensionless coefficients combined at contact time, and drag is a pure per-second decay rate — so all three transfer cleanly regardless of this port's own ball not being calibrated to real Rocket League's, the same category `RB-PHYSICS-001-FR-061`'s own speed caps cleared. Fixed by adding a new, additive `body::RigidBody::ball(radius, mass, position)` constructor, identical to `sphere` for radius/mass/position but setting `restitution = 0.6`, `friction = 0.35`, and `linear_damping = 0.03` instead of the generic defaults; `sphere` itself is completely unchanged, still generic — this is new API surface, not a migration, and no existing call site was switched from `sphere` to `ball`. Explicitly not adopted: `BALL_MASS_BT = CAR_MASS_BT / 6.f` — while the `1:6` mass ratio is in principle a portable, dimensionless quantity (similar to how `RB-PHYSICS-001-FR-059`'s dodge scale ratios were adopted), this project has no canonical "real" car construction site yet (no game binary consumes this crate; every `RigidBody::car_box` call site today is test-only) to normalize that ratio against, so there's nothing to keep a `1:6` ratio with — left for a future requirement | `crates/rb_physics_bullet/src/body.rs` (only file touched in the crate — the new `ball` constructor, added right after `sphere`, with its own doc comment) | `rb_physics_bullet` unit tests (3 new: `ball_sets_confirmed_real_material_properties` unit-tests the three set fields directly; `ball_otherwise_behaves_identically_to_sphere` confirms shape/position/mass/inertia match a `sphere` built with the same arguments; `sphere_still_defaults_to_the_generic_placeholder_material_properties` is a regression pin confirming `sphere`'s own default stayed untouched). All 306 of `rb_physics_bullet`'s pre-existing tests (as of `FR-061`) pass unchanged (net +3 tests over `FR-061`'s 306, bringing the crate to 309) | ball-material-properties commit | Implemented, Verified (unit tests; genuine new-API-surface addition — `RigidBody::ball` now gives callers a way to construct a ball with real Rocket League's own confirmed restitution/friction/drag instead of `sphere`'s generic placeholder, with `sphere` itself and every existing call site unchanged; `BALL_MASS_BT`'s `1:6` mass ratio explicitly not adopted, left open for a future requirement; does not close `FR-005`; not yet scored against real data) |
| `RB-PHYSICS-001-FR-063` (real Rocket League uses per-contact-pair-type restitution/friction overrides, not a per-body combine — audit finding, documentation only) | `PHASE-1-PHYSICS-CORE` | `RB-PHYSICS-001-FR-043`'s own investigation had already checked which *formula* Bullet's own generic `combine_restitution`/`combine_friction` should use (an unclamped product, not this port's kept average) and left open "which formula (if either) actually matches real Rocket League itself... still needs real recorded ball/ground behavior to calibrate against." This requirement resolves that differently than expected: the real answer isn't "a different formula" at all. Fetched RocketSim's own `RLConst.h` (matching `RB-PHYSICS-001-FR-057`/`FR-060`/`FR-061`/`FR-062`'s own method) and found real Rocket League's gameplay layer doesn't compute restitution or friction from a generic per-body combine at all for its own named contact-pair types — it hardcodes a distinct override per pair, bypassing whatever either body's own material would otherwise combine to: `CAR_COLLISION_FRICTION = 0.3f`/`CAR_COLLISION_RESTITUTION = 0.1f` (a generic fallback), `CARWORLD_COLLISION_FRICTION = 0.3f`/`CARWORLD_COLLISION_RESTITUTION = 0.3f` (car vs. static geometry), `CARCAR_COLLISION_FRICTION = 0.09f`/`CARCAR_COLLISION_RESTITUTION = 0.1f`, and `CARBALL_COLLISION_FRICTION = 2.0f`/`CARBALL_COLLISION_RESTITUTION = 0.0f`. Two of these are individually striking, not just "different from an average": `CARBALL_COLLISION_RESTITUTION = 0.0f` means a car hitting the ball has zero restitution-driven bounce in real Rocket League regardless of either body's own material — a stark contrast with this port's own `combine_restitution(ball.restitution, car.restitution)`, which since `RB-PHYSICS-001-FR-062` averages the ball's own confirmed real `0.6` against the car's still-generic `0.5` to a real `~0.55` bounce for exactly the pairing real Rocket League gives zero; and `CARBALL_COLLISION_FRICTION = 2.0f` is a friction coefficient above `1.0`, a value no combine of two bodies' own sane (`0.0..=1.0`-range) per-material friction fields could ever produce, confirming this is a genuinely different *model* real Rocket League uses, not merely an uncalibrated magnitude. Corrected `solver::combine_restitution`/`combine_friction`'s own doc comments to state this finding directly, and corrected the stale Open Questions bullet that had framed the still-open question as merely "which formula" rather than "a per-body combine at all." Does not implement per-contact-pair-type overrides — `combine_restitution`/`combine_friction`'s own signature (two `f32` material values in, one combined value out) has no way to know which *kind* of pair produced those two values; doing so for real would mean threading body/shape identity into every one of `solver.rs`'s several call sites (`resolve_contacts`, `resolve_contacts_between`, `resolve_static_manifolds`, `resolve_dynamic_manifolds`, `resolve_manifolds`), left as a candidate for a future, dedicated requirement. Does not adopt `CAR_COLLISION_FRICTION`/`CAR_COLLISION_RESTITUTION` as the car's own default material properties (mirroring `RB-PHYSICS-001-FR-062`'s own `RigidBody::ball`) — unlike the ball, real Rocket League has no single "the car's own" restitution/friction value to adopt that way; every real value found here is contact-pair-specific, so setting the car's own generic default to any one of them would be arbitrary, not confirmed. Does not change `combine_restitution`/`combine_friction`'s own kept average formula (still `RB-PHYSICS-001-FR-043`'s own correct-reason choice for the *within-model* question that finding answered). Does not touch `RB-PHYSICS-001-FR-005`'s real-data calibration, no longer blocked on `PHASE-0-EXIT` (now closed), but not itself started | `crates/rb_physics_bullet/src/solver.rs` (`combine_restitution`'s and `combine_friction`'s own doc comments only — no production code changed), `docs/specifications/physics/RB-PHYSICS-001-physics-core-port.md` (Requirements entry added, Open Questions bullet corrected, Change History updated; version bumped to 0.63.0) | No new tests: this is a pure documentation-audit finding, matching `RB-PHYSICS-001-FR-044`/`FR-060`'s own established precedent that no new test is needed for such a change. All 309 of `rb_physics_bullet`'s pre-existing tests (as of `FR-062`) pass unchanged (total unchanged from `FR-062`, still 309) | collision-pair-restitution-friction-audit commit | Investigated, Verified (documentation-only; zero production behavior changed, no new tests; resolves `RB-PHYSICS-001-FR-043`'s own left-open "which formula matches real Rocket League" question with a concrete negative finding — real Rocket League uses per-contact-pair-type overrides, not a per-body combine of either formula; does not close `FR-005`; not yet scored against real data) |
| `RB-PHYSICS-001-FR-064` (real mandatory minimum-hold window for a ground jump's variable-height acceleration) | `PHASE-1-PHYSICS-CORE` | `drive::JUMP_HOLD_MAX_DURATION`'s own doc comment had named this exact gap since `RB-PHYSICS-001-FR-031`'s own original audit: real Rocket League scales its jump-hold acceleration down during a `JUMP_MIN_TIME` (0.025s) mandatory window rather than applying it flat from the first held step, an unmodeled "two-phase ramp." This requirement fetched RocketSim's real `Car.cpp` (`_UpdateJump`, the same real-implementation-file-fetching technique `RB-PHYSICS-001-FR-058`/`FR-059` used, not just `RLConst.h`'s constants) and confirmed the exact mechanism: the hold force keeps applying, scaled by `JUMP_PRE_MIN_ACCEL_SCALE = 0.62f`, for the first `JUMP_MIN_TIME` seconds regardless of whether `jump` is still held — not merely a slower ramp, a hard step-scale, and applied unconditionally (even an instantaneous tap gets a small amount of extra height in real Rocket League). The reference source's own inline comment flags this system as a stopgap its own authors consider provisional (`// TODO: Either move to RLConst or preferably don't use this system at all`), adopted anyway since it's still the real, currently-shipping behavior. Implemented via `drive::JUMP_MIN_TIME = 0.025` and `drive::JUMP_PRE_MIN_ACCEL_SCALE = 0.62`, and a rework of `apply_driven_forces`'s hold-acceleration check to derive elapsed time since the ground-jump press as `JUMP_HOLD_MAX_DURATION - *jump_hold_time_remaining` rather than tracking a second, separate elapsed-time field — at rest (`jump_hold_time_remaining == 0.0`, no ground jump in flight) this derivation already reads as `JUMP_HOLD_MAX_DURATION`, comfortably past `JUMP_MIN_TIME`, so a car that never pressed jump never spuriously enters the mandatory branch. Zero changes needed to `PhysicsWorld` or any existing test call site — `jump_hold_time_remaining`'s public threading is completely unchanged | `crates/rb_physics_bullet/src/drive.rs` (new constants, reworked hold-acceleration check, 3 new tests), `docs/specifications/physics/RB-PHYSICS-001-physics-core-port.md` (Requirements entry, Change History; version bumped to 0.64.0) | `rb_physics_bullet` unit tests (3 new: `jump_hold_acceleration_is_scaled_down_during_the_mandatory_pre_min_time_window`, `releasing_jump_within_the_mandatory_pre_min_time_window_does_not_immediately_stop_the_extra_acceleration`, `mandatory_pre_min_time_window_closes_on_schedule_even_when_jump_is_never_held`). All 309 of `rb_physics_bullet`'s pre-existing tests (as of `FR-063`) pass unchanged — every existing hold-window test's own release/expiry timing happens to fall at or after `JUMP_MIN_TIME` has already elapsed, so none of them exercised the new early-release-within-the-mandatory-window case this requirement adds (net +3 tests over `FR-063`'s 309, bringing the crate to 312) | jump-hold-mandatory-min-time-window commit | Implemented, Verified (unit tests; genuine behavioral fix — a ground jump's variable-height acceleration now has two phases (a mandatory `JUMP_PRE_MIN_ACCEL_SCALE`-scaled phase for `JUMP_MIN_TIME`, then the pre-existing flat phase) instead of one flat phase for the whole hold; does not close `FR-005`; not yet scored against real data) |
| `RB-PHYSICS-001-FR-065` (real steering is a wheeled-vehicle raycast model, not a torque, with an inverted speed-vs-turning-ability curve — audit finding, documentation only) | `PHASE-1-PHYSICS-CORE` | `drive::STEER_TORQUE` had no public reference at all — this port applies a direct yaw torque about the car's local up axis, scaled up with speed via a `speed_factor` that is `0.0` at a standstill and rises to `1.0` at `MAX_CAR_SPEED`. This requirement fetched RocketSim's real `Car.cpp` (`_UpdateWheels`, the same real-implementation-file-fetching technique `RB-PHYSICS-001-FR-058`/`FR-059`/`FR-064` used, not just `RLConst.h`'s constants) and found real Rocket League's steering isn't a direct yaw-torque model at all: a wheel's steer angle (not a torque) is set from a confirmed `STEER_ANGLE_FROM_SPEED_CURVE` (a piecewise-linear curve in RocketSim's `RLConst.h` mapping forward speed to a maximum steering angle in radians), and that angled wheel's lateral tire friction — computed per-wheel by `btVehicleRL`, a custom extension of Bullet's own raycast vehicle system (`btDefaultVehicleRaycaster`), through a further confirmed `LAT_FRICTION_CURVE` slip-friction curve — is what actually turns the car; this port has no wheels, raycasting, or tire-slip model at all (the car is one rigid box), so this real mechanism can't be ported without a substantially larger architecture change, the same category `RB-PHYSICS-001-FR-063` already established for per-contact-pair-type restitution/friction. A second, independently striking finding: the confirmed real curve's own shape is the opposite of this port's own `speed_factor` — real Rocket League's maximum steering angle is highest at a standstill (`0.53356` rad ≈ 30.6° at 0 uu/s) and decreases sharply as speed rises (down to `0.03454` rad ≈ 2° at 3000 uu/s), while this port's own `speed_factor` does the exact opposite (zero torque at a standstill, scaling up to full `STEER_TORQUE` at `MAX_CAR_SPEED`). Not adopted as a fix: unlike `RB-PHYSICS-001-FR-058`'s throttle taper or `FR-059`'s dodge scale (direct multipliers on a force/impulse this port already applies the same way real Rocket League does), the real curve maps speed to a wheel angle, which real Rocket League then feeds through nonlinear tire-slip friction (dependent on wheelbase geometry and friction curves this port doesn't model at all) to produce the actual turning force — there's no principled way to carry even the curve's normalized shape onto this port's own direct-torque model; reversing `speed_factor`'s direction without that transfer function would substitute one unconfirmed guess for another, not adopt a confirmed real value, the same reasoning that kept `RB-PHYSICS-001-FR-057`'s `AIR_CONTROL_TORQUE` and `FR-059`'s `DODGE_SPEED` base magnitude as placeholders despite a real reference existing for each. The same investigation also found this spec's own Open Questions section's "Calibrating drive's constants" bullet still stale, claiming `AIR_CONTROL_TORQUE`/`JUMP_HOLD_MAX_DURATION`/`JUMP_HOLD_ACCELERATION` had "no public reference at all" — contradicted by `RB-PHYSICS-001-FR-057`'s and `FR-031`'s own already-shipped findings (both already have confirmed real references, one that doesn't transfer to this port's model, one already fully adopted) — corrected as part of this same commit since it directly touched the same passage | `crates/rb_physics_bullet/src/drive.rs` (`STEER_TORQUE`'s and `MAX_CAR_SPEED`'s own doc comments and the `speed_factor` call site's own inline comment corrected, plus the module doc's "commonly-cited constants" paragraph — no production code changed), `docs/specifications/physics/RB-PHYSICS-001-physics-core-port.md` (Requirements entry added, adjacent stale Open Questions text corrected, Change History updated; version bumped to 0.65.0) | No new tests: this is a pure documentation-audit finding, matching `RB-PHYSICS-001-FR-044`/`FR-060`/`FR-063`'s own established precedent that no new test is needed for such a change. All 312 of `rb_physics_bullet`'s pre-existing tests (as of `FR-064`) pass unchanged (total unchanged from `FR-064`, still 312) | steering-wheeled-vehicle-audit commit | Investigated, Verified (documentation-only; zero production behavior changed, no new tests; resolves whether real steering is a torque this port could calibrate against with a concrete negative finding — real Rocket League's steering is a wheeled-vehicle raycast/tire-slip model this port's single-rigid-box car can't represent, and the confirmed curve's own shape is inverted relative to this port's own `speed_factor`; does not close `FR-005`; not yet scored against real data) |
| `RB-PHYSICS-001-FR-066` (real handbrake friction reduction is anisotropic, not a single uniform multiplier — audit finding, documentation only) | `PHASE-1-PHYSICS-CORE` | `drive::HANDBRAKE_FRICTION_MULTIPLIER = 0.1` had no public reference at all — this port applies that single value as a flat multiplier onto the handbraking body's own isotropic `RigidBody.friction` scalar, read identically by the ground-contact solver for every direction. This requirement fetched RocketSim's real `Car.cpp` (`_UpdateWheels`, the same real-implementation-file-fetching technique `RB-PHYSICS-001-FR-058`/`FR-059`/`FR-064`/`FR-065` used, not just `RLConst.h`'s constants), continuing `RB-PHYSICS-001-FR-065`'s own `_UpdateWheels` investigation, and found real Rocket League's handbrake friction reduction is genuinely anisotropic: two separate confirmed real curves, `HANDBRAKE_LAT_FRICTION_FACTOR_CURVE` (a constant `0.1` factor at every speed) and `HANDBRAKE_LONG_FRICTION_FACTOR_CURVE` (`0.5` at a standstill, `0.9` at and above 1 uu/s — effectively a near-constant, barely-reduced `0.9` for any real driving speed), applied to lateral and longitudinal tire friction independently, not one shared multiplier applied to both. A second, striking finding: this port's own pre-existing `HANDBRAKE_FRICTION_MULTIPLIER = 0.1` happens to match the real *lateral-only* factor exactly — but this port applies that same `0.1` to its own single isotropic friction scalar, which the ground-contact solver reads identically for every direction; real Rocket League's own handbrake drift keeps a car's forward/backward grip almost intact (`x0.9`) while cutting sideways grip to a tenth (`x0.1`), so this port's own uniform `0.1` wrongly crushes longitudinal grip to a tenth too, understating real forward-momentum retention during a drift — a striking coincidence, not a confirmation. Corrected `drive::HANDBRAKE_FRICTION_MULTIPLIER`'s own doc comment, the module doc comment's "Handbrake" paragraph, and the "commonly-cited constants" paragraph to state this finding directly. Does not implement anisotropic friction: `solver::friction_directions` already computes two separate tangent directions per contact (since `RB-PHYSICS-001-FR-049`), but both directions currently read the same single combined-friction scalar when their row limits are computed; giving handbrake a genuinely different lateral-vs-longitudinal factor would mean threading a second, direction-specific friction coefficient through every one of `solver.rs`'s several row-limit call sites (`resolve_contacts`, `resolve_contacts_between`, `resolve_static_manifolds`, `resolve_dynamic_manifolds`, `resolve_manifolds`) plus a way for those call sites to know a specific body is currently handbraking — the same architecture-mismatch category `RB-PHYSICS-001-FR-063`/`FR-065` already established. Does not change `HANDBRAKE_FRICTION_MULTIPLIER`'s own value — its coincidental match to the real lateral factor is not grounds to keep it as-is or change it without also fixing the longitudinal side, which needs the architecture change above. Does not touch `RB-PHYSICS-001-FR-005`'s real-data calibration, no longer blocked on `PHASE-0-EXIT` (now closed), but not itself started | `crates/rb_physics_bullet/src/drive.rs` (`HANDBRAKE_FRICTION_MULTIPLIER`'s own doc comment and the module doc's "Handbrake" and "commonly-cited constants" paragraphs corrected — no production code changed), `docs/specifications/physics/RB-PHYSICS-001-physics-core-port.md` (Requirements entry added, Change History updated; version bumped to 0.66.0) | No new tests: this is a pure documentation-audit finding, matching `RB-PHYSICS-001-FR-044`/`FR-060`/`FR-063`/`FR-065`'s own established precedent that no new test is needed for such a change. All 312 of `rb_physics_bullet`'s pre-existing tests (as of `FR-065`) pass unchanged (total unchanged from `FR-065`, still 312) | handbrake-anisotropic-friction-audit commit | Investigated, Verified (documentation-only; zero production behavior changed, no new tests; confirms real handbrake friction reduction is anisotropic — a separate near-constant `~0.9` longitudinal factor alongside the confirmed `0.1` lateral factor this port's own multiplier coincidentally matches, which this port's single isotropic friction scalar can't capture without threading a direction-specific coefficient through `solver.rs`; does not close `FR-005`; not yet scored against real data) |
| `RB-PHYSICS-001-FR-067` (real Rocket League has no distinct wall-jump mechanic or constant at all — audit finding, documentation only) | `PHASE-1-PHYSICS-CORE` | `drive::WALL_JUMP_HORIZONTAL_SPEED` had no public reference at all — this port applies it as a horizontal push-off speed along a wall's normal, stacked with the existing vertical `JUMP_SPEED` component, for a wall jump. This requirement fetched RocketSim's real `Car.cpp` (`_UpdateJump`, the same real-implementation-file-fetching technique `RB-PHYSICS-001-FR-058`/`FR-059`/`FR-064`/`FR-065`/`FR-066` used, not just `RLConst.h`'s constants), closing a thread `RB-PHYSICS-001-FR-031`'s original audit only briefly noted ("a wall jump reusing the plain jump impulse rather than its own faster speed") without confirming the exact mechanism, and found real Rocket League has no separate wall-jump mechanic — or constant — at all: `_UpdateJump` applies exactly one impulse, `GetUpDir() * mutatorConfig.jumpImmediateForce` (the same real value this port's own `JUMP_SPEED` already matches), gated only on `isOnGround`, itself defined purely by wheel-contact count (`numWheelsInContact >= 3`) with no floor-vs-wall distinction at all; a dedicated search of `RLConst.h` for any `WALL`-named constant found only an unrelated Heatseeker-mode threshold (`WALL_BOUNCE_CHANGE_Y_THRESH`). Since `RB-PHYSICS-001-FR-065` already confirmed real cars ride Bullet's own raycast vehicle system (`btVehicleRL`), a car driving on a wall has its own orientation continuously tipped to match that wall by ordinary wheel/suspension contact forces, the same way a real car tilts to match a ramp — so `GetUpDir()` (the car's own local up axis in world space) already points along the wall's outward normal by the time a wall jump fires, with no special-cased direction logic needed anywhere in `_UpdateJump`; real Rocket League's "wall jump" is thus the identical single grounded-jump impulse, along whatever direction the car's own up axis currently points, never a distinct horizontal-plus-vertical composite with its own separate magnitude. Corrected `drive::WALL_JUMP_HORIZONTAL_SPEED`'s own doc comment, the module doc's own wall-jump section, and the module doc's "commonly-cited constants" paragraph to state this finding directly. Does not remove this port's own two-component composite impulse (a horizontal push-off along the wall's normal, stacked with the existing vertical `JUMP_SPEED`) — this port's car has no wheels, raycasting, or surface-tracking orientation system at all (the same architecture gap `RB-PHYSICS-001-FR-065` found for steering), so its own orientation doesn't automatically tip to match a touched wall the way a real car does; applying only `JUMP_SPEED` straight up on a wall touch, as the confirmed real mechanism would otherwise suggest, would produce no push-off from the wall at all in this port's own model, defeating the entire point of a wall jump — the composite shape is a deliberate, necessary substitute for the missing orientation mechanism, not an unfilled calibration gap. Does not change `WALL_JUMP_HORIZONTAL_SPEED`'s own magnitude, which remains an uncalibrated placeholder. Does not touch `RB-PHYSICS-001-FR-005`'s real-data calibration, no longer blocked on `PHASE-0-EXIT` (now closed), but not itself started | `crates/rb_physics_bullet/src/drive.rs` (`WALL_JUMP_HORIZONTAL_SPEED`'s own doc comment, the module doc's own wall-jump section, and the "commonly-cited constants" paragraph corrected — no production code changed), `docs/specifications/physics/RB-PHYSICS-001-physics-core-port.md` (Requirements entry added, Change History updated; version bumped to 0.67.0) | No new tests: this is a pure documentation-audit finding, matching `RB-PHYSICS-001-FR-044`/`FR-060`/`FR-063`/`FR-065`/`FR-066`'s own established precedent that no new test is needed for such a change. All 312 of `rb_physics_bullet`'s pre-existing tests (as of `FR-066`) pass unchanged (total unchanged from `FR-066`, still 312) | wall-jump-no-distinct-mechanism-audit commit | Investigated, Verified (documentation-only; zero production behavior changed, no new tests; confirms real Rocket League has no distinct wall-jump mechanic or constant at all — the identical single grounded-jump impulse applied along the car's own, wall-tipped up axis; this port's own two-component composite substitute remains deliberate and necessary since this port's car has no wheels, raycasting, or surface-tracking orientation system; does not close `FR-005`; not yet scored against real data) |
| `RB-PHYSICS-001-FR-068` (real per-axis air-control torque ratio) | `PHASE-1-PHYSICS-CORE` | `drive::AIR_CONTROL_TORQUE` was a single flat magnitude applied identically to all three air-control axes (pitch/yaw/roll) — `RB-PHYSICS-001-FR-031`'s own audit had already found real per-axis air-control torque coefficients exist but couldn't adopt them, since they're absolute torques calibrated against real Rocket League's own specific car mass/inertia tensor, the same "false precision" reasoning that kept `AIR_CONTROL_TORQUE` a placeholder. This requirement fetched RocketSim's real `Car.cpp` (`_UpdateAirTorque`, the same real-implementation-file-fetching technique `RB-PHYSICS-001-FR-058`/`FR-059`/`FR-064`/`FR-065`/`FR-066`/`FR-067` used, not just `RLConst.h`'s constants) and found the real mechanism — `torque = pitch * CAR_AIR_CONTROL_TORQUE.x + yaw * CAR_AIR_CONTROL_TORQUE.y + roll * CAR_AIR_CONTROL_TORQUE.z` — structurally identical to this port's own: a direct per-axis torque scaled by analog stick input, unlike steering (`FR-065`, a wheeled-vehicle raycast/tire-slip model) or handbrake (`FR-066`, a friction split this port's architecture can't represent), with `RLConst.h` confirming `CAR_AIR_CONTROL_TORQUE = Vec(130, 95, 400)` (pitch-yaw-roll order). Because the mechanism itself matches, the confirmed per-axis *ratio* — unlike the real absolute torque values, which the pre-existing "false precision" finding already ruled out — is adoptable the same way `RB-PHYSICS-001-FR-058`'s throttle taper and `FR-059`'s dodge scale ratios are: a direct multiplier on a torque this port already applies the same way real Rocket League does. Fixed by adding `drive::AIR_CONTROL_YAW_SCALE = 95.0 / 130.0` and `drive::AIR_CONTROL_ROLL_SCALE = 400.0 / 130.0`, redefining `AIR_CONTROL_TORQUE` (value unchanged, `1_000_000.0`, still an uncalibrated placeholder) as pitch's own magnitude specifically rather than a flat value shared by all three axes, and wiring both scales into `apply_driven_forces`'s air-control block — yaw's torque is now `AIR_CONTROL_TORQUE * AIR_CONTROL_YAW_SCALE`, roll's is `AIR_CONTROL_TORQUE * AIR_CONTROL_ROLL_SCALE`, pitch is unchanged. Also corrected the module-level air-control paragraph, the "commonly-cited constants" paragraph, and `AIR_CONTROL_TORQUE`'s own doc comment, which previously described the three axes as sharing one flat magnitude with no ratio modeled. Does not adopt RocketSim's own real absolute torque magnitudes (`130`, `95`, `400`) for this port's own axes — still independently uncalibrated, the same "ratio confirmed, magnitude not" split `RB-PHYSICS-001-FR-058`/`FR-059` already established; does not adopt RocketSim's own `CAR_AIR_CONTROL_DAMPING` (`Vec(30, 20, 50)`) — this port has no per-axis air-control damping term at all, a separate, independent addition left for a future requirement; does not adopt RocketSim's own `pitchTorqueScale` factor applied only to the pitch component in `_UpdateAirTorque` (an additional speed- or state-dependent scale this requirement's own fetch surfaced but didn't fully characterize), scoped out to keep this requirement to the confirmed, fully-characterized per-axis ratio alone. Does not touch `RB-PHYSICS-001-FR-005`'s real-data calibration, no longer blocked on `PHASE-0-EXIT` (now closed), but not itself started | `crates/rb_physics_bullet/src/drive.rs` (`AIR_CONTROL_YAW_SCALE`/`AIR_CONTROL_ROLL_SCALE` constants, `AIR_CONTROL_TORQUE`'s redefinition, the rewired air-control block in `apply_driven_forces`, and corrected doc comments; only file touched in the crate), `docs/specifications/physics/RB-PHYSICS-001-physics-core-port.md` (Requirements entry added, Change History updated; version bumped to 0.68.0) | `rb_physics_bullet` unit tests (2 new: `yaw_air_control_is_scaled_down_from_pitch_by_the_confirmed_real_ratio` and `roll_air_control_is_scaled_up_from_pitch_by_the_confirmed_real_ratio`, each computing the exact expected angular velocity in closed form from `AIR_CONTROL_TORQUE`/the new scale constant/`car().inv_inertia_world()` and asserting the actual post-step value matches within `1e-3`). All 312 of `rb_physics_bullet`'s pre-existing tests (as of `FR-067`) pass unchanged (net +2 tests over `FR-067`'s 312, bringing the crate to 314) | air-control-per-axis-ratio commit | Implemented, Verified (unit tests; genuine behavioral fix — yaw now produces measurably less angular velocity than pitch for equal analog input, and roll measurably more, where previously all three axes were identical; does not adopt the real absolute torque magnitudes, `CAR_AIR_CONTROL_DAMPING`, or the pitch-only `pitchTorqueScale` factor, all left open for a future requirement; does not close `FR-005`; not yet scored against real data) |
| `RB-PHYSICS-001-FR-069` (real flip spin is a continuous per-axis torque, not an instantaneous kick — audit finding, documentation only) | `PHASE-1-PHYSICS-CORE` | `drive::DODGE_ANGULAR_SPEED` applies a flat, instantaneous angular-velocity kick to the car when a dodge/flip starts, with no public reference confirming its own real mechanism — `RB-PHYSICS-001-FR-031`'s own original audit had already found real reference constants (`FLIP_TORQUE_X = 260`, `FLIP_TORQUE_Y = 224`, a `0.65s` duration) but not the mechanism behind them. This requirement fetched RocketSim's real `Car.cpp` (`_UpdateDoubleJumpOrFlip` and `_UpdateAirTorque`, the same real-implementation-file-fetching technique `RB-PHYSICS-001-FR-058`/`FR-059`/`FR-064`/`FR-065`/`FR-066`/`FR-067`/`FR-068` used, not just `RLConst.h`'s constants) and found the real mechanism is not an instantaneous kick at all but a continuous per-axis torque: `_UpdateDoubleJumpOrFlip` records a per-axis `flipRelTorque` once, at flip start, and a separate, later step, `_UpdateAirTorque`, applies `flipRelTorque * Vec(FLIP_TORQUE_X, FLIP_TORQUE_Y, 0)` every physics tick for as long as `isFlipping = hasFlipped && flipTime < FLIP_TORQUE_TIME` holds — a hard 0.65s cutoff, with no decay or ramp before it — confirming `RB-PHYSICS-001-FR-031`'s own already-found `FLIP_TORQUE_X`/`FLIP_TORQUE_Y`/`0.65s` constants are this mechanism's own coefficients and duration, not free-standing values. Corrected `drive::DODGE_ANGULAR_SPEED`'s own doc comment, the module doc's dodge section, and the module doc's "commonly-cited constants" paragraph to state this finding directly. Does not adopt the real continuous-torque mechanism as a fix: the real resulting spin rate depends on real Rocket League's own specific hitbox inertia tensor, which this port's placeholder car body doesn't match, the same "false precision" reasoning `RB-PHYSICS-001-FR-031` already applied elsewhere; and reproducing the real timed-torque *shape* (not just magnitude) would require new per-car elapsed-flip-time state threaded through `PhysicsWorld`, a redesign `RB-PHYSICS-001-FR-059`'s own Non-goals already flagged as out of scope. Does not touch `RB-PHYSICS-001-FR-005`'s real-data calibration, no longer blocked on `PHASE-0-EXIT` (now closed), but not itself started | `crates/rb_physics_bullet/src/drive.rs` (`DODGE_ANGULAR_SPEED`'s own doc comment, the module doc's dodge section, and the "commonly-cited constants" paragraph corrected — no production code changed), `docs/specifications/physics/RB-PHYSICS-001-physics-core-port.md` (Requirements entry added, Open Questions bullet corrected, Change History updated; version bumped to 0.69.0) | No new tests: this is a pure documentation-audit finding, matching `RB-PHYSICS-001-FR-044`/`FR-060`/`FR-063`/`FR-065`/`FR-066`/`FR-067`'s own established precedent that no new test is needed for such a change. All 314 of `rb_physics_bullet`'s pre-existing tests (as of `FR-068`) pass unchanged (total unchanged from `FR-068`, still 314) | dodge-spin-continuous-torque-audit commit | Investigated, Verified (documentation-only; zero production behavior changed, no new tests; confirms real Rocket League's flip spin is a continuous per-axis torque applied every tick for a hard 0.65s window, not an instantaneous kick, and that `RB-PHYSICS-001-FR-031`'s own already-found `FLIP_TORQUE_X`/`FLIP_TORQUE_Y`/`0.65s` constants are this mechanism's own coefficients and duration; not adopted as a fix since it would need both real Rocket League's own hitbox inertia tensor and new per-car elapsed-flip-time state this port doesn't have; does not close `FR-005`; not yet scored against real data) |
| `RB-PHYSICS-001-FR-070` (real flip-cancel is a continuous, proportional, pitch-axis-only torque reduction, not this port's own instantaneous full-axis zero-out — audit finding, documentation only) | `PHASE-1-PHYSICS-CORE` | This port's own flip-cancel (`RB-PHYSICS-001-FR-016`, in `drive.rs`'s `apply_driven_forces`) triggers on a fresh jump press while airborne (not touching a wall, double-jump already spent, `dodge_flip_active` still set) and zeros `RigidBody.angular_velocity` outright — every axis, regardless of dodge direction — with its own doc comment claiming this "matched real Rocket League," a claim never actually checked against real source. `RB-PHYSICS-001-FR-069`'s own fetch of RocketSim's real `Car.cpp` (`_UpdateAirTorque`) had itself surfaced a `pitchTorqueScale` factor applied only to the pitch component, scoped out at the time as "an additional speed- or state-dependent scale this requirement's own fetch surfaced but didn't fully characterize." This requirement closes that thread by fetching and reading `Car.cpp` again (matching `RB-PHYSICS-001-FR-058`/`FR-059`/`FR-064`/`FR-065`/`FR-066`/`FR-067`/`FR-068`/`FR-069`'s own method) and found the real mechanism: real Rocket League's flip-cancel is driven by holding the stick, not a discrete jump press — while still flipping (`isFlipping`), if the flip's own stored pitch-torque component (`flipRelTorque.y()`) is nonzero and `controls.pitch` is held in that same sign, `pitchScale = 1 - abs(controls.pitch)` is computed and multiplies only the pitch-axis torque component of `_UpdateAirTorque`'s continuous per-axis torque (the same continuous mechanism `RB-PHYSICS-001-FR-069` already confirmed for the dodge's own spin), applied every tick, for as long as the flip continues — continuous, proportional, and pitch-axis-only; a sideways (roll-only) dodge has no pitch-torque component at all, so real Rocket League cannot pitch-cancel it. Corrected `drive.rs`'s own module doc flip-cancel paragraph (removed the inaccurate "matching real Rocket League" claim, added the real mechanism and why it isn't adopted) and gave `RB-PHYSICS-001-FR-016`'s own Requirements entry a forward citation to this one. Does not adopt the real mechanism as a fix: this port's dodge is a single flat angular-velocity kick with no per-axis torque split to partially cancel — the same architecture gap `RB-PHYSICS-001-FR-069` already confirmed for the dodge's own spin — and reproducing the real continuous-hold trigger and pitch-only scope would need the same continuous per-axis torque and elapsed-flip-time state `RB-PHYSICS-001-FR-059`'s own Non-goals already flagged as out of scope. Does not touch `RB-PHYSICS-001-FR-005`'s real-data calibration, no longer blocked on `PHASE-0-EXIT` (now closed), but not itself started | `crates/rb_physics_bullet/src/drive.rs` (module doc flip-cancel paragraph corrected — no production code changed), `docs/specifications/physics/RB-PHYSICS-001-physics-core-port.md` (Requirements entry added, `RB-PHYSICS-001-FR-016`'s own entry given a forward citation, Change History updated; version bumped to 0.70.0) | No new tests: this is a pure documentation-audit finding, matching `RB-PHYSICS-001-FR-044`/`FR-060`/`FR-063`/`FR-065`/`FR-066`/`FR-067`/`FR-069`'s own established precedent that no new test is needed for such a change. All 314 of `rb_physics_bullet`'s pre-existing tests (as of `FR-069`) pass unchanged (total unchanged from `FR-069`, still 314) | flip-cancel-mechanism-audit commit | Investigated, Verified (documentation-only; zero production behavior changed, no new tests; confirms real Rocket League's flip-cancel is a continuous, proportional, pitch-axis-only torque reduction driven by holding the stick, not this port's own instantaneous full-axis-zero trigger on a fresh jump press; does not close `FR-005`; not yet scored against real data) |
| `RB-PHYSICS-001-FR-071` (real per-axis air-control damping mechanism — audit finding, documentation only) | `PHASE-1-PHYSICS-CORE` | `RB-PHYSICS-001-FR-068`'s own Non-goals had already found RocketSim's real `CAR_AIR_CONTROL_DAMPING = Vec(30, 20, 50)` — a per-axis air-control damping term this port has no equivalent for at all — but left it as "a separate, independent addition left for a future requirement" without examining the mechanism behind it. This requirement closes that thread by fetching RocketSim's real `Car.cpp` again (the same `_UpdateAirTorque` fetch `RB-PHYSICS-001-FR-070` used to characterize `pitchTorqueScale`, matching `RB-PHYSICS-001-FR-058`/`FR-059`/`FR-064`/`FR-065`/`FR-066`/`FR-067`/`FR-068`/`FR-069`/`FR-070`'s own method) and found the full mechanism: for each axis, real air control subtracts a damping torque — `(angular velocity along that axis) * CAR_AIR_CONTROL_DAMPING[axis] * (1 - abs(analog input on that axis))` — from the applied torque before scaling by inertia, with pitch's own input term additionally multiplied by `pitchTorqueScale` (`RB-PHYSICS-001-FR-070`'s own already-confirmed factor); releasing the stick on an axis gives full damping strength there, continuously bleeding off any existing spin, while holding it fully zeroes the damping, granting full torque authority with no resistance. Corrected `drive.rs`'s own module doc air-control paragraph and `AIR_CONTROL_ROLL_SCALE`'s own doc comment with this confirmed mechanism, and gave `RB-PHYSICS-001-FR-068`'s own Non-goals a forward citation to this one. Does not adopt the real mechanism as a fix: unlike `AIR_CONTROL_TORQUE`'s own pitch/yaw/roll ratio (`RB-PHYSICS-001-FR-068`), which scaled a torque this port already applies the same way real Rocket League does, this port has no existing damping term at all to apply a ratio to — introducing one is a genuinely new mechanism, not a multiplier transfer — and its absolute coefficients are calibrated against real Rocket League's own specific inertia tensor, the same "false precision" reasoning that already keeps `AIR_CONTROL_TORQUE` itself a placeholder. Does not touch `RB-PHYSICS-001-FR-005`'s real-data calibration, no longer blocked on `PHASE-0-EXIT` (now closed), but not itself started | `crates/rb_physics_bullet/src/drive.rs` (module doc air-control paragraph and `AIR_CONTROL_ROLL_SCALE`'s own doc comment corrected — no production code changed), `docs/specifications/physics/RB-PHYSICS-001-physics-core-port.md` (Requirements entry added, `RB-PHYSICS-001-FR-068`'s own Non-goals given a forward citation, Change History updated; version bumped to 0.71.0) | No new tests: this is a pure documentation-audit finding, matching `RB-PHYSICS-001-FR-044`/`FR-060`/`FR-063`/`FR-065`/`FR-066`/`FR-067`/`FR-069`/`FR-070`'s own established precedent that no new test is needed for such a change. All 314 of `rb_physics_bullet`'s pre-existing tests (as of `FR-070`) pass unchanged (total unchanged from `FR-070`, still 314) | air-control-damping-audit commit | Investigated, Verified (documentation-only; zero production behavior changed, no new tests; confirms real Rocket League's per-axis air-control damping — a velocity-proportional, input-release-driven torque subtracted before inertia scaling, with pitch's own term additionally scaled by `pitchTorqueScale` — is genuinely a new mechanism this port has no equivalent for, not a multiplier transfer onto an existing term; does not close `FR-005`; not yet scored against real data) |
| `RB-PHYSICS-001-FR-072` (normalized diagonal-dodge direction, matching real Rocket League) | `PHASE-1-PHYSICS-CORE` | `RB-PHYSICS-001-FR-059`'s own Non-goals had already found and flagged a gap — this port's dodge summed each axis' own full-strength `(pitch, roll)` contribution independently, so a diagonal dodge (both axes held) came out `sqrt(2)`-ish times faster than an axis-aligned one, unlike real Rocket League — "a separate, independent behavioral question this requirement doesn't take on." This requirement fetched RocketSim's real `Car.cpp` (`_UpdateDoubleJumpOrFlip`, the same real-implementation-file-fetching technique `RB-PHYSICS-001-FR-058`/`FR-059`/`FR-064` through `FR-071`'s own method used, not just `RLConst.h`'s constants) and found the exact real mechanism: `dodgeDir = btVector3(-controls.pitch, controls.yaw + controls.roll, 0)`, then `dodgeDir.safeNormalized()` — normalized to unit length *before* `FLIP_INITIAL_VEL_SCALE` and the further per-axis forward/backward/side speed scaling (`dodge_speed_scale`/`dodge_pitch_is_backward`, `RB-PHYSICS-001-FR-059`'s own already-adopted finding) are applied. Unlike a wheeled-vehicle/tire-slip model or a continuous-torque timing state this port's architecture can't represent (the categories `RB-PHYSICS-001-FR-063`/`FR-065`/`FR-069` already established), normalizing a 2D direction vector needs no new machinery — it's a pure geometric operation this port's own model represents exactly, so it transfers cleanly the same way `RB-PHYSICS-001-FR-058`/`FR-059`/`FR-068`'s own adopted ratios do, regardless of `DODGE_SPEED`'s own uncalibrated base magnitude. Fixed by adding `drive::normalize_dodge_direction(pitch, roll) -> (f32, f32)`, a small pure helper normalizing the combined `(pitch, roll)` direction to unit length (returning `(0.0, 0.0)` for zero input), wired into both the ground-dodge and wall-jump-dodge code paths in `apply_driven_forces`: the existing per-axis `DODGE_DEADZONE` trigger checks and `dodge_pitch_is_backward`'s own sign classification are unchanged (both still read the raw stick values), but the magnitude each axis contributes to `DODGE_SPEED`/`DODGE_ANGULAR_SPEED` now comes from the normalized pair instead of the raw one. Deliberately kept this port's own sign convention (`dodge_pitch` positive means forward) rather than the reference's own negated `-controls.pitch`, and did not fold in real yaw input's own contribution to `dodgeDir` (`controls.yaw + controls.roll`) — this port's dodge direction stays pitch/roll only, `RB-PHYSICS-001-FR-059`'s own Non-goals' own separate, pre-existing simplification. Does not adopt `DODGE_SPEED`'s own real base magnitude (`FLIP_INITIAL_VEL_SCALE = 500.f`) — still independently uncalibrated, unaffected by this requirement. Does not fold real yaw input into the dodge direction. Does not adopt RocketSim's own continuous torque-over-`FLIP_TORQUE_TIME` spin model — `DODGE_ANGULAR_SPEED` remains a single instantaneous kick, scaled by the same normalized direction as the linear impulse but still an architecture mismatch `RB-PHYSICS-001-FR-069`'s own Non-goals already established. Does not touch `RB-PHYSICS-001-FR-005`'s real-data calibration, no longer blocked on `PHASE-0-EXIT` (now closed), but not itself started | `crates/rb_physics_bullet/src/drive.rs` (module doc dodge paragraph corrected, new `normalize_dodge_direction` helper with full doc comment added, both dodge code paths in `apply_driven_forces` rewired, tests updated/added), `docs/specifications/physics/RB-PHYSICS-001-physics-core-port.md` (Requirements entry added, `RB-PHYSICS-001-FR-059`'s own Non-goals given a forward citation, a stale summary paragraph elsewhere in the doc corrected, Change History updated; version bumped to 0.72.0) | `rb_physics_bullet` unit tests (`a_diagonal_dodge_combines_pitch_and_roll` and `a_diagonal_wall_jump_dodge_combines_pitch_and_roll` updated to assert the corrected per-axis magnitude — `DODGE_SPEED / sqrt(2)` for an equal pitch/roll split, with the total magnitude still matching an axis-aligned dodge's own `DODGE_SPEED` — plus 3 new tests directly exercising `normalize_dodge_direction`). All 314 of `rb_physics_bullet`'s pre-existing tests (as of `FR-071`) pass with these 2 updated plus 3 new (net +3 tests over `FR-071`'s 314, bringing the crate to 317) | normalize-diagonal-dodge-direction commit | Implemented, Verified (unit tests; genuine behavioral fix — a diagonal dodge now produces the same total linear-impulse magnitude as an axis-aligned dodge, matching real Rocket League's own confirmed normalized-direction mechanism, where previously it came out `sqrt(2)`-ish times faster; does not adopt `DODGE_SPEED`'s own real base magnitude or fold real yaw input into the dodge direction; does not close `FR-005`; not yet scored against real data) |
| `RB-PHYSICS-001-FR-073` (fold yaw input into the dodge/wall-jump-dodge direction, matching real Rocket League) | `PHASE-1-PHYSICS-CORE` | `RB-PHYSICS-001-FR-059`'s own Non-goals (and `RB-PHYSICS-001-FR-072`'s own doc comment on `normalize_dodge_direction`) had already found and flagged a gap — this port's dodge/wall-jump-dodge direction read only `pitch`/`roll` stick input, never `yaw`, even though real Rocket League's own confirmed mechanism folds yaw into the same axis as roll. This requirement fetched RocketSim's real `Car.cpp` (`_UpdateDoubleJumpOrFlip`, the same real-implementation-file-fetching technique `RB-PHYSICS-001-FR-058`/`FR-059`/`FR-064` through `FR-072`'s own method used) and confirmed the exact mechanism: `dodgeDir = btVector3(-controls.pitch, controls.yaw + controls.roll, 0)`; `controls.yaw` appears nowhere else in the function, it only ever feeds `dodgeDir`. Confirmed this needed no new machinery: unlike a wheeled-vehicle model or a continuous-torque timing state, this port already reads `input.yaw` in the same function (`apply_driven_forces`) for air control, so folding it into the dodge's own roll-axis stick value is a pure additive combination of an already-available input, the same kind of "pure operation, no new architecture" transfer `RB-PHYSICS-001-FR-058`/`FR-059`/`FR-068`/`FR-072`'s own adopted findings share. Fixed by changing both dodge call sites (the ground double-jump-dodge branch and the wall-jump-dodge branch) so `dodge_roll`/`wall_roll` are now `input.roll.unwrap_or(0.0).clamp(-1.0, 1.0) + input.yaw.unwrap_or(0.0).clamp(-1.0, 1.0)` (each individually clamped first, matching how air control already clamps pitch/yaw/roll separately), rather than `input.roll` alone — this combined value feeds the existing `DODGE_DEADZONE` trigger check, `normalize_dodge_direction`, and the `DODGE_SPEED`/`DODGE_ANGULAR_SPEED` scaling, all unchanged otherwise; `dodge_pitch_is_backward`'s own sign check still reads raw pitch only. Not adopted: RocketSim's own all-or-nothing cancellation check (combined `yaw + roll` and `pitch` both under `0.1` zeroes the whole direction, vs. this port's independent per-axis trigger) and its post-normalization small-component zeroing — both separate architectural differences left open. Does not touch `RB-PHYSICS-001-FR-005`'s real-data calibration, no longer blocked on `PHASE-0-EXIT` (now closed), but not itself started | `crates/rb_physics_bullet/src/drive.rs` (both dodge call sites in `apply_driven_forces` rewired to fold `input.yaw` into the roll-axis stick value, tests added), `docs/specifications/physics/RB-PHYSICS-001-physics-core-port.md` (Requirements entry added, `RB-PHYSICS-001-FR-059`'s own Non-goals and `RB-PHYSICS-001-FR-072`'s own doc comment given forward citations, Change History updated; version bumped to 0.73.0) | `rb_physics_bullet` unit tests (`a_yaw_only_press_fires_a_sideways_dodge_like_roll`, `yaw_and_roll_combine_in_the_dodge_direction`, and `a_yaw_only_press_fires_a_sideways_wall_jump_dodge_like_roll` — 3 new tests directly exercising the combined roll+yaw dodge direction). All 317 of `rb_physics_bullet`'s pre-existing tests (as of `FR-072`) pass unchanged (net +3 tests over `FR-072`'s 317, bringing the crate to 320) | fold-yaw-into-dodge-direction commit | Implemented, Verified (unit tests; genuine behavioral fix — folds `input.yaw` into the dodge/wall-jump-dodge roll-axis stick value, matching real Rocket League's own confirmed `dodgeDir = (-pitch, yaw + roll, 0)`, where previously yaw contributed nothing to dodge direction; does not adopt RocketSim's own all-or-nothing cancellation check or post-normalization small-component zeroing; does not close `FR-005`; not yet scored against real data) |
| `RB-PHYSICS-001-FR-074` (snap a near-axis-aligned dodge to a pure single axis, matching real Rocket League) | `PHASE-1-PHYSICS-CORE` | `RB-PHYSICS-001-FR-073`'s own Non-goals had flagged RocketSim's post-normalization small-component zeroing as "a separate, independent simplification" left open — a mis-scoping this requirement corrects: it is not a separate mechanism at all, but a further pure post-processing step on the exact normalized `(pitch, roll)` pair `normalize_dodge_direction` already computes. This requirement re-confirmed RocketSim's real `Car.cpp` (`_UpdateDoubleJumpOrFlip`, the same real-implementation-file-fetching technique `RB-PHYSICS-001-FR-072`/`FR-073`'s own method used) and confirmed the exact mechanism: after `dodgeDir = dodgeDir.safeNormalized()`, any normalized component whose absolute value falls below `0.1` gets zeroed (`if (abs(dodgeDir.x()) < 0.1f) dodgeDir.x() = 0; if (abs(dodgeDir.y()) < 0.1f) dodgeDir.y() = 0;`), and not re-normalized afterward. Confirmed this needed no new machinery: like normalization itself (`FR-072`), zeroing a small component of an already-computed pair is a pure post-processing step this function's own existing return value already supports, the same kind of "pure operation, no new architecture" transfer `RB-PHYSICS-001-FR-058`/`FR-059`/`FR-068`/`FR-072`/`FR-073`'s own adopted findings share. Fixed by adding `drive::DODGE_DIRECTION_SNAP_THRESHOLD: f32 = 0.1` (a distinct named constant from `DODGE_DEADZONE`, despite sharing the same real value, since they serve different real purposes — a raw-stick trigger threshold vs. a post-normalization direction-snap threshold) and wiring the zeroing into `normalize_dodge_direction`'s own return path. Both existing dodge call sites (the ground double-jump-dodge branch and the wall-jump-dodge branch, in `apply_driven_forces`) already route through `normalize_dodge_direction`, so no call-site changes were needed | `crates/rb_physics_bullet/src/drive.rs` (`DODGE_DIRECTION_SNAP_THRESHOLD` added, `normalize_dodge_direction`'s own return path wired to zero a small component, tests added), `docs/specifications/physics/RB-PHYSICS-001-physics-core-port.md` (Requirements entry added, `RB-PHYSICS-001-FR-073`'s own Non-goals given a forward citation, Change History updated; version bumped to 0.74.0) | `rb_physics_bullet` unit tests (`normalize_dodge_direction_snaps_a_near_axis_aligned_input_to_a_pure_axis` and `normalize_dodge_direction_does_not_snap_a_clearly_diagonal_input` — 2 new tests directly exercising the post-normalization snap). All 320 of `rb_physics_bullet`'s pre-existing tests (as of `FR-073`) pass unchanged (net +2 tests over `FR-073`'s 320, bringing the crate to 322) | snap-near-axis-aligned-dodge commit | Implemented, Verified (unit tests; genuine behavioral fix — zeroes any post-normalization dodge-direction component whose magnitude falls below `DODGE_DIRECTION_SNAP_THRESHOLD` (`0.1`), matching real Rocket League's own confirmed post-`safeNormalized()` zeroing, correcting `FR-073`'s own Non-goals mis-scoping of this as "a separate, independent simplification"; does not close `FR-005`; not yet scored against real data) |
| `RB-PHYSICS-001-FR-075` (confirm `DODGE_DEADZONE` matches RocketSim's own real cancellation threshold — audit finding, documentation only) | `PHASE-1-PHYSICS-CORE` | This spec's own Open Questions section had claimed `DODGE_DEADZONE` "still has no public reference at all... so it may be off by a large factor," and `RB-PHYSICS-001-FR-074`'s own Non-goals (mirroring `FR-073`'s identical earlier claim) had separately framed RocketSim's all-or-nothing dodge-cancellation check as "a real but separate architectural difference" from this port's own independent per-axis `DODGE_DEADZONE` trigger. Both were wrong. Re-examining RocketSim's own confirmed `_UpdateDoubleJumpOrFlip` cancellation check (already fetched and quoted verbatim during `FR-072`/`FR-073`/`FR-074`'s own investigations, not a fresh fetch): `if (abs(controls.yaw + controls.roll) < 0.1f && abs(controls.pitch) < 0.1f) { dodgeDir = {0,0,0}; }` — by De Morgan's law, a dodge fires iff `abs(yaw + roll) >= 0.1 || abs(pitch) >= 0.1`. Since `RB-PHYSICS-001-FR-073`, this port's own `dodge_roll`/`wall_roll` already equal `roll + yaw` combined, and this port's own trigger (`dodge_pitch.abs() > DODGE_DEADZONE || dodge_roll.abs() > DODGE_DEADZONE`) is the identical boolean decision to RocketSim's own real one given `DODGE_DEADZONE == 0.1`, differing only in an unobservable strict (`>`) vs. non-strict (`>=`) comparison at the exact boundary value — a floating-point edge case, not a behavioral or architectural difference. `0.1` was already the real value; it simply hadn't been confirmed as such. No code change: this port's dodge trigger already matched real Rocket League exactly (given `FR-073`'s own prior yaw fold-in); this requirement corrects the record, not the behavior | `crates/rb_physics_bullet/src/drive.rs` (`DODGE_DEADZONE`'s own doc comment corrected), `docs/specifications/physics/RB-PHYSICS-001-physics-core-port.md` (Requirements entry added, this spec's own stale Open Questions bullet corrected, `RB-PHYSICS-001-FR-073`'s and `RB-PHYSICS-001-FR-074`'s own Non-goals given forward citations correcting their "separate architectural difference" framing, Change History updated; version bumped to 0.75.0) | Documentation-only; no new tests. All 322 pre-existing tests (as of `FR-074`) pass unchanged | confirm-dodge-deadzone-reference commit | Implemented, Verified (documentation only, zero code/behavioral change — confirms `DODGE_DEADZONE` (`0.1`) already matches RocketSim's own real cancellation threshold exactly, and that this port's per-axis trigger is the identical boolean decision to RocketSim's own all-or-nothing check once `FR-073`'s own yaw fold-in is in place, correcting `FR-073`'s and `FR-074`'s own Non-goals mischaracterization of this as "a separate architectural difference"; does not close `FR-005`; not yet scored against real data) |
| ADR-0003 | Target Bullet-derived fidelity, defer engine choice | `RB-PHYSICS-001`, `RB-RESEARCH-O001` |
| ADR-0004 | Resolve build-vs-integrate via direct Bullet3 source port | `RB-PHYSICS-001-FR-001..003`, `PHASE-1-PHYSICS-CORE-V0`, `RB-RESEARCH-O001` |
| ADR-0005 | JSON-Lines capture file format and shared `ControllerInput` schema | `RB-VERIFY-002-FR-001..002`, `RB-VERIFY-001-FR-004`, `RB-RESEARCH-O003` |

## Research traceability

| Research item | Feeds | State |
|---|---|---|
| RB-RESEARCH-S001..S006 (settled) | ADR-0001, ADR-0002, ADR-0003, RB-VERIFY-001/002/003 non-goals | Cited, no further action needed |
| RB-RESEARCH-O001 (build vs. integrate) | `RB-PHYSICS-001`, `PHASE-1-PHYSICS-CORE-V0` | Resolved by ADR-0004 |
| RB-RESEARCH-O002 (binary RE) | `SYSTEM-ARCHITECTURE.md` legal/IP boundary | Open — blocked on owner sign-off, not scheduled |
| RB-RESEARCH-O003 (capture tooling scope) | `RB-VERIFY-002` | Resolved by ADR-0005 |
