# Roadmap

Sequential phase gates, not a fixed calendar commitment (part-time project;
rough estimates noted per phase from the original scoping). Each phase's
exit gate is tied to the divergence metric (`RB-VERIFY-003`) where
applicable, per ADR-0002's verification-first ordering.

| Unit | Outcome | Depends on | Specs | Exit gate | Status | Evidence |
|---|---|---|---|---|---|---|
| `PHASE-0-BOOTSTRAP` | Governed repo baseline: charter, architecture, spec tree, ADRs, roadmap, traceability, minimal buildable workspace. | — | all | Docs + workspace present, `cargo fmt/clippy/test` green. | Done | Bootstrap commits on `claude/rocket-league-server-clone-u74q45` |
| `PHASE-0-REPLAY-INGEST` | `rb_replay_ingest` parses real `.replay` files into `PhysicsFrame`s (via `boxcars` + `subtr-actor`). | `PHASE-0-BOOTSTRAP` | RB-VERIFY-001 | RB-VERIFY-001 acceptance criteria met on ≥1 real replay. Met against a vendored third-party fixture (12,029 frames, sane bounds) and validated at scale against 40 of the owner's own real matches via the local `corpus_check` gate (40/40 clean, sane bounds). A stricter manual single-timestamp cross-check remains open (tracked in `RB-VERIFY-001`) but isn't required to close this unit's gate. | Done | `crates/rb_replay_ingest`, 10 tests + `corpus_check` (40/40 real replays) |
| `PHASE-0-CAPTURE-INGEST` | BakkesMod capture tool + `rb_capture_ingest` parse a real offline capture into `PhysicsFrame`s with input attached. | `PHASE-0-BOOTSTRAP` | RB-VERIFY-002 | RB-VERIFY-002 acceptance criteria met on ≥1 real capture. `rb_capture_ingest`'s JSON-Lines parser (ADR-0005) is implemented and tested against a synthetic fixture; the BakkesMod-side plugin that would produce a real capture (FR-001) is not built — blocked on the owner's own Windows/BakkesMod/game environment, same practical constraint as `RB-RESEARCH-O002`. Exit gate not met until a real capture exists. | In Progress | `crates/rb_capture_ingest`, 10 tests (synthetic fixture only) |
| `PHASE-0-EXIT` | End-to-end verification pipeline: real replay + real capture both score via `rb_verify_cli`. | `PHASE-0-REPLAY-INGEST`, `PHASE-0-CAPTURE-INGEST` | RB-VERIFY-003 | Pipeline runs end-to-end and produces a divergence score on ≥1 real replay and ≥1 real BakkesMod capture. The mechanical half is met: `rb_verify_cli::score_replay_against_capture` runs the full ingest-then-score pipeline against a real vendored replay fixture and a capture file without erroring, with ball scoring, car scoring, and real timestamp-tolerant alignment all engaged (6 frames matched by nearest timestamp within tolerance, 6 car pairs matched by `player_id`) — `RB-VERIFY-003`'s three functional requirements are all now implemented. Not fully met: that capture is synthetic, not a real BakkesMod recording (blocked on `PHASE-0-CAPTURE-INGEST`/`RB-VERIFY-002-FR-001`), and the score itself still isn't a meaningful fidelity number (the replay and capture are unrelated matches, and no Phase 1 candidate physics engine exists yet to actually generate a candidate trajectory from recorded inputs). | In Progress | `crates/rb_domain`, 10 divergence tests + 3 `angle_to` tests + `crates/rb_verify_cli`, 3 tests + manual run |
| `PHASE-1-PHYSICS-CORE-V0` | Bullet3-ported rigid-body integration + sequential-impulse solver, scoped to a dynamic sphere (ball) vs. static plane (ground) — per [ADR-0004](../adr/0004-bullet3-source-port-for-physics-core.md), started ahead of `PHASE-0-EXIT` on the strength of Bullet3's direct relevance/licensing. | `PHASE-0-BOOTSTRAP` | RB-PHYSICS-001 | Free-fall/resting/bounce/friction unit tests pass (met); real divergence scoring against `RB-VERIFY-001`/`002` data deferred to `PHASE-0-EXIT` landing. | Done (v0 scope) | `crates/rb_physics_bullet`, 26 tests |
| `PHASE-1-PHYSICS-CORE` | Extend to car (box) rigid bodies, general inertia tensors, multi-contact resolution, ball-vs-car and car-vs-car collision, multi-car `PhysicsWorld` support, driven car input (throttle/steering/boost/handbrake/variable-height-jump/double-jump/flip-cancelable-dodge/dodgeable-wall-jump/air-control/landing-auto-orientation), a modeled arena footprint, and curved fillets throughout its vertical boundary; calibrate constants against real recorded data. Est. 2-4 months part-time total for Phase 1. | `PHASE-1-PHYSICS-CORE-V0`, `PHASE-0-EXIT` | RB-PHYSICS-001 | Divergence score against recorded ground truth reaches a threshold calibrated from the first real scoring run (see RB-VERIFY-003 open questions). Box bodies, general 3x3 inertia, multi-contact resolution, sphere-vs-box (ball-vs-car), box-vs-box (car-vs-car), and body-vs-arena-wall collision, ground-driving car input (throttle/steering), boost, handbrake, a ground jump (variable-height), air control, a double jump (plain or a directional, flip-cancelable dodge), a wall jump (itself dodgeable, same flip-cancel treatment), a gentle landing auto-orientation assist, a modeled octagonal arena footprint plus ceiling (`PhysicsWorld::standard_arena`), curved fillets deflecting the ball (and, since `FR-027`, a car too, via a per-corner technique `FR-032` confirmed mathematically exact for this containment question) throughout the arena's vertical boundary — floor/ceiling seams for all 9 walls (cardinal and diagonal corner), all 8 of the corner walls' own vertical edges (`FR-022`), and all 16 compound corners where a vertical-edge fillet meets a floor- or ceiling-seam fillet (`FR-023`) — and, since `FR-024`, an actual goal-mouth window (with its own 3 rounded edges) cut into each back wall (and, since `FR-028`, a car can drive into a goal too, not just the ball), and, since `FR-025`, a distinctly larger, dedicated radius (`arena::CORNER_ARCH_RADIUS`) for the corner walls' own floor/ceiling-seam arches and the 16 compound-corner fillets that touch them (in place of the cardinal walls' own `arena::FILLET_RADIUS`), and, since `FR-026`, 4 more compound-corner fillets rounding off each goal's own remaining sharp post-crossbar vertex (2 per goal, reusing `arena::FILLET_RADIUS` unchanged, bringing `PhysicsWorld.corner_fillets` to 20 total), and, since `FR-027`, a car (box) actually deflected by every one of those fillets too (`collision::box_vs_quarter_pipe`/`box_vs_corner_fillet`'s per-corner testing technique, since `FR-032` confirmed mathematically exact for this containment question rather than the approximation it was originally believed to be), and, since `FR-028`, a car actually driving into a goal too, and, since `FR-029`, a modeled bounded interior behind each goal window (a solid bounding box — 2 back-of-net planes, 4 `StaticBoundedWall` side walls, 2 `StaticBoundedWall` roofs — stopping the ball or a car that passes through the window instead of letting it sail into unbounded open space), and, since `FR-030`, a combined multi-body solve — every ball-vs-car and car-vs-car contact touching in the same step now shares one interleaved `solver::resolve_dynamic_manifolds` solve instead of a sequence of fully independent pairwise solves — and, since `FR-031`, a constant-calibration audit of every uncalibrated placeholder constant in `drive.rs`/`arena.rs`/`world.rs` against three independent community reverse-engineering references (RocketSim, RLUtilities, the RLBot wiki) — `drive::JUMP_SPEED`/`drive::JUMP_HOLD_ACCELERATION` corrected to their precise real values and a new `drive::UNBOOSTED_MAX_CAR_SPEED` split out from `drive::MAX_CAR_SPEED` (a real behavioral fix: throttle alone could previously reach the boosted top speed), most other constants confirmed already correct, the rest explicitly flagged as still uncalibrated — and, since `FR-032`, a genuine GJK closest-points narrow phase was built and wired in as a replacement for `FR-027`'s per-corner technique, broke two previously-passing tests, and on investigation was found to answer the wrong question for a quarter-pipe/corner-fillet's containment test (is the box's farthest point from the axis at or beyond radius) — the maximum of that convex distance function over a convex box is always attained at one of its 8 corners, never a face's interior, so the original per-corner technique is mathematically exact here, not an approximation; the GJK code (including a new `gjk.rs` module) was reverted in full, `box_vs_quarter_pipe`/`box_vs_corner_fillet` are byte-for-byte unchanged from `FR-027`, and a new empirical regression test (`collision::tests::no_point_on_a_boxs_face_is_ever_farther_from_a_quarter_pipes_axis_than_its_own_corners`) confirms the exactness by dense sampling, and, since `FR-033`, a genuine mass-spring net panel (`net::NetMesh` — a rectangular grid of anchored-perimeter/free-interior point masses, each a real `RigidBody::sphere`, connected by structural and shear springs) now catches the ball at each goal, resolved via a new `collision::sphere_vs_sphere` test plus the existing `solver::resolve_contacts_between` two-body solve, wired in by `arena::standard_nets`/`PhysicsWorld.nets` and stepped after every other contact each frame (a car's own contact against a net was, at the time, a documented non-goal, still passing straight through and stopped by `FR-029`'s pre-existing bounding-box backstop instead — closed since by `FR-038`, see below), and, since `FR-034`, split impulse — every contact's normal row now also solves a second, separate "push" pseudo-velocity channel (`solver::resolve_push_row`/`resolve_two_body_push_row`), fed only by that contact's own positional/penetration (ERP) error, applied directly to the body's position/orientation via a new `solver::apply_push_delta` (mirroring Bullet's own `btSolverBody::writebackVelocity`) instead of folding into the body's real velocity, wired into all three solver entry points (`resolve_contacts`/`resolve_contacts_between`/`resolve_dynamic_manifolds`) with zero call-site changes outside `solver.rs`, and, since `FR-035`, warm-starting for `solver::resolve_dynamic_manifolds` — a new `solver::ContactCache` carries a manifold's converged real-channel impulses (normal plus both friction rows) from one call to the next, matched by each contact's approximate world position (`CONTACT_MATCH_DISTANCE`), and a new `warm_start_two_body_row` seeds that cached impulse directly into the manifold's shared `DeltaVelocity` accumulators before any iteration runs, `PhysicsWorld` persisting the cache map (`dynamic_manifold_caches`) across steps — scoped deliberately to `resolve_dynamic_manifolds` only, since that's the one call site with a documented under-convergence case (`FR-030`'s extreme mass-ratio "sandwiched" body) for warm-starting to demonstrably help; `resolve_contacts`/`resolve_contacts_between` are NOT warm-started (no scenario there yet shows any effect), and warm-starting does NOT fix the separately-documented "bouncy resting contact never settles" limitation, which needs sleeping (still unimplemented) instead, and, since `FR-036`, ball radius (`92.75` to `93.15`, not `91.25`) and `arena::CEILING_Z` (`2044.0` to `2048.0`) were corrected via real source-level research (RocketSim's and RLUtilities' own source, and the current RLBot wiki, read directly rather than guessed at) — the ball's collision radius corrected to the real games' separate, larger collision radius (`93.15`, the mesh's own collision margin), not the smaller `91.25` inertia radius FR-031 had assumed was the only alternative, since this port's single unified radius field has no room for that distinction, and `arena::CEILING_Z` confirmed to share the same reference point as RocketSim's `ARENA_HEIGHT = 2048.f`, resolving both ambiguities `FR-031`'s audit had surfaced but deliberately didn't act on (plus two low-risk doc-comment-only corrections to `arena::CORNER_LENGTH` and `arena::GOAL_DEPTH`, both confirmed exact, values unchanged), and, since `FR-037`, sleeping (`body::RigidBody::update_sleep_state`/`wake`) forcibly zeros a body's velocity once it's stayed below both a linear and an angular threshold for a sustained time, fixing the "bouncy resting contact never settles" limitation warm-starting alone couldn't (see `FR-035`'s own entry) — a car wakes unconditionally on any genuinely active input, before that input's own force has had a chance to move it, so an asleep car can always start moving again, and, since `FR-038`, `net::NetMesh::step` takes every body that can touch the net (the ball and every car, via a `&mut [RigidBody]` slice) instead of the ball alone, closing this port's own former "a car still passes through untouched" Non-goal — no new collision code was needed, since `collision::contacts_between` already dispatches box-vs-sphere for a car against a net point the same way it always has for ball-vs-car, and, since `FR-039`, a wall jump at a corner (a car touching two walls at once) pushes off along every touched wall's normal summed and normalized, instead of picking whichever wall came first in `PhysicsWorld.walls` — closes the "first wall in `self.walls`" simplification `FR-013` originally documented and `FR-019`'s own new diagonal corner walls made reachable in the standard arena for the first time; no new collision-detection code was needed, since `resolve_plane_contact` already resolved a car touching two walls simultaneously correctly — only the wall-jump push-off direction picker, `drive::apply_driven_forces`'s own input, was affected, and, since `FR-040`, a dedicated documentation-only research pass investigated (without implementing) the two placeholder constants `FR-036` deliberately left untouched (`arena::FILLET_RADIUS`/`CORNER_ARCH_RADIUS`) — see "Not met", below, for the finding), and, since `FR-041`, `solver::resolve_dynamic_manifolds`'s shared-body impulse scaling — a parameter-free `1 / k` per-manifold velocity-row scale for a body touched by `k >= 2` manifolds this step, adopted after a naive global SOR-style over-relaxation factor was tried first and found to provably diverge `FR-030`'s own symmetric-pinch scenario for any `omega > 1.0` (worse than the pre-`FR-030` independent-pairwise approach, not just under-converged), while `omega < 1.0` only ever improved it, matching standard PGS/SOR theory — narrows that scenario's combined-solve result from ~89.5 to ~32 units/s (independent-pairwise stays ~98.9) at zero added iteration cost, without achieving full within-one-call convergence (see "Not met", below), and, since `FR-042`, `box_vs_box`'s edge-edge contact point and face-clipping degenerate ("zero points") fallback — both previously "reasonable, tested choices" never checked against the reference — were validated directly against Bullet's own `btBoxBoxDetector::dBoxBox` source, fetched and read rather than guessed at: the edge-edge contact point (`closest_points_on_segments`) is confirmed strictly more rigorous than the reference's own unclamped-infinite-line `dLineClosestApproach`, and the face-clipping fallback's choice to synthesize a clamped-center contact rather than drop the collision outright (as the reference's own identical, equally undocumented "should never happen" branch does with `return 0`) is confirmed a deliberate, favorable divergence rather than an unproven guess; a candidate fix for `edge_contact`'s tangent sign-selection heuristic (swapping the center-to-center vector for the SAT-resolved normal, matching the reference's own approach) was built and empirically tested against a brute-force ground truth across 50,000 randomized two-box configurations and found genuinely regime-dependent (current heuristic better at large/arbitrary penetration depths, ~11.6% vs. ~8.7% optimal-match rate; candidate better under realistic sub-0.5-unit depths, ~93% vs. ~77%), so not adopted (see "Not met", below) — zero production code changed, only doc comments — and, since `FR-043`, this spec's own prior claim that Bullet's default restitution/friction combine mode is `btMax` was checked directly against Bullet's real `btManifoldResult.h`/`btManifoldResult.cpp` reference source (fetched and read in full, matching `FR-036`/`FR-042`'s own method) and found wrong — the real default for both restitution and friction is an unclamped product (`a * b`; friction additionally clamps to `[-10, 10]`), with no `max` mode anywhere in the reference — `solver::combine_restitution`/`combine_friction`'s own average (`(a + b) * 0.5`) is unchanged, now justified by a correct reason instead of the now-corrected wrong one: unlike the reference's product, average preserves the identity `combine(a, a) == a`, which matters since most bodies in this port currently share the same uncalibrated placeholder `0.5` coefficient for both restitution and friction; whether either formula matches real Rocket League itself remains genuinely open, unaffected by this correction; zero production code changed, only doc comments and 2 new dedicated `solver.rs` tests (see "Not met", below), and, since `FR-044`, this spec's own top-level "Non-goals (this increment)" section was found still carrying a stale "Split impulse" bullet (claiming this port always takes Bullet's non-split contact-resolution branch), contradicted by `FR-034`'s own already-shipped implementation — confirmed still genuinely present via `solver::resolve_push_row`/`resolve_two_body_push_row`/`apply_push_delta` directly in `solver.rs`, and confirmed via a repo-wide grep that this was the only stale occurrence anywhere in code or docs — corrected to a strikethrough-and-close note, matching the same convention this section already uses for its own two other resolved Non-goals items (the wall-jump-corner disambiguation closed via `FR-039`; the curved-geometry Non-goal closed progressively via `FR-026` through `FR-033`); zero production code changed, no new tests, and, since `FR-045`, `integrate.rs`'s own doc comments claiming close fidelity to three real Bullet functions (`btRigidBody::applyDamping`, `btRigidBody::integrateVelocities`, `btTransformUtil::integrateTransform`) were checked directly against Bullet's real `btRigidBody.cpp`/`.h`, `btTransformUtil.h`, `btQuaternion.h`, and `btScalar.h` reference source (matching `FR-036`/`FR-042`/`FR-043`'s own method) and confirmed byte-for-byte accurate in every formula and constant; a minor numeric difference (this port's `1e-12` degenerate-quaternion epsilon vs. Bullet's real `SIMD_EPSILON`/`FLT_EPSILON`, ~1.19e-7, about 5 orders of magnitude larger) was found and not adopted, behaviorally indistinguishable for every reachable scenario, and `integrate_transform`'s own check-then-normalize fallback branch was confirmed load-bearing, not defensive theater — it mirrors Bullet's own real fallback choice to preserve a body's prior orientation on a degenerate predicted quaternion rather than reset to identity, a distinction an unconditional call to `rb_domain::Quat::normalize` would have silently gotten wrong; 1 new test added pinning this exact distinction, zero production code changed, and, since `FR-046`, `body.rs`'s `Shape::local_inertia`/`RigidBody::update_inertia_tensor` and `mat3.rs`'s `Mat3::scaled_columns`/`Mat3::from_quat` were checked directly against Bullet's real `btSphereShape.cpp`, `btBoxShape.cpp`, `btRigidBody.cpp`/`.h`, and `btMatrix3x3.h` reference source (matching `FR-036`/`FR-042`/`FR-043`/`FR-045`'s own method) and confirmed byte-for-byte accurate in every formula checked (sphere/box local-inertia, `update_inertia_tensor`, `Mat3::scaled_columns`); a genuine difference was found in `Mat3::from_quat` and not adopted — it hardcodes an `s = 2` factor assuming an exactly unit-length input quaternion, while the reference's own `btMatrix3x3::setRotation` computes `s = 2 / q.length2()` to self-correct a non-unit-length input, not adopted since this function's only production call site (`RigidBody::update_inertia_tensor`) always receives an already-renormalized orientation (an invariant `integrate::integrate_transform` maintains, per `FR-045`'s own finding), making the reference's own self-correction unreachable defensive theater here; 1 new test added pinning this exact distinction, zero production code changed), and, since `FR-047`, `collision.rs`'s remaining closed-form shape pairings (`sphere_vs_plane`, `box_vs_plane`, `sphere_vs_box`, `sphere_vs_sphere` — `box_vs_box` already covered by `FR-042`) were checked directly against Bullet's real `btConvexPlaneCollisionAlgorithm.cpp`/`.h`, `btSphereBoxCollisionAlgorithm.cpp`, `btSphereSphereCollisionAlgorithm.cpp`, and `btManifoldPoint.h` reference source (matching `FR-036`/`FR-042`/`FR-043`/`FR-045`/`FR-046`'s own method) — `sphere_vs_plane` and `sphere_vs_sphere` confirmed exact, byte-for-byte matches to Bullet's real algorithms; `sphere_vs_box`'s deep-penetration face-selection logic confirmed to reproduce Bullet's own exact `+x, -x, +y, -y, +z, -z` face-check tie-break order (first-checked-wins-ties), not just a mathematically-equivalent alternative, pinned by 1 new test; a genuine, deliberate divergence was found in `box_vs_plane` and not adopted — real Bullet's default config only generates one contact point per frame (a single GJK support-vertex query), relying on several frames of persistent-manifold accumulation to reach a resting box's full 4-corner manifold, whereas this port's `box_vs_plane` computes all 4 corners exactly in a single pass, confirmed a more rigorous simplification in the same spirit as `box_vs_box`'s own `FR-042` finding; zero production code changed, 1 new test added, and, since `FR-048`, `solver.rs`'s constraint-row setup/resolve functions (`restitution_curve`, `plane_space`, `setup_rows`, `resolve_row`) and `btContactSolverInfo`'s own cited default constants were checked directly against Bullet's real `btSequentialImpulseConstraintSolver.cpp`/`.h`, `btContactSolverInfo.h`, and `btVector3.h` reference source (matching `FR-036`/`FR-042`/`FR-043`/`FR-045`/`FR-046`/`FR-047`'s own method) — `plane_space` confirmed byte-for-byte exact against real `btPlaneSpace1`; `restitution_curve` confirmed behaviorally exact, folding real Bullet's own call-site clamp (its one caller clamps a non-positive result to exactly `0` before use) inline via `.max(0.0)` instead of as a separate step, a confirmed-equivalent restructuring, not a divergence; `setup_rows` confirmed exact against real `setupContactConstraint`/`setupFrictionConstraint` (also corrected a stale doc-comment citation — the doc previously cited a differently-named, unrelated function `setFrictionConstraintImpulse`, which in real Bullet only resets a cached impulse to zero; the actual friction-row-setup function is `setupFrictionConstraint`, reached via `addFrictionConstraint`'s own default `desiredVelocity = 0`/`cfmSlip = 0` arguments); `resolve_row` confirmed a behaviorally-equivalent unification of real Bullet's two separate resolve functions (`resolveSingleConstraintRowLowerLimit` for the normal row, `resolveSingleConstraintRowGeneric` for friction rows), since the normal row's own `upper_limit` (`1e10`, matching Bullet's own value exactly) makes the extra upper-bound check this port always runs unreachable there; all 6 of `btContactSolverInfo`'s cited default constants (`ERP2 = 0.2`, `GLOBAL_CFM = 0.0`, `LINEAR_SLOP = 0.0`, `RESTITUTION_VELOCITY_THRESHOLD = 0.2`, `RELAXATION = 1.0`, `SOLVER_ITERATIONS = 10`) confirmed exact against real `btContactSolverInfoData`'s own constructor; one genuine, significant divergence was found and not adopted (see "Not met", below) — zero production code changed, only doc comments in `solver.rs`, 1 new test added, and, since `FR-049`, that exact divergence was closed — a new `solver::friction_directions(normal, relative_velocity)` helper aligns friction direction 1 with the tangential component of the current relative sliding velocity itself (matching real Bullet's own default), with direction 2 completing a right-handed orthonormal basis via `dir1.cross(normal)`, falling back to the old `plane_space` fixed basis in two cases — negligible tangential velocity (matching real Bullet's own `SIMD_EPSILON` threshold) and a second, genuinely new case found while implementing this fix, a near-head-on-collision catastrophic floating-point cancellation that can produce a degenerate tangential vector, which real Bullet's own unguarded `normalize()` would silently mishandle but this crate's own `Option`-returning `Vec3::normalize()` instead falls back gracefully from — wired into both `setup_rows` (one-body contacts) and `setup_two_body_rows` (two-body contacts), with 3 new tests added, including a dedicated isotropic-friction regression test verified to fail under the old fixed-basis behavior, proving the fix has real behavioral bite, and, since `FR-050`, `net::NetMesh::step`'s own similarly independent-pairwise resolution of every body-vs-net-point contact was investigated against the same order-dependent under-convergence bug `FR-030` already fixed for ball-vs-car/car-vs-car contacts — `net.rs`'s own doc comment had waved this off as irrelevant since a net point's mass is "tiny enough" relative to a real ball or car, an untested claim found false: `NET_POINT_MASS` (`0.5`) is exactly half a typical ball's own mass (`1.0`), not a lopsided ratio, and a ball or car pressing into the net commonly overlaps two or more free net points at once given `NET_POINT_RADIUS`'s generous coverage-radius sizing; a dedicated single-shot test confirmed the mechanism genuinely order-dependent (a ball placed exactly symmetrically between two net-point-like bodies is left with a nonzero, purely arbitrary sideways velocity whose sign flips with iteration order), and a `NetMesh::step`-level test measured the real-world bias directly — a ball fired squarely at the net's own center, straddling two symmetric free interior points, was deflected sideways by ~0.25 units/s out of a 2000 units/s impact under the old sequential loop; fixed by adopting `solver::resolve_dynamic_manifolds`'s combined multi-body solve for every body-vs-point contact detected within a sub-step instead of resolving each pair immediately and independently, measured to reduce that same residual from ~0.25 to ~0.016 units/s, roughly a 15-fold improvement; warm-starting was deliberately not added — `NetMesh::step` still passes a fresh, empty `ContactCache` every sub-step, cold-starting every call exactly as before — left as the same kind of open follow-up work `FR-035` already scoped out for `resolve_contacts`/`resolve_contacts_between` generally, and, since `FR-051`, `PhysicsWorld::step`'s own similarly independent-pairwise resolution of a body's contact against each different static shape type (the ground, every wall, every curve, every corner fillet, every goal wall, every bounded wall — one independent `solver::resolve_contacts` call per shape, fully resolved before the next shape's setup even reads the body's velocity) was investigated against the same order-dependent under-convergence bug `FR-030`/`FR-050` already fixed for dynamic-vs-dynamic and net-point contacts, this time for a body touching two different static surfaces at once (a car driving near a wall close to the ground, or wedged into a corner, a routine gameplay occurrence, not an edge case); a dedicated single-shot test confirmed the mechanism genuinely order-dependent — a ball wedged symmetrically into a corner formed by two static walls, moving diagonally into both at once, is left biased toward whichever wall is resolved last (mirror-image results depending on order) instead of the true, by-symmetry equal-component answer; fixed by a new `solver::resolve_static_manifolds`, generalizing `resolve_contacts` to combine every static-shape manifold a body touches into one shared solve (each manifold group still computes its own combined restitution/friction against the body's own material properties, but all groups now share one DeltaVelocity/push-delta accumulator across the whole `SOLVER_ITERATIONS` loop), wired into `PhysicsWorld::step` via a new `StaticScene` struct (bundling the six static-shape slices into one parameter to stay under clippy's `too_many_arguments` limit) and a new `resolve_static_contacts` function that gathers every one of a body's contacts across every static shape into one manifold list and resolves them all together — replacing the old five separate per-shape-type helper functions (`resolve_plane_contact`, `resolve_curve_contact`, `resolve_corner_fillet_contact`, `resolve_goal_wall_contact`, `resolve_bounded_wall_contact`, all removed) and their sequential per-shape call sequence in `step`, for both the ball and every car; a `PhysicsWorld::step`-level test (a ball fired diagonally into a real two-wall corner via an actual `PhysicsWorld` with two `with_wall` calls) confirmed the fix at the real public API, verified to fail under the old sequential per-shape loop before the rewire, and, since `FR-052`, `PhysicsWorld::step`'s own remaining boundary between `FR-051`'s own two already-combined solves was investigated and closed — a body's static-shape manifolds (`solver::resolve_static_manifolds`) and its dynamic-vs-dynamic manifolds (`solver::resolve_dynamic_manifolds`) were still resolved via two separate sequential calls in `step`, a body's static contacts fully resolved and applied before the dynamic solve's own setup for that same body ever read the result, and a dedicated single-shot test (reusing `FR-051`'s own symmetric two-wall corner setup, with one wall swapped for a geometrically identical very-heavy dynamic body routed through the dynamic-manifold path instead) confirmed the same order-dependent bias `FR-030`/`FR-050`/`FR-051` already fixed within each channel separately reappears at the boundary between them; fixed by a new `solver::resolve_manifolds` folding a step's static and dynamic manifolds into one shared solve, every body's own static rows and dynamic-manifold rows now sharing one DeltaVelocity/push-delta accumulator for the whole `SOLVER_ITERATIONS` loop (`FR-041`'s own `1/k` shared-body relaxation deliberately kept scoped to dynamic manifolds only, after extending it to static rows was tried and found to regress `FR-051`'s own two-static-wall test's convergence), wired into `PhysicsWorld::step` by renaming `resolve_static_contacts` to `static_contact_manifolds` (now gathering a body's static manifold groups instead of resolving them directly) and replacing `step`'s two separate combined-solve calls with one `solver::resolve_manifolds` call; a `PhysicsWorld::step`-level test (a ball fired diagonally into a real wall-and-heavy-car corner, one real `StaticPlane` wall via `with_wall`, one real heavy-mass car via `with_car`) confirmed the fix at the real public API, settling with nearly equal x/y velocity components, verified to fail under the old two-call sequence before the rewire, and, since `FR-053`, `btManifoldResult.cpp` was re-fetched and re-read directly to confirm one further detail `FR-043`'s own investigation didn't separately examine — real Bullet's `calculateCombinedFriction` additionally clamps its product result to `[-10.0, 10.0]` (`calculateCombinedRestitution` has no such clamp) — confirmed currently inert for every friction coefficient this crate itself ever actually sets (all in `0.1..=0.9`, nowhere near either bound) but adopted anyway for reference conformance, since every relevant `friction` field is a public, unvalidated `f32` with no range enforcement anywhere in the crate today; `solver::combine_friction` now clamps its own average result to `[-10.0, 10.0]` (a clamp addition only, not a formula change — `FR-043`'s own average formula is unchanged), while `solver::combine_restitution` stays unclamped, matching the reference's own choice not to clamp restitution — and, since `FR-054`, `collision::box_vs_goal_wall`'s own remaining open question from `FR-028` (whether a car face resting flush against the goal window's own edge could read as clear on every corner while its middle already overlapped the window) was resolved via a convex-hull argument distinct from `FR-032`'s own — a box's touching face is the convex hull of whichever corners penetrate the plane, so "every corner outside the window" is exactly equivalent to "the face doesn't fully fit through it", confirming no bug exists — while the same investigation found the structurally-identical `collision::box_vs_bounded_wall`'s (`FR-029`) own mirror-image case (a face bigger than a bound and centered on it) *is* a genuine under-detection gap, confirmed unreachable given this project's own car/ball sizes against the standard arena's own bound sizes and documented as a new Non-goal rather than fixed, and, since `FR-055`, `arena::GOAL_HALF_WIDTH`/`arena::GOAL_HEIGHT` (carrying a "commonly-cited community number, not independently confirmed" caveat since `FR-024`) were confirmed exact against the current RLBot wiki's own "Useful Game Values" page (the same page `FR-036`'s own research already used to confirm `arena::GOAL_DEPTH`), a sourcing-status upgrade only, no value changed, and this spec's own "Open questions" section was corrected where it still described `arena::GOAL_DEPTH` as an unconfirmed "uncalibrated invention" (contradicting `FR-036`'s own already-shipped entry), now stating all three goal-geometry constants (`GOAL_HALF_WIDTH`, `GOAL_HEIGHT`, `GOAL_DEPTH`) are confirmed, leaving only `arena::NET_DEPTH` (`FR-033`) open in that vicinity, and, since `FR-056`, `drive::BOOST_ACCELERATION` — a single flat constant (`991.667`) applied identically by `apply_driven_forces` whether its own `on_ground` parameter was true or false, despite this port's own doc comments (including `RB-PHYSICS-001-FR-008`'s own Requirements entry) explicitly claiming boost "works identically airborne" — was checked against RocketSim's own `RLConst.h` (fetched directly, matching `FR-036`/`FR-042`/`FR-043`/`FR-045`/`FR-046`/`FR-047`/`FR-048`/`FR-053`'s own method) and found that claim itself wrong: the reference defines two distinct constants, `BOOST_ACCEL_GROUND = 2975.f / 3.f` (matching this port's existing flat value) and `BOOST_ACCEL_AIR = 3175.f / 3.f` (about 6.5% higher), a genuine ground/air split this port's single-constant model didn't capture, meaning every airborne boost this crate ever applied understated real Rocket League's own airborne boost strength; fixed by splitting `BOOST_ACCELERATION` into `BOOST_ACCELERATION_GROUND` (unchanged value, rewritten as the exact `2975.0 / 3.0` fraction) and a new `BOOST_ACCELERATION_AIR` (`3175.0 / 3.0`), wired into `apply_driven_forces`'s existing `on_ground` parameter to select between them (no new gating logic — boost already applied in both cases, only the magnitude changed), with every doc comment that had claimed the two were identical (this module's own, `RB-PHYSICS-001-FR-008`'s own Requirements entry, and this spec's own Non-goals/Open-questions mentions) corrected; the same fetch also confirmed `BOOST_CONSUMPTION_RATE`/`MAX_BOOST` already match RocketSim's own `BOOST_USED_PER_SECOND = BOOST_MAX / 3`, and, since `FR-057`, a hard ceiling on car angular speed — nothing previously bounded how fast sustained air control torque (or a dodge's own kick, or the landing-orientation assist) could spin a car — was added after a second fetch of RocketSim's own `RLConst.h` (matching `FR-036`/`FR-042`/`FR-043`/`FR-045`/`FR-046`/`FR-047`/`FR-048`/`FR-053`/`FR-056`'s own method), this time targeting every `drive.rs` constant this crate's own doc comments flagged as having "no public reference at all", which surfaced `CAR_MAX_ANG_SPEED = 5.5f` (rad/s), a hard "car can never exceed this angular velocity" ceiling; added `drive::MAX_CAR_ANGULAR_SPEED = 5.5` and a new `drive::clamp_angular_speed`, a genuine clamp scaling `RigidBody.angular_velocity` back down to the cap (preserving direction) when exceeded, unlike `MAX_CAR_SPEED`/`UNBOOSTED_MAX_CAR_SPEED`, which only ever gate new throttle/boost force and never reduce velocity already past their own cap, wired in once per step immediately after `integrate::integrate_velocities` in both `world.rs`'s production `drive_and_integrate_velocities` and `drive.rs`'s own test helper `step_with_input_and_dodge_flip`; several other real constants the same fetch surfaced (dodge per-direction impulse scaling, auto-flip/landing-assistance thresholds, a ramping powerslide model, a steering-torque mapping, RocketSim's own per-axis `CAR_AIR_CONTROL_TORQUE`) were considered and explicitly not adopted, either needing further investigation or repeating `FR-031`'s own "false precision" finding for torque constants calibrated against a car body/inertia tensor this port's own placeholder doesn't match; `drive::DODGE_ANGULAR_SPEED`'s pre-existing numeric equality to this same `5.5` value is flagged as a documented coincidence, not a second confirmation of it, with 3 new tests added (`clamp_angular_speed_is_a_no_op_below_the_cap`, `clamp_angular_speed_scales_an_over_cap_velocity_down_to_the_cap_preserving_direction`, `sustained_full_roll_input_never_exceeds_the_hard_angular_speed_cap`), and, since `FR-058`, a real speed-dependent throttle taper — `drive::THROTTLE_ACCELERATION`'s own doc comment had named this exact gap since it was first introduced, applying full flat acceleration right up to a hard cutoff at `UNBOOSTED_MAX_CAR_SPEED` instead of a genuine taper — was added after fetching RocketSim's own `Car.cpp` (not just `RLConst.h`'s constants) to find exactly how its own `THROTTLE_TORQUE_AMOUNT` is actually used, surfacing the real mechanism: drive force is scaled by `DRIVE_SPEED_TORQUE_FACTOR_CURVE`, a confirmed 3-point piecewise-linear curve (`{0, 1.0}, {1400, 0.1}, {1410, 0.0}`), not applied flat; `THROTTLE_TORQUE_AMOUNT` itself is expressed in Bullet's own internal units and doesn't transfer cleanly to this port's own differently-calibrated car body (repeating `FR-031`'s and `FR-057`'s own "false precision" finding), but the curve's shape is a pure, unitless ratio that transfers regardless, the same reasoning `FR-057` used to adopt `MAX_CAR_ANGULAR_SPEED`; fixed by adding `drive::DRIVE_SPEED_TAPER_BREAKPOINTS`/`drive::drive_speed_taper` (a small piecewise-linear interpolator evaluated against this port's own pre-existing signed `throttle.signum() * forward_speed` quantity, not RocketSim's own direction-agnostic `abs(forward speed)`, a separate behavioral question left out of scope) and rewiring `apply_driven_forces`'s throttle block to scale `THROTTLE_ACCELERATION` by the taper instead of gating on a hard speed threshold; `THROTTLE_ACCELERATION`'s own peak magnitude (`1600.0`) is unchanged, still an uncalibrated placeholder — only the curve's real shape is adopted, with 2 new tests added (`drive_speed_taper_matches_the_real_curve_breakpoints_exactly`, `throttle_acceleration_tapers_well_before_reaching_unboosted_max_speed`), and, since `FR-059`, real forward-speed-dependent dodge impulse scaling — `FR-031`'s own audit had already found real Rocket League's dodge impulse has "direction/speed-dependent scaling" but couldn't adopt it without the actual formula, only `RLConst.h`'s bare constants — was added after fetching RocketSim's own `Car.cpp` (`_UpdateDoubleJumpOrFlip`, the same file/technique `FR-058` used for the throttle taper), surfacing the real mechanism: a dodge's base impulse scales per-axis by `((maxSpeedScale - 1) * forwardSpeedRatio) + 1`, where `maxSpeedScale` is `1.0` for a forward dodge (no change, ever), `2.5` for a backward dodge (opposing the car's own current velocity direction), or `1.9` for any side/roll dodge; adopted the confirmed real ratios (`drive::DODGE_BACKWARD_SPEED_SCALE = 2.5`, `drive::DODGE_SIDE_SPEED_SCALE = 1.9`), not the real base magnitude (`FLIP_INITIAL_VEL_SCALE = 500.0`) — since the real forward-dodge scale is exactly `1.0`, `DODGE_SPEED`'s own existing (still-uncalibrated) value doubles as that unscaled case unchanged, the same "shape confirmed, magnitude not" split `FR-058` used for `THROTTLE_ACCELERATION`; added `drive::dodge_speed_scale` and `drive::dodge_pitch_is_backward` (the real backward classification, re-derived in this port's own pitch-sign convention, with a `drive::DODGE_BACKWARD_CLASSIFICATION_SPEED_THRESHOLD = 100.0` low-speed fallback) and rewired both `apply_driven_forces` dodge sites (the ground-dodge block and its wall-jump-dodge variant) to scale `DODGE_SPEED` by this logic before applying it; every pre-existing dodge test dodges from a standing start, where the new scale evaluates to `1.0` regardless of direction, a zero-regression-risk property confirmed by inspection before implementation, with 5 new tests added (`dodge_speed_scale_matches_the_real_curve`, `dodge_pitch_is_backward_matches_the_real_classification`, `a_backward_dodge_scales_up_with_current_forward_speed`, `a_forward_dodge_does_not_scale_with_current_forward_speed`, `a_side_dodge_scales_up_with_current_forward_speed`), and, since `FR-060`, this spec's own `FR-057` Non-goals had left open whether RocketSim's auto-flip constants (`CAR_AUTOFLIP_IMPULSE/TORQUE/TIME/NORMZ_THRESH/ROLL_THRESH`) could map onto this port's own `drive::LANDING_AUTO_UPRIGHT_TORQUE` — resolved (audit finding, documentation only) by fetching and reading RocketSim's actual `Car.cpp` source directly (matching `FR-058`/`FR-059`'s own method) and finding no such continuous, input-free, airborne mechanic exists in real Rocket League at all, only two distinct, real, *grounded*, input-gated systems (auto-flip, a turtle-recovery flip gated on ground contact plus roll; and auto-roll, a continuous ground-alignment torque gated on throttle plus wheel contact), neither matching this port's own mechanic — a "these don't map, confirmed" negative finding, not a value change; `drive.rs`'s own module doc comment (the "Landing auto-orientation assistance" section and its "commonly-cited constants" paragraph) and this spec's own Open Questions/`FR-057` Non-goals text corrected accordingly; zero production code changed, no new tests, and, since `FR-061`, real hard caps on the ball's own linear and angular speed — nothing previously bounded either, unlike the car's own `drive::MAX_CAR_ANGULAR_SPEED` (`FR-057`) — were added after fetching and reading RocketSim's real `RLConst.h` and `Ball.cpp` source directly (matching `FR-036`/`FR-042`/`FR-043`/`FR-045`/`FR-046`/`FR-047`/`FR-048`/`FR-053`/`FR-056`/`FR-057`'s own method), finding two confirmed real hard caps enforced inside RocketSim's own `_FinishPhysicsTick()` (called after collision resolution, at the end of the physics tick) via a hard clamp (`if (vel.length2() > max*max) vel = vel.normalized() * max`): `BALL_MAX_SPEED = 6000.f` (uu/s) and `BALL_MAX_ANG_SPEED = 6.f` (rad/s, "Ball can never exceed this angular velocity") — a genuine behavioral fix, since the ball previously had no speed cap of any kind; fixed by adding `world::BALL_MAX_SPEED = 6000.0` and `world::BALL_MAX_ANG_SPEED = 6.0` and a new private `world::clamp_ball_velocity`, generalizing `FR-057`'s own `drive::clamp_angular_speed` shape (car-specific) to both linear and angular speed for the ball, placed in `world.rs` rather than `drive.rs` since the ball has no drive-input-gated mechanic of its own, wired into `PhysicsWorld::step` right after that step's own contact resolution (matching RocketSim's own "after collision resolution, end of tick" placement more precisely than `drive::clamp_angular_speed`'s own mid-pipeline placement for the car); `BALL_DRAG = 0.03f` ("net-velocity drag multiplier") was explicitly not adopted — RocketSim sets it once at ball construction as a per-match mutator-config default, not a hardcoded system invariant like the two speed caps, and this port's own `RigidBody::sphere` constructor takes no opinion on a "real" ball's own damping default, left for a future requirement; 4 new tests added (`clamp_ball_velocity_is_a_no_op_below_both_caps`, `clamp_ball_velocity_scales_an_over_cap_linear_velocity_down_to_the_cap_preserving_direction`, `clamp_ball_velocity_scales_an_over_cap_angular_velocity_down_to_the_cap_preserving_direction`, `a_ball_launched_far_past_ball_max_speed_never_exceeds_it_after_a_step`), bringing the crate to 306 — are all implemented, unit-tested, and wired into a real N-car `PhysicsWorld` (`RB-PHYSICS-001-FR-004`/`FR-006`/`FR-007`/`FR-008`/`FR-009`/`FR-010`/`FR-011`/`FR-012`/`FR-013`/`FR-014`/`FR-015`/`FR-016`/`FR-017`/`FR-018`/`FR-019`/`FR-020`/`FR-021`/`FR-022`/`FR-023`/`FR-024`/`FR-025`/`FR-026`/`FR-027`/`FR-028`/`FR-029`/`FR-030`/`FR-031`/`FR-032`/`FR-033`/`FR-034`/`FR-035`/`FR-036`/`FR-037`/`FR-038`/`FR-039`/`FR-040`/`FR-041`/`FR-042`/`FR-043`/`FR-044`/`FR-045`/`FR-046`/`FR-047`/`FR-048`/`FR-049`/`FR-050`/`FR-051`/`FR-052`/`FR-053`/`FR-054`/`FR-055`/`FR-056`/`FR-057`/`FR-058`/`FR-059`/`FR-060`/`FR-061`/`FR-062`, complete). Not met: real-data constant calibration (`FR-005`) — explicitly NOT closed by `FR-031`'s audit, which only sourced constants against community reverse-engineering references, not this project's own recorded ground truth, and NOT touched by `FR-032`, `FR-033`, `FR-034`, `FR-035`, or `FR-036` either; still blocked on `PHASE-0-EXIT` (no real BakkesMod capture exists yet); a genuine net mesh for the ball is no longer an open item — `FR-033`'s `net::NetMesh` now catches the ball at each goal with real spring/damping physics (a car's own contact against a net was, at the time, a documented non-goal, unaddressed by `FR-033` — closed since by `FR-038`, which extended `NetMesh::step` to catch every car too); a genuine convex-vs-curved-surface narrow phase for a car in place of `FR-027`'s per-corner technique is no longer an open item — `FR-032` investigated it and found none is needed, since the per-corner technique is exact for this containment question; resolving deep penetration injecting spurious velocity into a body is no longer an open item — `FR-034`'s split-impulse push channel now applies that correction directly to position instead; warm-starting is no longer an open item either — `FR-035` closes it for `resolve_dynamic_manifolds`; sleeping is no longer an open item either — `FR-037` closes it, forcibly zeroing a body's velocity once it's stayed below both a linear and an angular threshold for a sustained time, the actual fix for a bouncy (restitution > 0) resting contact never settling that neither split impulse (`FR-034`) nor warm-starting (`FR-035`) alone could provide; a wall jump at a corner picking only one of two touched walls' normals is no longer an open item either — `FR-039` closes it, summing and normalizing every touched wall's normal instead of `Iterator::find`-ing the first match, so a corner wall jump now pushes diagonally away from the corner; full convergence of `FR-030`'s combined solve for an extreme mass-ratio "sandwiched" body at the crate's current `SOLVER_ITERATIONS = 10` — measurably better warm-started across calls per `FR-035`, and, since `FR-041`, further narrowed within a single call too via a parameter-free `1 / k` shared-body impulse scale (that scenario's combined-solve result narrows from ~89.5 to ~32 units/s; independent-pairwise stays ~98.9), but still not fully closed — real recorded multi-car contact data would still be needed to know whether the remaining residual error matters for fidelity in practice, or whether raising `SOLVER_ITERATIONS` itself (a real added per-step cost `FR-041` deliberately declined to adopt without such data) is worth paying — see spec's Non-goals and Open questions; `arena::FILLET_RADIUS`/`CORNER_ARCH_RADIUS` remain untouched by `FR-036` and still genuinely uncalibrated; `FR-040` ran a dedicated follow-up search (RocketSim/RLUtilities source, the RLBot wiki, RLGym's own game-value list) and found no adoptable value — the RLBot wiki's sole candidate ("Wall bottom ramp radius: Aprox. 256 (but they are not circular)") carries no citation, doesn't distinguish the two constants' distinctly different radii, explicitly disclaims being a true circular arc, and shares its numeral with RLGym's own unrelated `RAMP_HEIGHT` constant (a ramp's height from the ground, not a curve's radius) — so `256` was deliberately not adopted and both constants remain unchanged (`292.0`/`750.0`); genuinely closing this still needs actual extracted collision-mesh geometry (e.g. `ZealanL/RLArenaCollisionDumper`'s real triangle-mesh dump), blocked on the owner's own Windows/Rocket League environment, the same blocker `RB-VERIFY-002-FR-001` already documents; `box_vs_box`'s `edge_contact` tangent sign-selection heuristic (which of a box's 4 candidate parallel edges is "near") remains an open item too — `FR-042` built and empirically tested a normal-based candidate fix against a brute-force ground truth and found the result genuinely mixed by penetration-depth regime, so it wasn't adopted; a truly rigorous fix needs a non-heuristic nearest-edge-pair selection, not a swap between two different imperfect heuristics, and still awaits either real recorded car-vs-car contact data or a concrete visible-artifact motivation to justify the added complexity; which restitution/friction combine mode (if either) matches real Rocket League is also still open — `FR-043` only corrected this spec's own wrong reference-fact claim about Bullet's default (`btMax` is not real; an unclamped product is), it did not calibrate this port's own average choice against `RB-VERIFY-001`/`RB-VERIFY-002` data, and stays blocked on `PHASE-0-EXIT` like every other real-data calibration question; `integrate.rs`'s own degenerate-quaternion epsilon threshold (`1e-12`) also stays open in the same sense — `FR-045` found it differs from Bullet's real `SIMD_EPSILON`/`FLT_EPSILON` by about 5 orders of magnitude but deliberately didn't adopt the reference value, for lack of a concrete reason beyond "closer to the reference," and this too does not touch `FR-005`, still blocked on `PHASE-0-EXIT`; `Mat3::from_quat`'s own hardcoded `s = 2` factor stays open in the same reachability sense — `FR-046` found it differs from Bullet's real, self-correcting `s = 2 / q.length2()` (`btMatrix3x3::setRotation`), but deliberately didn't adopt it since this function's only production call site always receives an already-renormalized orientation, making the reference's own self-correction unreachable here, and this too does not touch `FR-005`, still blocked on `PHASE-0-EXIT`; `box_vs_plane`'s own single-contact-plus-persistence divergence from real Bullet is confirmed favorable, not an open item — `FR-047` found it the same deliberate, more-rigorous simplification `FR-042` already established for `box_vs_box`, so no fix is owed; `sphere_vs_box`'s and `sphere_vs_sphere`'s own numeric-epsilon threshold differences (a linear vs. a squared outside/inside bound; `1e-6` vs. `SIMD_EPSILON`) stay open in the same reachability sense as `FR-045`'s own epsilon finding — found and not adopted, behaviorally indistinguishable for every reachable scenario, and this too does not touch `FR-005`, still blocked on `PHASE-0-EXIT`; `setup_rows`'s own fixed, velocity-independent `plane_space(&contact.normal)` friction-direction basis is no longer an open item — `FR-048` found real Bullet's actual default instead derives friction direction 1 from the tangential component of the current relative sliding velocity itself, falling back to the fixed basis only when that tangential velocity is negligible (physically meaningful, not cosmetic, since each friction row is independently clamped to `[-mu*N, +mu*N]`, so two fixed orthogonal rows approximate the true circular friction cone with a square in tangent space, letting up to `sqrt(2)` times the correct friction magnitude be achieved at the square's diagonal for a body sliding along neither fixed axis), and, since `FR-049`, a new `solver::friction_directions` helper closes it — aligning friction direction 1 with the tangential component of relative sliding velocity itself, falling back to the fixed basis only for negligible tangential velocity or a degenerate tangential vector from near-head-on-collision floating-point cancellation, wired into both `setup_rows` and `setup_two_body_rows`, with a dedicated isotropic-friction regression test verified to fail under the old fixed-basis behavior proving the fix has real behavioral bite, and, since `FR-062`, a new, additive `body::RigidBody::ball(radius, mass, position)` constructor adds new API surface setting real Rocket League's own confirmed ball material properties (`restitution = 0.6`, `friction = 0.35`, `linear_damping = 0.03`, sourced from RocketSim's own `RLConst.h` `BALL_RESTITUTION`/`BALL_FRICTION`/`BALL_DRAG`) instead of the generic `0.5`/`0.5`/`0.0` placeholder every `RigidBody::sphere` call gets — resolving the exact gap `FR-061`'s own Non-goals had deferred (no way for a caller to say "this one is a real ball"); `sphere` itself is completely unchanged, still generic, and no existing call site was migrated to `ball` — this is new, additive API surface, not a behavioral fix to an existing one; `BALL_MASS_BT = CAR_MASS_BT / 6.f`'s own `1:6` mass ratio was considered and explicitly not adopted, since this project has no canonical "real" car construction site yet to normalize it against (every `RigidBody::car_box` call site today is test-only), left for a future requirement. | In Progress | `crates/rb_physics_bullet`, 3 new tests (309 total) |
| `PHASE-2-DETERMINISM` | Physics core steps identically given identical inputs, run to run. Est. ~1 month part-time. | `PHASE-1-PHYSICS-CORE` | RB-SIM-001 | Repeated-run divergence score is zero; RB-SIM-001 acceptance criteria met. | Not Started | — |
| `PHASE-3-NETCODE` | Server-authoritative simulation with client prediction/rollback, per ADR-0001. Est. 2-3 months part-time. | `PHASE-2-DETERMINISM` | RB-NET-001 | Own client/server pair reproduces the GDC talk's documented reconciliation behavior under simulated network conditions; owner can play an online-style match against a friend. | Not Started | — |
| `PHASE-4-POLISH` | Open-ended: UX, content, tooling, whatever remains once Phase 3 is playable. | `PHASE-3-NETCODE` | (spec'd when reached) | Defined when reached — deliberately not spec'd in detail at bootstrap. | Not Started | — |

## Notes

- Total estimate for Phases 1-3: ~6-9 months, part-time. Not a deadline —
  phases are dependency gates, and Phase 0 must exit before Phase 1 work
  starts (ADR-0002).
- `RB-RESEARCH-O001` (build vs. integrate) is resolved by ADR-0004 (direct
  Bullet3 port) — see `docs/research/RESEARCH-BACKLOG.md`. `PHASE-1-PHYSICS-CORE-V0`
  started ahead of `PHASE-0-EXIT` on that basis; real divergence-score
  validation of the approach still waits on `PHASE-0-EXIT` landing.
- Online-specific lag/reconciliation validation (the project's original
  diagnostic goal) is addressed in `PHASE-3-NETCODE`, validated against the
  GDC talk's documented architecture rather than live official-server
  traffic (no ground-truth source exists for the latter).
