# Specification Registry

IDs remain stable. Link superseding artifacts rather than reusing an ID for
a new meaning.

| ID | Title | Version | Design | Implementation | Verification | Depends on | Owner | Location | Evidence |
|---|---|---:|---|---|---|---|---|---|---|
| `RB-VERIFY-001` | Replay Ingestion | 0.4.0 | In Progress | In Progress (FR-001/002/003/004) | Verified (real fixture + 40-replay owner corpus) | — | baileyrd | [spec](verification/RB-VERIFY-001-replay-ingestion.md) | `crates/rb_replay_ingest` tests (14) + `corpus_check` (40/40) |
| `RB-VERIFY-002` | BakkesMod Offline Capture Ingestion | 0.4.0 | In Progress | In Progress (FR-001 built/loaded/run against real Rocket League + BakkesMod; FR-002 implemented) | Verified (FR-001 real captures + FR-002 synthetic fixture and real-capture parse) | — | baileyrd | [spec](verification/RB-VERIFY-002-capture-ingestion.md) | `crates/rb_capture_ingest` tests (10); `bakkesmod-plugin/rusty_bullet_capture/` (built, loaded, run — 2 real captures) |
| `RB-VERIFY-003` | Divergence Scoring | 0.11.0 | Draft | All FRs implemented (ball + car scoring, timestamp-tolerant alignment, CLI wiring); run end-to-end against a real replay + real BakkesMod capture, closing `PHASE-0-EXIT` | Verified (algorithm + mechanical end-to-end run on real data) | RB-VERIFY-001, RB-VERIFY-002 | baileyrd | [spec](verification/RB-VERIFY-003-divergence-scoring.md) | `crates/rb_domain/src/divergence.rs` tests (10) + `angle_to` tests (3) + `rb_verify_cli` tests (3) + 2 manual runs |
| `RB-PHYSICS-001` | Physics Core Port | 0.101.0 | In Progress | In Progress (sphere + box bodies, ball-vs-car, car-vs-car, body-vs-arena-wall, and ball-and-car-vs-curved-fillet collision wired into a multi-car `PhysicsWorld`, ground-driving car input, boost, handbrake, a variable-height ground jump, a double jump (plain or a flip-cancelable dodge), a wall jump (itself dodgeable), air control, a gentle landing auto-orientation assist, a modeled octagonal arena footprint, curved fillets throughout the arena's vertical boundary (floor/ceiling seams for all 9 walls, all 8 corner-wall vertical edges, and all 16 compound-corner spherical patches — the 4 corner walls' own floor/ceiling-seam arches and all 16 compound-corner fillets now sized with a distinctly larger, dedicated radius since FR-025), an actual goal-mouth window (with its own 3 rounded edges) cut into each back wall, with the goal's own remaining post-crossbar compound corners now rounded by 4 more corner fillets since FR-026 (20 compound-corner fillets total), and, since FR-027, a car actually deflected by every one of those fillets too via the same "test every corner" technique `box_vs_plane` already uses, and, since FR-028, a car actually driving into a goal through that same window too (`collision::box_vs_goal_wall`'s own per-corner window test), and, since FR-029, a modeled bounded interior volume behind each goal window too — a solid bounding box (not a net mesh) stopping the ball or a car that passes through instead of letting it sail into unbounded open space, and, since FR-030, every ball-vs-car and car-vs-car contact touching in the same step now shares one interleaved solve (`solver::resolve_dynamic_manifolds`) instead of a sequence of fully independent pairwise solves, and, since FR-031, every uncalibrated placeholder constant in `drive.rs`/`arena.rs`/`world.rs` audited against three independent community reverse-engineering references (RocketSim, RLUtilities, the RLBot wiki) — `drive::JUMP_SPEED`/`drive::JUMP_HOLD_ACCELERATION` corrected to their precise real values and a new `drive::UNBOOSTED_MAX_CAR_SPEED` split out from `drive::MAX_CAR_SPEED` (a real behavioral fix: throttle alone could previously reach the boosted top speed), most other constants confirmed already correct, the rest explicitly flagged as still uncalibrated rather than silently ambiguous — this audit does NOT close FR-005, which stays blocked on `PHASE-0-EXIT`, and, since FR-032, FR-027's own "test every corner" technique investigated against a claimed under-detection bug (a box face resting flush against a shallow curve leaving the face's middle overlapping the fillet while every corner reads clear): a full GJK closest-points narrow phase was built and wired in, broke two previously-passing tests, and on investigation was found to answer the wrong question for this shape pair — a quarter-pipe/corner-fillet contact test is a containment question (is the box's farthest point from the axis at or beyond radius), and the maximum of that convex distance function over a convex box is always attained at one of its 8 corners, never a face's interior, so the original per-corner technique is mathematically exact here, not an approximation; the GJK code (including the `gjk.rs` module) was reverted in full and `box_vs_quarter_pipe`/`box_vs_corner_fillet` are byte-for-byte unchanged from FR-027, with the claimed under-detection bug corrected in doc comments across the crate and a new empirical regression test added in its place, and, since FR-033, a genuine mass-spring net panel (`net::NetMesh`, a rectangular grid of anchored-perimeter/free-interior point masses connected by structural and shear springs, each point a real `RigidBody::sphere` resolved against the ball via a new `collision::sphere_vs_sphere` test plus the existing `solver::resolve_contacts_between` two-body solve) now catches the ball at each goal, closing FR-029's own "genuine net mesh" non-goal for the ball (a car's own contact against a net was, at the time, a documented non-goal, still passing straight through and stopped by FR-029's pre-existing bounding-box backstop instead — closed since by FR-038, see that entry), and, since FR-034, every contact's normal row now also solves a second, separate split-impulse "push" pseudo-velocity channel (`solver::resolve_push_row`/`resolve_two_body_push_row`), fed only by that contact's own positional/penetration (ERP) error and applied directly to the body's position/orientation via a new `solver::apply_push_delta` (mirroring Bullet's own `btSolverBody::writebackVelocity`) instead of injecting spurious velocity, wired into all three solver entry points with zero call-site changes outside `solver.rs`, and, since FR-035, warm-starting scoped to `solver::resolve_dynamic_manifolds` only — a new `solver::ContactCache` carries a manifold's converged real-channel impulses from one call to the next, matched by each contact's approximate world position (`CONTACT_MATCH_DISTANCE`), and a new `warm_start_two_body_row` seeds that cached impulse directly into the manifold's shared `DeltaVelocity` accumulators before iteration starts, helping FR-030's own documented extreme-mass-ratio "sandwiched" under-convergence case without being wired into `resolve_contacts`/`resolve_contacts_between` (no scenario there yet demonstrates any value) and without fixing the separately-documented "bouncy resting contact never settles" limitation, which needs sleeping instead, and, since FR-036, a dedicated follow-up to FR-031's own audit used real source-level research (RocketSim's and RLUtilities' own source, and the current RLBot wiki, read directly rather than guessed at) to resolve the two genuine ambiguities that audit surfaced but deliberately didn't act on — the ball's collision radius corrected from `92.75` to `93.15` (the real games' separate, larger collision radius, not the smaller `91.25` inertia radius, since this port's single unified radius field has no room for that distinction) and `arena::CEILING_Z` corrected from `2044.0` to `2048.0` (confirmed the same reference point as RocketSim's `ARENA_HEIGHT = 2048.f`), plus two low-risk doc-comment-only corrections to `arena::CORNER_LENGTH` and `arena::GOAL_DEPTH` (both confirmed exact, values unchanged), with `arena::FILLET_RADIUS`/`CORNER_ARCH_RADIUS` deliberately left untouched and still genuinely uncalibrated (a separate mesh-ingestion follow-up); FR-005 open, car-vs-curve, car-into-goal, goal-interior bounding volume, the combined multi-body solve, and the constant-calibration audit now resolved (FR-027/FR-028/FR-029/FR-030/FR-031), FR-032 closes the "genuine convex-vs-curved-surface narrow phase" open question by verifying none is needed, FR-033 closes the ball-side "genuine net mesh" open question, FR-034 closes the "no split impulse" half of the solver-simplification gap, FR-035 closes the warm-starting half of what was left, and FR-036 closes the two ambiguities FR-031's audit surfaced but deliberately didn't act on, and, since FR-037, sleeping (`body::RigidBody::update_sleep_state`/`wake`) closes what used to be the last open item, forcibly zeroing a body's velocity once it's stayed below a linear and angular threshold for a sustained time, the actual fix for a bouncy resting contact never settling that neither split impulse nor warm-starting alone could provide, and, since FR-038, `net::NetMesh::step` takes every body that can touch the net (the ball and every car) instead of the ball alone, closing this port's own former "a car still passes through untouched" Non-goal — no new collision code was needed, since `collision::contacts_between` already dispatches box-vs-sphere for a car against a net point the same way it always has for ball-vs-car, and, since FR-039, a wall jump at a corner (a car touching two walls at once) pushes off along every touched wall's normal summed and normalized, instead of picking whichever wall came first in `PhysicsWorld.walls` — closes the "first wall in `self.walls`" simplification FR-013 originally documented and FR-019's new diagonal corner walls made reachable for the first time, and, since FR-040, a dedicated research pass — matching FR-036's own real-source-research method — specifically targeting the two remaining uncalibrated placeholder constants FR-036 itself deliberately left untouched (`arena::FILLET_RADIUS`/`CORNER_ARCH_RADIUS`): searched this port's established reference tier (RocketSim/RLUtilities source, the RLBot wiki, RLGym's game-value list) and found no adoptable value — the RLBot wiki's sole candidate, an uncited "Wall bottom ramp radius: Aprox. 256 (but they are not circular)", doesn't clear FR-036's own bar for adoption (no citation, doesn't distinguish the two constants' distinctly different radii, disclaims being a true circular arc, and shares its numeral with RLGym's own unrelated `RAMP_HEIGHT` constant) — so both constants remain unchanged (`292.0`/`750.0`) and still genuinely uncalibrated, now with a documented research trail rather than a silent gap, and, since FR-041, `solver::resolve_dynamic_manifolds` scales each manifold's velocity-row impulse by a parameter-free `1 / k` for a body shared by `k >= 2` manifolds this step — after first trying, and rejecting, a naive global SOR-style over-relaxation factor that provably diverges FR-030's own symmetric-pinch scenario above `omega = 1.0` — narrowing that scenario's combined-solve result from ~89.5 to ~32 units/s at zero added iteration cost, without closing the gap outright), and, since FR-042, `box_vs_box`'s edge-edge contact point and face-clipping degenerate fallback were both validated directly against Bullet's own `btBoxBoxDetector::dBoxBox` reference source (fetched and read directly, not guessed at) — the edge-edge contact point confirmed strictly more rigorous than the reference's own unclamped-infinite-line `dLineClosestApproach` (which applies its `alpha`/`beta` offsets with no bounds check at all), and the face-clipping fallback's synthesize-rather-than-drop policy confirmed a deliberate, favorable divergence from the reference's own `return 0` (the reference contains the identical undocumented "should never happen" judgment call, unproven either way, so this port's own framing wasn't a weaker position); a candidate fix for `edge_contact`'s tangent sign-selection heuristic (swapping the center-to-center vector `d` for the SAT-resolved normal, matching the reference's own approach) was built and empirically tested against a brute-force ground truth across 50,000 randomized two-box configurations and found genuinely mixed — the current `d`-based heuristic wins at large/arbitrary penetration depths (~11.6% vs. ~8.7% optimal-match rate), the normal-based candidate wins at realistic under-0.5-unit near-first-contact depths (~93% vs. ~77%) — so not adopted, left as a still-open item; zero production code changed, only doc comments in `collision.rs`), and, since FR-043, this spec's own prior claim that Bullet's default restitution/friction combine mode is `btMax` was checked directly against Bullet's real `btManifoldResult.h`/`btManifoldResult.cpp` reference source (fetched and read in full, matching FR-036/FR-042's own method) and found wrong — the real default for both restitution and friction is an unclamped product (`a * b`; friction additionally clamps the result to `[-10, 10]`), with no `max` mode, no geometric mean, and no per-pair override anywhere in the reference short of a custom `gContactAddedCallback`; `solver::combine_restitution`/`combine_friction`'s own average (`(a + b) * 0.5`) is unchanged, now justified by a correct reason instead of the now-corrected wrong one — unlike the reference's product, average preserves the identity `combine(a, a) == a` (the reference's own product gives `0.25` for two `0.5` coefficients, not `0.5`), which matters since most bodies in this port currently share that same uncalibrated placeholder `0.5` for both coefficients (see `body.rs`'s `Default` impls); the wrong claim was corrected everywhere it appeared (this spec's Open Questions, `solver.rs`'s module doc comment, `body.rs`'s field doc comment); whether either formula matches real Rocket League itself remains genuinely open, unaffected by this correction; zero production code changed, only doc comments and 2 new dedicated `solver.rs` identity-preserving unit tests), and, since FR-044, this spec's own top-level "Non-goals (this increment)" section was found still carrying a stale "Split impulse. This port always takes Bullet's non-split contact-resolution branch" bullet, contradicted by FR-034's own already-shipped implementation (that Requirements entry, the version 0.34.0 Change History entry, and `rb_physics_bullet::solver`'s own module doc comment all already correctly describe split impulse as implemented) — confirmed the implementation is genuinely present by locating `solver::resolve_push_row`/`resolve_two_body_push_row`/`apply_push_delta` directly in `solver.rs`, and confirmed via a repo-wide grep that this was the only stale occurrence anywhere in code or docs; corrected the bullet to a strikethrough-and-close note, matching the same convention this section already uses for its own two other resolved Non-goals items (the wall-jump-corner disambiguation closed via FR-039; the curved-geometry Non-goal closed progressively via FR-026 through FR-033); zero production code changed, no new tests), and, since FR-045, `integrate.rs`'s own Bullet-reference doc comments (`apply_damping`, `integrate_velocities`, `integrate_transform`) were checked directly against Bullet's real `btRigidBody.cpp`/`.h`, `btTransformUtil.h`, `btQuaternion.h`, and `btScalar.h` reference source (fetched and read directly, matching FR-036/FR-042/FR-043's own method) and confirmed byte-for-byte accurate in every formula and constant checked; one minor numeric difference was found and not adopted (this port's `1e-12` degenerate-quaternion epsilon vs. Bullet's real `SIMD_EPSILON`/`FLT_EPSILON`, ~1.19e-7, about 5 orders of magnitude larger, but behaviorally indistinguishable for every reachable scenario), and a more significant finding confirmed `integrate_transform`'s check-then-normalize fallback branch load-bearing rather than defensive theater — it mirrors Bullet's own real fallback choice to preserve a body's prior orientation on a degenerate predicted quaternion rather than reset to identity, a distinction an unconditional call to `rb_domain::Quat::normalize` would have silently gotten wrong (that function's own generic guard substitutes `IDENTITY` instead); 1 new test added pinning this exact distinction, zero production code changed), and, since FR-046, `body.rs`'s `Shape::local_inertia`/`RigidBody::update_inertia_tensor` and `mat3.rs`'s `Mat3::scaled_columns`/`Mat3::from_quat` were checked directly against Bullet's real `btSphereShape.cpp`, `btBoxShape.cpp`, `btRigidBody.cpp`/`.h`, and `btMatrix3x3.h` reference source (fetched and read directly, matching FR-036/FR-042/FR-043/FR-045's own method) and confirmed byte-for-byte accurate — the sphere/box local-inertia formulas, `update_inertia_tensor`, and `Mat3::scaled_columns` all match the reference exactly; one genuine difference was found in `Mat3::from_quat` and not adopted — it hardcodes an `s = 2` factor assuming an exactly unit-length input quaternion, while the reference's own `btMatrix3x3::setRotation` computes `s = 2 / q.length2()` to self-correct for a non-unit-length input, not adopted since this function's only production call site (`RigidBody::update_inertia_tensor`) always receives an already-renormalized orientation (an invariant `integrate::integrate_transform` maintains, per FR-045's own finding), making the reference's own self-correction unreachable defensive theater here; 1 new test added pinning this exact distinction, zero production code changed), and, since FR-047, `collision.rs`'s remaining closed-form shape pairings (`sphere_vs_plane`, `box_vs_plane`, `sphere_vs_box`, `sphere_vs_sphere` — `box_vs_box` already covered by FR-042) were checked directly against Bullet's real `btConvexPlaneCollisionAlgorithm.cpp`/`.h`, `btSphereBoxCollisionAlgorithm.cpp`, `btSphereSphereCollisionAlgorithm.cpp`, and `btManifoldPoint.h` reference source (fetched and read directly, matching FR-036/FR-042/FR-043/FR-045/FR-046's own method) and checked every specific claim against it — `sphere_vs_plane` and `sphere_vs_sphere` confirmed exact, byte-for-byte matches to Bullet's real algorithms; `sphere_vs_box`'s deep-penetration face-selection logic confirmed to reproduce Bullet's own exact `+x, -x, +y, -y, +z, -z` face-check tie-break order (first-checked-wins-ties), not just a mathematically-equivalent alternative, pinned by 1 new test; a genuine, deliberate divergence was found in `box_vs_plane` and not adopted — real Bullet's default config only generates one contact point per frame (a single GJK support-vertex query), relying on several frames of persistent-manifold accumulation to reach a resting box's full 4-corner manifold, whereas this port's `box_vs_plane` computes all 4 corners exactly in a single pass, confirmed a more rigorous simplification in the same spirit as `box_vs_box`'s own FR-042 finding; 1 new test added pinning the tie-break order, zero production code changed), and, since FR-048, `solver.rs`'s constraint-row setup/resolve functions (`restitution_curve`, `plane_space`, `setup_rows`, `resolve_row`) and `btContactSolverInfo`'s own cited default constants were checked directly against Bullet's real `btSequentialImpulseConstraintSolver.cpp`/`.h`, `btContactSolverInfo.h`, and `btVector3.h` reference source (fetched and read directly, matching FR-036/FR-042/FR-043/FR-045/FR-046/FR-047's own method) — `plane_space` confirmed byte-for-byte exact against real `btPlaneSpace1`; `restitution_curve` confirmed behaviorally exact — real Bullet's `restitutionCurve` can return a raw negative value, but its one caller (`setupContactConstraint`) immediately clamps a non-positive result to exactly `0` before it's used, and this port's own `restitution_curve` folds that call-site clamp inline via `.max(0.0)` instead of as a separate step, a confirmed-equivalent restructuring, not a divergence; `setup_rows` confirmed exact against real `setupContactConstraint`/`setupFrictionConstraint` (also corrected a stale doc-comment citation — the doc previously cited a differently-named, unrelated function `setFrictionConstraintImpulse`, which in real Bullet only resets a cached impulse to zero; the actual friction-row-setup function is `setupFrictionConstraint`, reached via `addFrictionConstraint`'s own default `desiredVelocity = 0`/`cfmSlip = 0` arguments); `resolve_row` confirmed a behaviorally-equivalent unification — this port uses one function (checking both lower and upper bounds) for every row, where real Bullet dispatches to two different functions (`resolveSingleConstraintRowLowerLimit`, lower-bound-only, for the normal row; `resolveSingleConstraintRowGeneric`, both bounds, for friction rows), confirmed to change nothing since the normal row's own `upper_limit` (`1e10`, matching real Bullet's own value exactly) is astronomically larger than any real impulse, making the extra upper check unreachable there; all 6 of `btContactSolverInfo`'s cited default constants (`ERP2 = 0.2`, `GLOBAL_CFM = 0.0`, `LINEAR_SLOP = 0.0`, `RESTITUTION_VELOCITY_THRESHOLD = 0.2`, `RELAXATION = 1.0`, `SOLVER_ITERATIONS = 10`) confirmed exact against real `btContactSolverInfoData`'s own constructor; one genuine, significant divergence was found and NOT adopted — this port's `setup_rows` always derives both friction directions from a fixed, velocity-independent `plane_space(&contact.normal)` basis, while real Bullet's actual default (in `convertContact`) instead derives friction direction 1 from the tangential component of the current relative sliding velocity itself, falling back to `btPlaneSpace1`'s own fixed basis only when that tangential velocity is negligible — physically meaningful, not cosmetic, since each friction row is independently clamped to `[-mu*N, +mu*N]`, so two fixed orthogonal rows approximate the true circular friction cone with a square in tangent space, letting up to `sqrt(2)` times the correct friction magnitude be achieved at the square's diagonal for a body sliding along neither fixed axis; left as open follow-up work for a dedicated future FR, the same scoping this port already used for FR-030/FR-034/FR-035/FR-037, rather than folded into this reference-validation pass; zero production code changed, only doc comments in `solver.rs`, 1 new test added pinning the `restitution_curve`/call-site-clamp equivalence), and, since FR-049, that exact open item was closed — a new `solver::friction_directions(normal, relative_velocity)` helper now aligns friction direction 1 with the tangential component of the current relative sliding velocity (matching real Bullet's own default), with direction 2 completing a right-handed orthonormal basis via `dir1.cross(normal)`, falling back to the old `plane_space` fixed basis in two cases — negligible tangential velocity (matching real Bullet's own `SIMD_EPSILON` threshold) and a second, genuinely new case found while implementing this fix, a near-head-on-collision catastrophic floating-point cancellation that can produce a degenerate tangential vector, which real Bullet's own unguarded `normalize()` would silently mishandle but this crate's own `Option`-returning `Vec3::normalize()` instead falls back gracefully from — wired into both `setup_rows` (one-body contacts) and `setup_two_body_rows` (two-body contacts), with 3 new tests added, including a dedicated isotropic-friction regression test verified to fail under the old fixed-basis behavior, proving the fix has real behavioral bite, and, since FR-050, `net::NetMesh::step`'s own similarly independent-pairwise resolution of every body-vs-net-point contact was investigated against the same order-dependent under-convergence bug FR-030 already fixed for ball-vs-car/car-vs-car contacts — `net.rs`'s own doc comment had waved this off as irrelevant since a net point's mass is "tiny enough" relative to a real ball or car, an untested claim found false: `NET_POINT_MASS` (0.5) is exactly half a typical ball's own mass (1.0), not a lopsided ratio, and a ball or car pressing into the net commonly overlaps two or more free net points at once given `NET_POINT_RADIUS`'s generous coverage-radius sizing; a dedicated single-shot test confirmed the mechanism genuinely order-dependent (a ball placed exactly symmetrically between two net-point-like bodies is left with a nonzero, purely arbitrary sideways velocity whose sign flips with iteration order), and a `NetMesh::step`-level test measured the real-world bias directly — a ball fired squarely at the net's own center, straddling two symmetric free interior points, was deflected sideways by ~0.25 units/s out of a 2000 units/s impact under the old sequential loop; fixed by adopting `solver::resolve_dynamic_manifolds`'s combined multi-body solve for every body-vs-point contact detected within a sub-step instead of resolving each pair immediately and independently, measured to reduce that same residual from ~0.25 to ~0.016 units/s, roughly a 15-fold improvement; warm-starting was deliberately not added — `NetMesh::step` still passes a fresh, empty `ContactCache` every sub-step, cold-starting every call exactly as before — left as the same kind of open follow-up work FR-035 already scoped out for `resolve_contacts`/`resolve_contacts_between` generally), and, since FR-051, `PhysicsWorld::step`'s own similarly independent-pairwise resolution of a body's contact against each different static shape type — the ground, then every wall, then every curve, then every corner fillet, then every goal wall, then every bounded wall, each resolved via its own fully independent `solver::resolve_contacts` call before the next shape's setup even reads the body's velocity — was investigated against the same order-dependent under-convergence bug FR-030/FR-050 already fixed for dynamic-vs-dynamic and net-point contacts, this time for a body touching two different static surfaces at once (a car driving near a wall close to the ground, or wedged into a corner, a routine gameplay occurrence, not an edge case); a dedicated single-shot test confirmed the mechanism genuinely order-dependent, not merely slow to converge — a ball wedged symmetrically into a corner formed by two static walls (perpendicular normals, identical restitution/friction), moving diagonally into both at once, is left biased toward whichever wall is resolved last (mirror-image results depending on order) instead of the true, by-symmetry equal-velocity-component answer; fixed by a new `solver::resolve_static_manifolds` generalizing `resolve_contacts` to combine every static-shape manifold a body touches into one shared solve (each manifold group still computes its own combined restitution/friction against the body's own material properties, but all groups now share one DeltaVelocity/push-delta accumulator across the whole SOLVER_ITERATIONS loop, the same fix FR-030/FR-050 already made), wired into `PhysicsWorld::step` via a new `StaticScene` struct (bundling the six static-shape slices into one parameter to stay under clippy's `too_many_arguments` limit) and a new `resolve_static_contacts` function that gathers every one of a body's contacts across every static shape into one manifold list and resolves them all together, replacing the old five separate per-shape-type helper functions (`resolve_plane_contact`, `resolve_curve_contact`, `resolve_corner_fillet_contact`, `resolve_goal_wall_contact`, `resolve_bounded_wall_contact`, all removed) and their sequential per-shape call sequence in `step`, for both the ball and every car; a `PhysicsWorld::step`-level test (a ball fired diagonally into a real two-wall corner via an actual `PhysicsWorld` with two `with_wall` calls) confirmed the fix at the real public API, verified to fail under the old sequential per-shape loop before the rewire, and, since FR-052, `PhysicsWorld::step`'s own remaining boundary between FR-051's own two already-combined solves — a body's static-shape manifolds (`solver::resolve_static_manifolds`) and its dynamic-vs-dynamic manifolds (`solver::resolve_dynamic_manifolds`) were still resolved via two separate sequential calls, a body's static contacts fully resolved and applied before the dynamic solve's own setup for that same body ever read the result — was investigated against the same order-dependent under-convergence bug FR-030/FR-050/FR-051 already found elsewhere, and confirmed present: a dedicated single-shot test reused FR-051's own symmetric two-wall corner setup but replaced one wall with a very-heavy (mass 1e9) dynamic body routed through the dynamic-manifold path instead of the static one, geometrically identical to the wall it replaced, and found the same order-dependent bias FR-051 already fixed within each channel separately reappearing at the boundary between them; fixed by a new `solver::resolve_manifolds` folding a step's static manifolds and dynamic manifolds into one shared solve — every body's own static rows and dynamic-manifold rows now share one DeltaVelocity/push-delta accumulator for the whole SOLVER_ITERATIONS loop (FR-041's own 1/k relaxation deliberately kept counting k from dynamic manifolds only, after extending it to static rows was tried and found to regress FR-051's own two-static-wall test's convergence); wired into `PhysicsWorld::step` by renaming `resolve_static_contacts` to `static_contact_manifolds` (now returning a body's gathered static manifold groups instead of resolving them directly) and replacing `step`'s two separate `resolve_static_manifolds`-per-body-then-`resolve_dynamic_manifolds`-once calls with one `solver::resolve_manifolds` call; a `PhysicsWorld::step`-level test (a ball fired diagonally into a real wall-and-heavy-car corner, one real `StaticPlane` wall via `with_wall`, one real heavy-mass car via `with_car`) confirmed the fix at the real public API, settling with nearly equal x/y velocity components, verified to fail under the old two-call sequence before the rewire), and, since FR-053, `btManifoldResult.cpp` was re-fetched and re-read directly (matching FR-036/FR-042/FR-043/FR-045/FR-046/FR-047/FR-048's own method) to confirm one more detail FR-043's own investigation never separately examined — real Bullet's `calculateCombinedFriction` additionally clamps its product result to `[-10.0, 10.0]` (a plain `if` clamp, not `btClamped`; `calculateCombinedRestitution` has no such clamp at all) — confirmed currently inert for every friction coefficient this crate itself ever actually sets (every `RigidBody`/`StaticPlane`/`StaticQuarterPipe`/`StaticCornerFillet`/`StaticGoalWall`/`StaticBoundedWall` this crate constructs uses a value in `0.1..=0.9`, nowhere near either bound), and adopted anyway for reference conformance since every one of those types' own `friction` field is a public, unvalidated `f32` with zero range enforcement anywhere in the crate today; `solver::combine_friction` now clamps its own average result to `[-10.0, 10.0]` (a pure clamp addition, not a formula change — the average FORMULA `RB-PHYSICS-001-FR-043` already decided to keep is unchanged), while `solver::combine_restitution` is left unclamped, matching the reference's own choice not to clamp restitution; 1 new test added (`combine_friction_clamps_to_the_same_bound_real_bullet_uses`), zero other production code changed), and, since FR-054, `collision::box_vs_goal_wall`'s own remaining open question from FR-028 (whether a car face resting flush against the goal window's own edge could read as clear on every corner while its middle already overlapped the window) was resolved via a convex-hull argument distinct from FR-032's own — a box's touching face is the convex hull of whichever corners penetrate the plane, so "every corner outside the window" is exactly equivalent to "the face doesn't fully fit through it", confirming no bug exists, matching FR-032's own precedent of a suspected gap that doesn't exist, via a distinct argument; the same investigation examined the structurally-identical `collision::box_vs_bounded_wall` (FR-029) and found its own mirror-image case (a face bigger than a bound and centered on it) *is* a genuine under-detection gap, confirmed unreachable given this project's own car (60x30x18 half-extents)/ball (93.15 radius) sizes against the standard arena's own `StaticBoundedWall` bound sizes, documented as a new Non-goals item rather than fixed; 2 new tests added, zero production code changed), and, since FR-055, `arena::GOAL_HALF_WIDTH` (892.755) and `arena::GOAL_HEIGHT` (642.775) — carrying a "commonly-cited community number, not independently confirmed" caveat since FR-024 introduced them — were confirmed exact against the current RLBot wiki's "Useful Game Values" page (fetched directly, the same page FR-036's own research already used to confirm `arena::GOAL_DEPTH`), a sourcing-status upgrade only, no value changed; the same investigation also found this spec's own "Open questions" section still describing `arena::GOAL_DEPTH` as an unconfirmed "uncalibrated invention", contradicting FR-036's own already-shipped Requirements entry and this spec's own Non-goals section, both already stating GOAL_DEPTH is confirmed — corrected (with a strikethrough over the superseded text) to state all three goal-geometry constants (`GOAL_HALF_WIDTH`, `GOAL_HEIGHT`, `GOAL_DEPTH`) are confirmed, leaving only `arena::NET_DEPTH` (FR-033, a distinct, still genuinely uncalibrated quantity) as an open invention in that vicinity; only doc comments changed in `arena.rs` (`GOAL_HALF_WIDTH`, `GOAL_HEIGHT`, `NET_DEPTH`), zero production behavior changed, no new tests), and, since FR-056, `drive::BOOST_ACCELERATION` — a single flat constant (`991.667`) applied identically by `apply_driven_forces` whether its own `on_ground` parameter was true or false, despite this port's own doc comments (including RB-PHYSICS-001-FR-008's own Requirements entry) explicitly claiming boost "works identically airborne" — was checked against RocketSim's own `RLConst.h` (fetched directly, matching FR-036/FR-042/FR-043/FR-045/FR-046/FR-047/FR-048/FR-053's own method) and found that claim itself wrong: the reference defines two distinct constants, `BOOST_ACCEL_GROUND = 2975.f / 3.f` (≈991.667, exactly matching this port's existing flat value) and `BOOST_ACCEL_AIR = 3175.f / 3.f` (≈1058.333, about 6.5% higher) — a genuine ground/air split this port's single-constant model didn't capture, meaning every airborne boost this crate ever applied understated real Rocket League's own airborne boost strength; fixed by splitting `BOOST_ACCELERATION` into `BOOST_ACCELERATION_GROUND` (unchanged value, rewritten as the exact `2975.0 / 3.0` fraction) and a new `BOOST_ACCELERATION_AIR` (`3175.0 / 3.0`), wired into `apply_driven_forces`'s existing `on_ground` parameter to select between them (no new gating logic — boost already applied in both cases, only the magnitude changed), with every doc comment that had claimed the two were identical (this module's own doc comment, RB-PHYSICS-001-FR-008's own Requirements entry, and this spec's own Non-goals/Open-questions mentions) corrected; the same fetch also confirmed `BOOST_CONSUMPTION_RATE`/`MAX_BOOST` already match RocketSim's own `BOOST_USED_PER_SECOND = BOOST_MAX / 3`; 1 new test added (`boost_accelerates_an_airborne_car_faster_than_a_grounded_one`), confirming the exact ratio between grounded and airborne boost acceleration matches RocketSim's own ratio), and, since FR-057, a hard ceiling on car angular speed — nothing previously bounded how fast sustained air control torque (or a dodge's own kick, or the landing-orientation assist) could spin a car — was added after a second fetch of RocketSim's own `RLConst.h` (matching FR-056's own method), this time targeting every `drive.rs` constant this crate's own doc comments flagged as having "no public reference at all", which surfaced `CAR_MAX_ANG_SPEED = 5.5f` (rad/s), a hard "car can never exceed this angular velocity" ceiling; added `drive::MAX_CAR_ANGULAR_SPEED = 5.5` and a new `drive::clamp_angular_speed`, a genuine clamp scaling `RigidBody.angular_velocity` back down to the cap (preserving direction) when exceeded, unlike `MAX_CAR_SPEED`/`UNBOOSTED_MAX_CAR_SPEED`, which only ever gate new throttle/boost force and never reduce velocity already past their own cap, wired in once per step immediately after `integrate::integrate_velocities` in both `world.rs`'s production `drive_and_integrate_velocities` and `drive.rs`'s own test helper `step_with_input_and_dodge_flip`; several other real constants the same fetch surfaced (dodge per-direction impulse scaling, auto-flip/landing-assistance thresholds, a ramping powerslide model, a steering-torque mapping, RocketSim's own per-axis `CAR_AIR_CONTROL_TORQUE`) were considered and explicitly not adopted, either needing further investigation or repeating `RB-PHYSICS-001-FR-031`'s own "false precision" finding for torque constants calibrated against a car body/inertia tensor this port's own placeholder doesn't match; `drive::DODGE_ANGULAR_SPEED`'s pre-existing numeric equality to this same `5.5` value is flagged as a documented coincidence, not a second confirmation of it; 3 new tests added (`clamp_angular_speed_is_a_no_op_below_the_cap`, `clamp_angular_speed_scales_an_over_cap_velocity_down_to_the_cap_preserving_direction`, `sustained_full_roll_input_never_exceeds_the_hard_angular_speed_cap`), bringing the crate to 295), and, since FR-058, `drive::THROTTLE_ACCELERATION`'s own doc comment had named this exact gap since it was first introduced — applying full flat acceleration right up to a hard cutoff at `UNBOOSTED_MAX_CAR_SPEED` instead of a genuine taper — was closed after fetching RocketSim's own `Car.cpp` (not just `RLConst.h`'s constants) to find exactly how its own `THROTTLE_TORQUE_AMOUNT` is actually used, surfacing the real mechanism: drive force is scaled by `DRIVE_SPEED_TORQUE_FACTOR_CURVE`, a confirmed 3-point piecewise-linear curve (`{0, 1.0}, {1400, 0.1}, {1410, 0.0}`), not applied flat; `THROTTLE_TORQUE_AMOUNT` itself is expressed in Bullet's own internal units and doesn't transfer cleanly to this port's own differently-calibrated car body (repeating `RB-PHYSICS-001-FR-031`'s and `FR-057`'s own "false precision" finding), but the curve's shape is a pure, unitless ratio that transfers regardless, the same reasoning `FR-057` used to adopt `MAX_CAR_ANGULAR_SPEED`; added `drive::DRIVE_SPEED_TAPER_BREAKPOINTS` and `drive::drive_speed_taper` (a small piecewise-linear interpolator evaluated against this port's own pre-existing signed `throttle.signum() * forward_speed` quantity, not RocketSim's own direction-agnostic `abs(forward speed)`, a separate behavioral question left out of scope) and rewired `apply_driven_forces`'s throttle block to scale `THROTTLE_ACCELERATION` by the taper instead of gating on a hard speed threshold; `THROTTLE_ACCELERATION`'s own peak magnitude (`1600.0`) is unchanged, still an uncalibrated placeholder — only the curve's real shape is adopted; 2 new tests added (`drive_speed_taper_matches_the_real_curve_breakpoints_exactly`, `throttle_acceleration_tapers_well_before_reaching_unboosted_max_speed`), bringing the crate to 297), and, since FR-059, `drive::DODGE_SPEED`'s own flat impulse magnitude — applied identically regardless of dodge direction or the car's current speed — was closed after a second fetch of RocketSim's own `Car.cpp` (`_UpdateDoubleJumpOrFlip`, the same file/technique `FR-058` used for the throttle taper) surfaced the real mechanism `RB-PHYSICS-001-FR-031`'s own audit had flagged but couldn't adopt without it: a dodge's base impulse scales per-axis by `((maxSpeedScale - 1) * forwardSpeedRatio) + 1`, where `maxSpeedScale` is `1.0` for a forward dodge (no change, ever), `2.5` for a backward dodge (opposing the car's own current velocity direction), or `1.9` for any side/roll dodge; adopted the confirmed real *ratios* (`DODGE_BACKWARD_SPEED_SCALE = 2.5`, `DODGE_SIDE_SPEED_SCALE = 1.9`), not the real base magnitude (`FLIP_INITIAL_VEL_SCALE = 500.0`) — since the real forward-dodge scale is exactly `1.0`, `DODGE_SPEED`'s own existing (still-uncalibrated) value doubles as that unscaled case unchanged, the same "shape confirmed, magnitude not" split `FR-058` used for `THROTTLE_ACCELERATION`; added `drive::dodge_speed_scale` (linear interpolation from `1.0` to `scale_at_max_speed` as speed rises to `MAX_CAR_SPEED`, clamped beyond it), `drive::dodge_pitch_is_backward` (the real backward classification, re-derived in this port's own pitch-sign convention, with a `DODGE_BACKWARD_CLASSIFICATION_SPEED_THRESHOLD = 100.0` low-speed fallback), and rewired both `apply_driven_forces` dodge sites (the ground-dodge block and its wall-jump-dodge variant) to scale `DODGE_SPEED` by this logic before applying it; every pre-existing dodge test dodges from a standing start, where the new scale evaluates to `1.0` regardless of direction, a zero-regression-risk property confirmed by inspection before implementation; 5 new tests added (`dodge_speed_scale_matches_the_real_curve`, `dodge_pitch_is_backward_matches_the_real_classification`, `a_backward_dodge_scales_up_with_current_forward_speed`, `a_forward_dodge_does_not_scale_with_current_forward_speed`, `a_side_dodge_scales_up_with_current_forward_speed`), bringing the crate to 302), and, since FR-060, `RB-PHYSICS-001-FR-057`'s own Non-goals had left open whether RocketSim's auto-flip constants (`CAR_AUTOFLIP_IMPULSE/TORQUE/TIME/NORMZ_THRESH/ROLL_THRESH`) could map onto this port's own `drive::LANDING_AUTO_UPRIGHT_TORQUE` (a gentle continuous torque nudging an airborne car upright with no player input), and this requirement resolved that investigation by fetching and reading RocketSim's actual `Car.cpp` source directly (matching FR-058/FR-059's own method) and finding no such mechanic exists in real Rocket League at all — it instead has two distinct, real, *grounded*, input-gated systems: auto-flip, a turtle-recovery flip firing only when the player presses jump while the car has ground contact on a roughly-horizontal surface (contact normal Z above `CAR_AUTOFLIP_NORMZ_THRESH`) and the car's current roll exceeds `CAR_AUTOFLIP_ROLL_THRESH`, applying a downward pinning impulse plus a forward-axis torque sustained over a timer (`CAR_AUTOFLIP_TIME * absRoll / PI`); and auto-roll, a continuous ground-alignment torque active only while throttle is held and the car has partial or full wheel contact — neither airborne, neither firing without a specific player input, both the opposite shape from this port's own continuous, input-free, airborne-only nudge, a "these don't map, confirmed" negative finding rather than a value change; `drive.rs`'s own module doc comment (the "Landing auto-orientation assistance" section and its "commonly-cited constants" paragraph) and this spec's own Open Questions and FR-057 Non-goals text corrected accordingly; zero production code changed, no new tests, crate remains at 302), and, since FR-061, real hard caps on the ball's own linear and angular speed — nothing previously bounded either, unlike the car's own `drive::MAX_CAR_ANGULAR_SPEED` (FR-057) — were added after fetching and reading RocketSim's real `RLConst.h` and `Ball.cpp` source directly (matching FR-036/FR-042/FR-043/FR-045/FR-046/FR-047/FR-048/FR-053/FR-056/FR-057's own method), finding two confirmed real hard caps enforced inside RocketSim's own `_FinishPhysicsTick()` (called after collision resolution, at the end of the physics tick) via a hard clamp (`if (vel.length2() > max*max) vel = vel.normalized() * max`): `BALL_MAX_SPEED = 6000.f` (uu/s) and `BALL_MAX_ANG_SPEED = 6.f` (rad/s, "Ball can never exceed this angular velocity"); added `world::BALL_MAX_SPEED = 6000.0` and `world::BALL_MAX_ANG_SPEED = 6.0` and a new private `world::clamp_ball_velocity`, generalizing FR-057's own `drive::clamp_angular_speed` shape (car-specific) to both linear and angular speed for the ball, placed in `world.rs` rather than `drive.rs` since the ball has no drive-input-gated mechanic of its own, wired into `PhysicsWorld::step` right after that step's own contact resolution (matching RocketSim's own "after collision resolution, end of tick" placement more precisely than `drive::clamp_angular_speed`'s own mid-pipeline placement for the car); `BALL_DRAG = 0.03f` ("net-velocity drag multiplier") was explicitly not adopted — RocketSim sets it once at ball construction as a per-match mutator-config default, not a hardcoded system invariant like the two speed caps, and this port's own `RigidBody::sphere` constructor takes no opinion on a "real" ball's own damping default, left for a future requirement; 4 new tests added (`clamp_ball_velocity_is_a_no_op_below_both_caps`, `clamp_ball_velocity_scales_an_over_cap_linear_velocity_down_to_the_cap_preserving_direction`, `clamp_ball_velocity_scales_an_over_cap_angular_velocity_down_to_the_cap_preserving_direction`, `a_ball_launched_far_past_ball_max_speed_never_exceeds_it_after_a_step`), bringing the crate to 306), and, since FR-062, a new, additive `body::RigidBody::ball(radius, mass, position)` constructor sets real Rocket League's own confirmed ball material properties instead of the generic placeholder every `RigidBody::sphere` call gets — resolving the exact gap `FR-061`'s own Non-goals had deferred (no way to say "this one is a real ball") — after fetching and reading RocketSim's real `RLConst.h` source directly (matching `FR-036`/`FR-042`/`FR-043`/`FR-045`/`FR-046`/`FR-047`/`FR-048`/`FR-053`/`FR-056`/`FR-057`/`FR-058`/`FR-059`/`FR-061`'s own method) and confirming `BALL_RESTITUTION = 0.6f` ("Bounce factor"), `BALL_FRICTION = 0.35f`, and `BALL_DRAG = 0.03f` ("Net-velocity drag multiplier") — none a torque or force calibrated against a specific mass/inertia (the same "false precision" category `FR-031` ruled out elsewhere), so all three transfer cleanly, the same category `FR-061`'s own speed caps cleared; `ball` sets `restitution = 0.6`, `friction = 0.35`, and `linear_damping = 0.03`, otherwise behaving identically to `sphere` (itself completely unchanged, still generic — this is new API surface, not a migration; no existing call site was switched from `sphere` to `ball`); explicitly not adopted: `BALL_MASS_BT = CAR_MASS_BT / 6.f` — while the `1:6` ratio is in principle as portable as `FR-059`'s own dodge scale ratios, this project has no canonical "real" car construction site yet to normalize it against (every `RigidBody::car_box` call site today is test-only), left for a future requirement; 3 new tests added (`ball_sets_confirmed_real_material_properties`, `ball_otherwise_behaves_identically_to_sphere`, `sphere_still_defaults_to_the_generic_placeholder_material_properties`), bringing the crate to 309), and, since FR-063, `solver::combine_restitution`/`combine_friction`'s own left-open real-Rocket-League-calibration question — "which formula (if either) actually matches real Rocket League itself" — was resolved differently than expected by fetching and reading RocketSim's real `RLConst.h` source directly (matching FR-036/FR-042/FR-043/FR-045/FR-046/FR-047/FR-048/FR-053/FR-056/FR-057/FR-058/FR-059/FR-061/FR-062's own method): the real answer isn't a different combine formula at all — real Rocket League's own gameplay layer hardcodes a distinct restitution/friction value per named contact-pair type instead of computing either from any generic per-body combine, bypassing whatever either body's own material would otherwise combine to (`CAR_COLLISION_FRICTION = 0.3f`/`CAR_COLLISION_RESTITUTION = 0.1f`, a generic car fallback; `CARWORLD_COLLISION_FRICTION = 0.3f`/`CARWORLD_COLLISION_RESTITUTION = 0.3f`, car vs. static geometry; `CARCAR_COLLISION_FRICTION = 0.09f`/`CARCAR_COLLISION_RESTITUTION = 0.1f`; and `CARBALL_COLLISION_FRICTION = 2.0f`/`CARBALL_COLLISION_RESTITUTION = 0.0f`), with two of these individually striking rather than merely "different from an average" — `CARBALL_COLLISION_RESTITUTION = 0.0f` means a car hitting the ball has zero restitution-driven bounce in real Rocket League regardless of either body's own material, a stark contrast with this port's own `combine_restitution(ball.restitution, car.restitution)`, which since FR-062 averages the ball's own confirmed real `0.6` against the car's still-generic `0.5` to a real `~0.55` bounce for exactly the pairing real Rocket League gives zero, and `CARBALL_COLLISION_FRICTION = 2.0f` is a friction coefficient above `1.0`, a value no combine of two bodies' own sane (`0.0..=1.0`-range) per-material friction fields could ever produce, confirming a genuinely different model (per-pair-type override) rather than an uncalibrated magnitude within the same per-body-combine model; `solver::combine_restitution`/`combine_friction`'s own doc comments and this spec's own Open Questions bullet corrected to state this finding directly; not adopted as a real fix, since `combine_restitution`/`combine_friction`'s own signature (two `f32` material values in, one combined value out) has no way to know which kind of pair produced those inputs — implementing real per-pair-type overrides would mean threading body/shape identity into every one of `solver.rs`'s several call sites (`resolve_contacts`, `resolve_contacts_between`, `resolve_static_manifolds`, `resolve_dynamic_manifolds`, `resolve_manifolds`), left as a candidate for a future, dedicated requirement; also not adopted: setting the car's own generic default restitution/friction to any of these values (mirroring FR-062's own `RigidBody::ball` constructor) — unlike the ball, real Rocket League has no single "the car's own" restitution/friction value, every real number found here is contact-pair-specific, so picking one for a generic default would be arbitrary, not confirmed; `combine_restitution`/`combine_friction`'s own kept average formula is unchanged; zero production code changed, no new tests, crate remains at 309), and, since FR-064, `drive::JUMP_HOLD_MAX_DURATION`'s own doc comment's long-flagged gap — that real Rocket League scales its jump-hold acceleration down during a `JUMP_MIN_TIME` (0.025s) mandatory window rather than applying it flat, an unmodeled "two-phase ramp" since FR-031's own original audit — was closed by fetching and reading RocketSim's real `Car.cpp` (`_UpdateJump`) directly (matching FR-058/FR-059's own real-implementation-file method) and confirming the exact mechanism: the hold force keeps applying, scaled by `JUMP_PRE_MIN_ACCEL_SCALE = 0.62f`, for `JUMP_MIN_TIME` seconds regardless of whether `jump` is still held, not merely a slower ramp; added `drive::JUMP_MIN_TIME`/`JUMP_PRE_MIN_ACCEL_SCALE` and reworked `apply_driven_forces`'s hold-acceleration check to derive elapsed time since the press from the existing `jump_hold_time_remaining` state rather than tracking a second field, so no caller needed to change; a genuine behavioral fix, not a doc correction; 3 new tests, bringing the crate to 312), and, since FR-065, `drive::STEER_TORQUE`'s own long-standing "no public reference at all" status was checked by fetching and reading RocketSim's real `Car.cpp` (`_UpdateWheels`) directly (matching FR-058/FR-059/FR-064's own method), finding real Rocket League's steering isn't a direct yaw-torque model at all — a wheel's steer angle (from a confirmed `STEER_ANGLE_FROM_SPEED_CURVE`) feeds Bullet's own raycast vehicle system (`btVehicleRL`), whose per-wheel lateral tire friction (a further confirmed `LAT_FRICTION_CURVE`) is what actually turns the car, an architecture this port's single-rigid-box car has no way to represent, the same category FR-063 already established; the confirmed curve's own shape is also the opposite of this port's own `speed_factor` (real turning ability is highest at a standstill and decreases with speed; this port's own scales up from zero with speed); corrected `STEER_TORQUE`'s/`MAX_CAR_SPEED`'s own doc comments and the `speed_factor` call site's comment, and fixed adjacent stale Open-Questions text wrongly still claiming `AIR_CONTROL_TORQUE`/`JUMP_HOLD_MAX_DURATION`/`JUMP_HOLD_ACCELERATION` had no public reference (contradicting FR-057's/FR-031's own already-shipped findings); not adopted as a fix since the real curve's wheel-angle-to-torque translation depends on tire-slip friction this port doesn't model; zero production code changed, no new tests, crate remains at 312), and, since FR-066, `drive::HANDBRAKE_FRICTION_MULTIPLIER`'s own doc comment was checked by fetching and reading RocketSim's real `Car.cpp` (`_UpdateWheels`) directly (matching FR-058/FR-059/FR-064/FR-065's own method), continuing FR-065's own `_UpdateWheels` investigation, and found real Rocket League's handbrake friction reduction is genuinely anisotropic — a confirmed constant `0.1` lateral factor and a separate, near-constant `~0.9` longitudinal factor, applied to tire friction independently rather than one shared multiplier; this port's own pre-existing `HANDBRAKE_FRICTION_MULTIPLIER = 0.1` happens to match the real lateral-only factor exactly, a striking coincidence, not a confirmation, since applying that same `0.1` to this port's own single isotropic friction scalar also wrongly crushes longitudinal grip to a tenth, where real Rocket League keeps it near `0.9`; corrected `HANDBRAKE_FRICTION_MULTIPLIER`'s own doc comment and the module doc's "Handbrake" and "commonly-cited constants" paragraphs; not adopted as a fix since `solver::friction_directions`' own two tangent rows currently share one combined-friction scalar, and giving handbrake a genuinely different lateral-vs-longitudinal factor would mean threading a second friction coefficient through every one of `solver.rs`'s several row-limit call sites plus a way to know which body is handbraking — the same architecture-mismatch category FR-063/FR-065 already established; zero production code changed, no new tests, crate remains at 312), and, since FR-067, `drive::WALL_JUMP_HORIZONTAL_SPEED`'s own doc comment was checked by fetching and reading RocketSim's real `Car.cpp` (`_UpdateJump`) directly (matching FR-058/FR-059/FR-064/FR-065/FR-066's own method) and found real Rocket League has no separate wall-jump mechanic or constant at all — `_UpdateJump` applies exactly one impulse, `GetUpDir() * mutatorConfig.jumpImmediateForce` (the same real value this port's own `JUMP_SPEED` already matches), gated only on `isOnGround`, itself defined purely by wheel-contact count with no floor-vs-wall distinction, with no `WALL_JUMP`-named constant anywhere in `RLConst.h`; since FR-065 already confirmed real cars ride Bullet's own raycast vehicle system, a car driving on a wall has its own orientation continuously tipped by wheel/suspension contact forces to match that wall, so `GetUpDir()` already points along the wall's outward normal by the time a wall jump fires — real Rocket League's "wall jump" is thus the identical single grounded-jump impulse, not a distinct horizontal-plus-vertical composite; corrected `WALL_JUMP_HORIZONTAL_SPEED`'s own doc comment, the module doc's own wall-jump section, and the "commonly-cited constants" paragraph; not adopted as a fix since this port's car has no wheels, raycasting, or surface-tracking orientation system at all (the same architecture gap FR-065 found for steering), so its own two-component composite substitute remains deliberate and necessary; zero production code changed, no new tests, crate remains at 312), and, since FR-068, `drive::AIR_CONTROL_TORQUE`'s own single flat magnitude applied identically to all three air-control axes (pitch/yaw/roll) — a gap `RB-PHYSICS-001-FR-031`'s own audit had already found real per-axis air-control torque coefficients exist for but couldn't adopt outright, since they're absolute torques calibrated against real Rocket League's own specific car mass/inertia tensor, the same "false precision" reasoning that kept `AIR_CONTROL_TORQUE` a placeholder — was closed after fetching and reading RocketSim's real `Car.cpp` (`_UpdateAirTorque`) directly (matching FR-058/FR-059/FR-064/FR-065/FR-066/FR-067's own method) and finding the real mechanism structurally identical to this port's own (a direct per-axis torque scaled by analog input), unlike steering's or handbrake's own architecture mismatches, with `RLConst.h` confirming `CAR_AIR_CONTROL_TORQUE = Vec(130, 95, 400)` (pitch-yaw-roll order); adopted the confirmed real per-axis *ratio* (not the real absolute magnitudes, still ruled out by the same "false precision" finding), the same "shape confirmed, magnitude not" split FR-058/FR-059 already used — added `drive::AIR_CONTROL_YAW_SCALE = 95.0 / 130.0` and `drive::AIR_CONTROL_ROLL_SCALE = 400.0 / 130.0`, redefined `AIR_CONTROL_TORQUE` (value unchanged) as pitch's own magnitude specifically, and wired both scales into `apply_driven_forces`'s yaw/roll torque application; a genuine behavioral fix, not a doc correction — yaw now produces measurably less angular velocity than pitch for equal analog input, and roll measurably more; 2 new tests added (`yaw_air_control_is_scaled_down_from_pitch_by_the_confirmed_real_ratio`, `roll_air_control_is_scaled_up_from_pitch_by_the_confirmed_real_ratio`), bringing the crate to 314, and, since FR-069, `drive::DODGE_ANGULAR_SPEED`'s own flat, instantaneous angular-velocity kick applied at dodge start was checked by fetching and reading RocketSim's real `Car.cpp` (`_UpdateDoubleJumpOrFlip` and `_UpdateAirTorque`) directly (matching FR-058/FR-059/FR-064/FR-065/FR-066/FR-067/FR-068's own method), continuing `RB-PHYSICS-001-FR-031`'s own original constant-calibration audit thread, and found real Rocket League's flip spin is not an instantaneous kick at all but a continuous per-axis torque — `_UpdateDoubleJumpOrFlip` records a per-axis `flipRelTorque` once, at flip start, and a separate, later step, `_UpdateAirTorque`, applies `flipRelTorque * Vec(FLIP_TORQUE_X, FLIP_TORQUE_Y, 0)` every physics tick for as long as `isFlipping = hasFlipped && flipTime < FLIP_TORQUE_TIME` holds, a hard 0.65s cutoff with no decay or ramp before it — confirming the real `FLIP_TORQUE_X = 260`/`FLIP_TORQUE_Y = 224`/`0.65s` constants FR-031's own original audit had already found are this mechanism's own coefficients and duration, not free-standing values; not adopted as a fix, since the real resulting spin rate depends on real Rocket League's own specific hitbox inertia tensor, which this port's placeholder car body doesn't match (the same "false precision" reasoning FR-031 already applied elsewhere), and reproducing the real timed-torque *shape* (not just magnitude) would require new per-car elapsed-flip-time state threaded through `PhysicsWorld`, a redesign FR-059's own Non-goals already flagged as out of scope; `DODGE_ANGULAR_SPEED`'s own doc comment, the module doc's dodge section, and the "commonly-cited constants" paragraph corrected; zero production code changed, no new tests, crate remains at 314), and, since FR-070, `RB-PHYSICS-001-FR-069`'s own fetch of RocketSim's real `Car.cpp` (`_UpdateAirTorque`) had itself surfaced a `pitchTorqueScale` factor that fetch left unresolved, scoped out at the time as "an additional speed- or state-dependent scale this requirement's own fetch surfaced but didn't fully characterize" — closed by fetching and reading `Car.cpp` again (matching FR-058/FR-059/FR-064/FR-065/FR-066/FR-067/FR-068/FR-069's own method) and finding the real mechanism: real Rocket League's flip-cancel — while still flipping (`isFlipping`), if the flip's own stored pitch-torque component (`flipRelTorque.y()`) is nonzero and `controls.pitch` is held in that same sign, `pitchScale = 1 - abs(controls.pitch)` multiplies only the pitch-axis torque component, continuously, every tick, for as long as the flip continues — a continuous, proportional, pitch-axis-only reduction driven by holding the stick, not a discrete jump-press trigger, and one a sideways (roll-only) dodge, having no pitch-torque component at all, can never trigger; this port's own flip-cancel (`RB-PHYSICS-001-FR-016`, in `drive.rs`'s `apply_driven_forces`) triggers instead on a fresh jump press while airborne (not touching a wall, double-jump already spent, `dodge_flip_active` still set) and zeros `RigidBody.angular_velocity` outright, every axis regardless of dodge direction — its own doc comment's claim that this "matched real Rocket League" had never actually been checked against real source, and is corrected here; not adopted as a fix, since this port's dodge is a single flat angular-velocity kick with no per-axis torque split to partially cancel (the same architecture gap `RB-PHYSICS-001-FR-069` already confirmed for the dodge's own spin), and reproducing the real continuous-hold trigger and pitch-only scope would need the same continuous per-axis torque and elapsed-flip-time state `RB-PHYSICS-001-FR-059`'s own Non-goals already flagged as out of scope; `drive.rs`'s own module doc flip-cancel paragraph corrected (the inaccurate "matching real Rocket League" claim removed, the real mechanism and why it isn't adopted added), and `RB-PHYSICS-001-FR-016`'s own Requirements entry given a forward citation to this one; zero production code changed, no new tests, crate remains at 314), and, since FR-071, `RB-PHYSICS-001-FR-068`'s own Non-goals had left RocketSim's confirmed `CAR_AIR_CONTROL_DAMPING = Vec(30, 20, 50)` — a per-axis air-control damping term this port has no equivalent for at all — as "a separate, independent addition left for a future requirement" without examining the mechanism itself; closed by fetching RocketSim's real `Car.cpp` again (the same fetch `RB-PHYSICS-001-FR-070` used to characterize `pitchTorqueScale`) and finding the full mechanism: for each axis, real air control subtracts a damping torque `(angular velocity along that axis) * CAR_AIR_CONTROL_DAMPING[axis] * (1 - abs(analog input on that axis))` from the applied torque before scaling by inertia — pitch's own input term additionally multiplies by `pitchTorqueScale` — so releasing the stick on an axis gives full damping strength there, continuously bleeding off any existing spin, while holding it fully zeroes the damping, granting full torque authority with no resistance; not adopted as a fix, since unlike `AIR_CONTROL_TORQUE`'s own pitch/yaw/roll ratio (`RB-PHYSICS-001-FR-068`), which scaled a torque this port already applies the same way, this port has no existing damping term at all to apply a ratio to — introducing one is a genuinely new mechanism, not a multiplier transfer — and its absolute coefficients are also calibrated against real Rocket League's own specific inertia tensor, the same "false precision" reasoning that already keeps `AIR_CONTROL_TORQUE` itself a placeholder; `drive.rs`'s own module doc air-control paragraph and `AIR_CONTROL_ROLL_SCALE`'s own doc comment corrected with the confirmed mechanism, and `RB-PHYSICS-001-FR-068`'s own Non-goals given a forward citation to this one; zero production code changed, no new tests, crate remains at 314), and, since FR-072, `RB-PHYSICS-001-FR-059`'s own Non-goals had already found and flagged a genuine behavioral gap — this port's dodge summed each axis' own full-strength `(pitch, roll)` contribution independently, so a diagonal dodge (both axes held) came out `sqrt(2)`-ish times faster than an axis-aligned one, unlike real Rocket League's own normalized direction — "a separate, independent behavioral question this requirement doesn't take on"; closed by fetching RocketSim's own `Car.cpp` (`_UpdateDoubleJumpOrFlip`, matching this port's own established real-implementation-file method) and confirming the exact real mechanism: `dodgeDir = btVector3(-controls.pitch, controls.yaw + controls.roll, 0)`, then `dodgeDir.safeNormalized()` — normalized to unit length *before* `FLIP_INITIAL_VEL_SCALE` and the further per-axis forward/backward/side speed scaling (`dodge_speed_scale`/`dodge_pitch_is_backward`, `FR-059`'s own already-adopted finding) are applied; confirmed a pure geometric operation this port's own model represents exactly — unlike a wheeled-vehicle/tire-slip model or a continuous-torque timing state this port's architecture can't represent, normalizing a 2D direction vector needs no new machinery, so it transfers cleanly the same way `FR-058`/`FR-059`/`FR-068`'s own adopted ratios do, regardless of `DODGE_SPEED`'s own uncalibrated base magnitude; a genuine behavioral fix, not a doc correction — added `drive::normalize_dodge_direction(pitch, roll) -> (f32, f32)`, a small pure helper, wired into both the ground-dodge and wall-jump-dodge code paths in `apply_driven_forces`, with the existing per-axis `DODGE_DEADZONE` trigger checks and `dodge_pitch_is_backward`'s own sign classification left unchanged (both still read the raw stick values) — only the magnitude each axis contributes to `DODGE_SPEED`/`DODGE_ANGULAR_SPEED` now comes from the normalized pair; this port's own sign convention is kept (unlike the reference's own negated `-controls.pitch`) and real yaw input's own contribution to `dodgeDir` is not folded in — this port's dodge direction stays pitch/roll only, `FR-059`'s own Non-goals' own separate, pre-existing simplification; the two existing diagonal-dodge tests (`a_diagonal_dodge_combines_pitch_and_roll`, `a_diagonal_wall_jump_dodge_combines_pitch_and_roll`) updated to assert the corrected per-axis magnitude (`DODGE_SPEED / sqrt(2)` for an equal pitch/roll split, with the total magnitude still matching an axis-aligned dodge's own `DODGE_SPEED`), and 3 new tests added directly exercising `normalize_dodge_direction`, bringing the crate to 317) | Verified (unit tests; not yet scored against real data) | RB-VERIFY-003 | baileyrd | [spec](physics/RB-PHYSICS-001-physics-core-port.md) | `crates/rb_physics_bullet` tests (389) |
| `RB-SIM-001` | Deterministic Simulation | 0.1.0 | Draft (placeholder) | Not Started | Not Verified | RB-PHYSICS-001 | baileyrd | [spec](simulation/RB-SIM-001-deterministic-simulation.md) | — |
| `RB-NET-001` | Client Prediction and Rollback Netcode | 0.1.0 | Draft (placeholder) | Not Started | Not Verified | RB-SIM-001 | baileyrd | [spec](netcode/RB-NET-001-client-prediction-rollback.md) | — |

See [docs/roadmap/ROADMAP.md](../roadmap/ROADMAP.md) for how these map to
phases, and [docs/traceability/TRACEABILITY.md](../traceability/TRACEABILITY.md)
for requirement-level traceability to implementation and verification.
