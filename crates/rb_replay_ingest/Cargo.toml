[package]
name = "rb_replay_ingest"
description = "Replay-file ingestion adapter (RB-VERIFY-001): implements PhysicsStateSource by parsing .replay files. Parser backend not yet wired up."
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
publish.workspace = true

[lints]
workspace = true

[dependencies]
rb_domain = { path = "../rb_domain" }
