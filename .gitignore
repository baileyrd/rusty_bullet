/target
**/*.rs.bk
*.pdb
.DS_Store

# Local BakkesMod captures and replay fixtures are large binary artifacts
# and may contain data from the user's own matches; keep them out of git.
/captures/
/replays/
!crates/**/fixtures/**
